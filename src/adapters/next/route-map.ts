/**
 * NextJsAdapter.mapRouteToEntry — Phase 4, NEXT-01/02/03.
 *
 * STUB — full implementation arrives in plan 04-03 task 2.
 * This stub exists only so the segments.ts classifier tests can run.
 */

import type { RouteMatch } from "../types.js";

export async function matchRoute(_absRoot: string, _route: string): Promise<RouteMatch> {
  return { matched: false, entries: [], params: {}, slots: {} };
}
