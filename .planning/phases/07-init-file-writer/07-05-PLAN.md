---
phase: 07-init-file-writer
plan: 05
type: execute
wave: 3
depends_on: [1, 4]
files_modified:
  - src/cli.ts
autonomous: false
requirements:
  - INIT-01
  - INIT-02
  - INIT-03
  - INIT-11
user_setup: []
tags:
  - cli
  - dispatch
  - regression-gate

must_haves:
  truths:
    - "Running `node dist/cli.js` (no args) calls `startServer()` exactly as in v1.0 — existing smoke test `test/mcp/smoke.spawn.test.ts` remains green (INIT-02)"
    - "Running `node dist/cli.js --init` invokes `runInit(values)` then exits with the returned code; `startServer()` is never invoked on this branch"
    - "Running `node dist/cli.js --init --target foo` exits with code 1 and writes an error message to stderr before any file is touched (INIT-03)"
    - "Running `node dist/cli.js --help` writes help text to stderr and exits 0; running `--version` writes the tool version to stderr and exits 0"
    - "stdout receives zero bytes on any `--init` or `--help` or `--version` code path (INIT-11 stdout invariant at the entry point)"
    - "Unhandled errors from `runInit` are caught and rendered as `[init] error <message>` to stderr with exit code 1"
  artifacts:
    - path: "src/cli.ts"
      provides: "argv dispatch fork: --init path vs existing startServer path; preserves v1.0 catch block byte-for-byte in the else branch"
      contains: "parseArgs"
      contains: "runInit"
      contains: "startServer"
  key_links:
    - from: "src/cli.ts"
      to: "src/init/index.ts"
      via: "import { runInit }"
      pattern: "from ['\"]\\./init/index\\.js['\"]"
    - from: "src/cli.ts"
      to: "src/init/argv.ts"
      via: "import parseInitArgs (or inline parseArgs + validation — see action)"
      pattern: "parseArgs|parseInitArgs"
    - from: "src/cli.ts"
      to: "src/mcp/server.ts"
      via: "existing import preserved byte-for-byte in else branch"
      pattern: "startServer"
---

<objective>
Add the argv dispatch fork to `src/cli.ts`. This is the only edit to existing v1.0 code. When `--init` is present, route to `runInit` and exit; otherwise fall through to the existing `startServer()` invocation byte-for-byte. Add `--help` and `--version` short-circuits before either branch.

Purpose: INIT-01 wire-up (the npx entry point gains the `--init` capability) and INIT-02 regression gate (the bare `npx ui-hierarchy-mcp` invocation behaves exactly as in v1.0). All file-writer logic already lives in `src/init/` from Wave 1+2; this plan only adds dispatch.
Output: Updated `src/cli.ts` + the v1.0 smoke test still green + a basic shape test asserting the dispatch decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-init-file-writer/07-SPEC.md
@.planning/phases/07-init-file-writer/07-CONTEXT.md
@.planning/phases/07-init-file-writer/07-PATTERNS.md
@.planning/phases/07-init-file-writer/07-RESEARCH.md
@src/cli.ts
@src/mcp/server.ts
@src/mcp/log.ts
@src/init/argv.ts
@src/init/index.ts
@test/mcp/smoke.spawn.test.ts

<interfaces>
<!-- Existing src/cli.ts (v1.0) — lines 1-11 -->
```typescript
import { log } from "./mcp/log.js";
import { startServer } from "./mcp/server.js";

// Note: shebang (#!/usr/bin/env node) is injected by tsup banner — do NOT add it here.

startServer().catch((err: unknown) => {
  log.error("server error", {
    message: err instanceof Error ? err.message : String(err),
  });
  process.exit(1);
});
```

<!-- New src/cli.ts target shape (PATTERNS Pattern: src/cli.ts dispatch fork) -->
```typescript
import { parseArgs } from 'node:util';
import { log } from "./mcp/log.js";
import { startServer } from "./mcp/server.js";
import { runInit } from "./init/index.js";
import { parseInitArgs } from "./init/argv.js";
// ... parseArgs call ...
// ... help / version short-circuits ...
// if (values.init) { runInit(...).then(process.exit).catch(stderrError) }
// else { startServer().catch(/* v1.0 block VERBATIM */) }
```

The v1.0 catch block (lines 6-10 of the original cli.ts) MUST appear verbatim inside the `else` branch — this is the INIT-02 regression contract.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parseArgs dispatch fork to src/cli.ts; preserve v1.0 path byte-for-byte in else branch</name>
  <files>src/cli.ts</files>
  <read_first>
    - src/cli.ts (current 11-line entry — copy lines 6-10 verbatim into the else branch)
    - src/init/argv.ts (parseInitArgs API)
    - src/init/index.ts (runInit signature)
    - test/mcp/smoke.spawn.test.ts (the regression gate this plan must keep green)
    - .planning/phases/07-init-file-writer/07-PATTERNS.md `src/cli.ts (modify — dispatch fork)` section
    - .planning/phases/07-init-file-writer/07-RESEARCH.md Code Examples — Complete cli.ts Dispatch Fork; Pitfall 7 (exit code placement)
    - .planning/phases/07-init-file-writer/07-CONTEXT.md D-03
    - .planning/phases/07-init-file-writer/07-SPEC.md INIT-01, INIT-02, INIT-11
  </read_first>
  <action>
    Edit src/cli.ts. The final shape:

    1. Imports at the top, in this order: `parseArgs` from `node:util`; `log` from `./mcp/log.js`; `startServer` from `./mcp/server.js`; `runInit` from `./init/index.js`; `parseInitArgs` from `./init/argv.js`.

    2. Preserve the existing comment about the tsup banner shebang (do NOT add a source-level shebang).

    3. Call `parseInitArgs(process.argv.slice(2))` to get the validated flags. If the result is `{ok:false, message}`: write `[init] error ${message}\n` to stderr and `process.exit(1)`. This single call handles strict unknown-flag rejection AND target enum validation in one place (INIT-03 wire-up).

    4. Define the `HELP_TEXT` constant as a multi-line string listing: usage `npx ui-hierarchy-mcp [--init [--target ...] [--dry-run] [--force]]`; the four targets `claude | codex | cursor | copilot`; the four flags; pointer to the npm package URL. Keep it concise per CONTEXT Claude's Discretion. Write `HELP_TEXT` to `process.stderr.write` (per RESEARCH Open Question 3 — stderr is safer given the stdout invariant) when `--help` was passed (you can re-parse with raw parseArgs or have parseInitArgs expose the boolean — simplest: re-call parseArgs once at the top to read `help` and `version` booleans before parseInitArgs validates targets).

       Simpler approach: Use a single `parseArgs` call up front for `help` and `version` (with `strict: false` so unknown flags pass through), short-circuit on those, then call `parseInitArgs` with `strict:true` to validate the full set. Choose this approach to keep the help/version code-path independent of `--target` validity.

    5. If `values.help` is true: `process.stderr.write(HELP_TEXT)`; `process.exit(0)`.

    6. If `values.version` is true: `process.stderr.write(\`${__TOOL_VERSION__}\n\`)`; `process.exit(0)`.

    7. If `parseInitArgs` succeeded and `flags.init` is conceptually true (the orchestrator treats the presence of `--init` as the trigger; expose this via parseInitArgs or a second light parseArgs check on `values.init`): call `runInit(flags).then((code) => process.exit(code)).catch((err) => { process.stderr.write(\`[init] error ${err instanceof Error ? err.message : String(err)}\n\`); process.exit(1); })`.

       Note: argv.ts already returns `flags.targets`, `flags.dryRun`, `flags.force`. Decide whether `parseInitArgs` should also return `flags.init: boolean` so cli.ts has a single source of truth. If it currently does not, add the field to argv.ts in Plan 01's `InitFlags` interface (this is a backward-compatible additive change; if Plan 01 has already been merged, surface this as a small follow-up edit to argv.ts in this task — record it as a 2-line addition).

    8. Else branch (no `--init`): paste the v1.0 catch block from the original cli.ts lines 6-10 VERBATIM:
       ```
       startServer().catch((err: unknown) => {
         log.error("server error", {
           message: err instanceof Error ? err.message : String(err),
         });
         process.exit(1);
       });
       ```
       Do not modify these five lines. The regression test `test/mcp/smoke.spawn.test.ts` covers this path.

    9. Do NOT write to `process.stdout` from any branch.

    10. Do NOT add a source-level `#!/usr/bin/env node` shebang — the tsup banner option in tsup.config.ts already injects it during build.

    Open the file and check whether `parseInitArgs` returns `flags.init`; if not, also edit src/init/argv.ts to add `init: boolean` to the `InitFlags` interface and set it from `Boolean(values.init)` in parseInitArgs. Update test/init/argv.test.ts only if a test asserts on the InitFlags shape (additive field; existing assertions should still pass).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run test/mcp/smoke.spawn.test.ts test/init/ --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` succeeds.
    - `npx vitest run test/mcp/smoke.spawn.test.ts` passes (v1.0 MCP stdio handshake regression gate green — INIT-02).
    - `npx vitest run test/init/` passes (Wave 1+2 suites still green after argv.ts additive edit if any).
    - `grep -n "startServer" src/cli.ts` matches exactly the verbatim v1.0 block inside the else branch.
    - `grep -n "runInit" src/cli.ts` matches at least once.
    - `grep -n "parseArgs\|parseInitArgs" src/cli.ts` matches.
    - `grep -n "process.stdout" src/cli.ts` returns no matches (stdout invariant at entry point — INIT-11).
    - `grep -n "console\\." src/cli.ts` returns no matches.
    - `grep -n "#!/usr/bin/env node" src/cli.ts` returns no matches (banner is tsup-injected only).
    - File contains the v1.0 catch block byte-for-byte: `grep -A2 "startServer().catch" src/cli.ts` shows `log.error("server error"` exactly.
  </acceptance_criteria>
  <done>cli.ts dispatches to runInit on --init and falls through to the byte-for-byte v1.0 startServer path otherwise; --help and --version short-circuit to stderr; the v1.0 smoke test stays green, proving INIT-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke-test built dist/cli.js against a scratch directory</name>
  <what-built>
    Phase 7 `--init` File Writer feature is fully implemented across `src/init/` (8 files) and the dispatch fork in `src/cli.ts`. The build artifact at `dist/cli.js` (after `npm run build`) should now expose the `--init` subcommand while preserving the v1.0 MCP stdio server on the no-args path.
  </what-built>
  <how-to-verify>
    1. Build the project: `npm run build` (produces `dist/cli.js` with the tsup-injected shebang and `__INIT_MARKER_VERSION__` substituted).
    2. Create a scratch temp directory: `mkdir /tmp/ui-init-smoke && cd /tmp/ui-init-smoke`.
    3. Run `node /path/to/repo/dist/cli.js --init`. Expected:
       - `CLAUDE.md` is created in the scratch dir containing both `<!-- ui-hierarchy-mcp:start version=0.1 fingerprint=...` and `<!-- ui-hierarchy-mcp:end -->` markers.
       - Stderr shows one line: `[init] create CLAUDE.md`.
       - Stdout is empty (capture separately with `> /dev/null 2>&1` redirects to confirm).
       - Exit code is 0 (`echo $?`).
    4. Run `node /path/to/repo/dist/cli.js --init` a second time. Expected:
       - No change to CLAUDE.md bytes (verify with `sha256sum CLAUDE.md` before and after — must match).
       - Stderr shows `[init] noop CLAUDE.md`.
       - Exit code is 0.
    5. Run `node /path/to/repo/dist/cli.js --init --target claude,codex,cursor,copilot`. Expected:
       - All four files exist at correct paths (`CLAUDE.md`, `AGENTS.md`, `.cursor/rules/ui-hierarchy-mcp.mdc`, `.github/copilot-instructions.md`).
       - `.cursor/rules/ui-hierarchy-mcp.mdc` starts with `---\ndescription:` frontmatter above the marker block.
       - Stderr shows four `[init] ...` lines.
    6. Run `node /path/to/repo/dist/cli.js --init --target foo`. Expected:
       - Exit code 1.
       - Stderr contains `Unknown target` or similar mentioning `foo`.
       - No files were touched in the directory.
    7. Run `node /path/to/repo/dist/cli.js --init --dry-run --target claude,codex` in a fresh empty directory. Expected:
       - Stderr shows two `[init] would create ...` lines.
       - `ls` shows the directory is still empty.
       - Exit code 0.
    8. Run `node /path/to/repo/dist/cli.js` (no args) with an MCP stdio client (or rely on the smoke test passing). Expected: MCP stdio handshake works exactly as in v1.0; no `[init]` output anywhere.

    Confirm each step matches the expected behavior. If anything diverges, describe which step failed.
  </how-to-verify>
  <files>dist/cli.js (built artifact; no source files modified by this task)</files>
  <action>Perform the eight-step manual smoke verification described in <how-to-verify> against a freshly built dist/cli.js. This task makes no source changes; it is a human gate confirming end-to-end behavior on a real build artifact.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All eight smoke steps from <how-to-verify> produce the expected behavior; user has typed approved or the divergence has been addressed.</done>
  <resume-signal>Type "approved" if all eight smoke steps pass, or describe the divergence (which step, observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `process.argv` from the user's shell → cli.ts | Untrusted argv enters parseArgs |
| dispatch decision → side effects | `--init` runs file mutations; no-args runs the MCP server with full SDK surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Tampering | argv injection of unknown flags | mitigate | `parseInitArgs` uses `parseArgs({strict: true})` which throws `ERR_PARSE_ARGS_UNKNOWN_OPTION` on unknown flags; cli.ts catches via the `{ok:false, message}` envelope and exits 1 before any file write or server boot |
| T-07-23 | Spoofing | stdout contamination from `--init` path | mitigate | All cli.ts output uses `process.stderr.write`; `grep -n "process.stdout" src/cli.ts` returns zero matches (INIT-11 entry-point gate) |
| T-07-24 | EoP | --init path inadvertently boots MCP server | mitigate | `--init` branch calls `runInit().then(process.exit)` — `startServer()` is in the unreachable `else` arm. The smoke test (INIT-02) explicitly verifies the no-args branch still works; a separate test or manual verification (Task 2 checkpoint) confirms the --init branch never produces MCP framing on stdout. |
| T-07-25 | DoS | unhandled rejection from `runInit` orphans the process | mitigate | The `.catch` on the `runInit().then(...)` chain calls `process.exit(1)` after writing `[init] error ...` to stderr (RESEARCH Pitfall 7) |
| T-07-26 | Tampering | banner shebang stripped or mangled | accept | `tsup.config.ts:16` configures `banner.js: "#!/usr/bin/env node"`; existing v1.0 builds already validate this works. No new risk introduced by Phase 7. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npx vitest run` (full suite) passes — including `test/mcp/smoke.spawn.test.ts` (INIT-02 regression) and all `test/init/*` suites.
- All `grep` source assertions in Task 1's `acceptance_criteria` pass.
- The human-verify checkpoint (Task 2) confirms end-to-end behavior on a real `npm run build` artifact.
- No file outside `src/cli.ts` (and optionally `src/init/argv.ts` for the additive `init: boolean` field) is touched.
</verification>

<success_criteria>
- All `must_haves.truths` hold:
  - The v1.0 smoke test stays green (INIT-02 regression).
  - `--init` invokes runInit and exits with its returned code.
  - `--target foo` exits 1 before any file write.
  - `--help` / `--version` short-circuit to stderr.
  - stdout invariant preserved.
  - runInit errors render as `[init] error <message>` to stderr.
- Phase 7 is feature-complete after this plan ships.
</success_criteria>

<output>
After completion, create `.planning/phases/07-init-file-writer/07-05-SUMMARY.md`.
</output>
