---
phase: "10-interface-widening-analyzer-de-next-ification"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/adapters/next/NextJsAdapter.ts
  - src/core/Analyzer.ts
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02

must_haves:
  truths:
    - "NextJsAdapter implements all 8 FrameworkAdapter methods with no TypeScript errors"
    - "Analyzer.ts contains zero Next.js-specific string literals as logic conditions"
    - "The 5 module-scope functions (isPageFile, isLayoutFile, isSpecialFile, deriveRoutesFromEntries, collectChildrenSlotLines) are absent from Analyzer.ts"
    - "Full vitest suite exits 0 with >= 360 passing tests"
    - "Snapshots are re-locked after migration — zero diverging snapshots remain"
  artifacts:
    - path: "src/adapters/next/NextJsAdapter.ts"
      provides: "Full 8-method NextJsAdapter implementation"
      contains: "classifyEntry enumerateRoutes slotMarker"
    - path: "src/core/Analyzer.ts"
      provides: "Framework-agnostic Analyzer with adapter delegation"
      exports: ["Analyzer"]
  key_links:
    - from: "src/core/Analyzer.ts (buildUnionIR)"
      to: "this.adapter.enumerateRoutes(this.root)"
      via: "single async call replacing discoverEntries + deriveRoutesFromEntries"
      pattern: "adapter\\.enumerateRoutes"
    - from: "src/core/Analyzer.ts (buildRouteTree)"
      to: "this.adapter.classifyEntry(entry)"
      via: "replaces isPageFile / isLayoutFile calls"
      pattern: "adapter\\.classifyEntry"
    - from: "src/core/Analyzer.ts (private collectChildrenSlotLines)"
      to: "adapter.slotMarker(expr.name, \"\")"
      via: "adapter capture in traverse visitor"
      pattern: "adapter\\.slotMarker"
---

<objective>
Implement the three new methods in NextJsAdapter (classifyEntry, enumerateRoutes, slotMarker),
then de-Next-ify Analyzer.ts by removing its 5 module-scope functions and replacing every callsite
with adapter delegation calls. Finish with a snapshot re-lock pass to confirm byte-identical output.

Purpose: Complete Phase 10 — after this plan, Analyzer.ts contains zero framework-specific logic,
and any downstream adapter (ExpoRouterAdapter, Phase 12) can implement the widened interface
without touching core.
Output: Migrated NextJsAdapter.ts, de-next-ified Analyzer.ts, re-locked snapshots, green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-SPEC.md
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-CONTEXT.md
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-RESEARCH.md
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-PATTERNS.md
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-01-SUMMARY.md

<interfaces>
<!-- New 8-method FrameworkAdapter interface (from Plan 01 output) -->
export interface FrameworkAdapter {
  detect(absRoot: string): Promise<boolean> | boolean;
  discoverEntries(absRoot: string): Promise<string[]> | string[];
  resolveModule(ctx: ParseContext, fromFile: string, specifier: string, importedName: string): ResolveResult;
  extractComponents(ctx: ParseContext, entryFiles: string[], opts?: { fullClasses?: boolean }): ComponentDefinition[];
  mapRouteToEntry(absRoot: string, route: string): Promise<RouteMatch> | RouteMatch;
  classifyEntry(absPath: string): "page" | "layout" | "special" | "other";
  enumerateRoutes(absRoot: string): string[] | Promise<string[]>;
  slotMarker(name: string, importSource: string): boolean;
}

<!-- NextJsAdapter existing pattern (src/adapters/next/NextJsAdapter.ts) -->
<!-- Object literal: export const NextJsAdapter: FrameworkAdapter = { ... } -->
<!-- Already imports: discoverEntries as discoverNextEntries from "./discover.js", toForwardSlash from "../../core/paths.js" -->

<!-- Analyzer.ts leak sites to remove (exact line numbers from RESEARCH.md) -->
<!-- - collectChildrenSlotLines: lines 491–507 (module-scope function) -->
<!-- - isPageFile: lines 663–666 -->
<!-- - isSpecialFile: lines 668–671 -->
<!-- - isLayoutFile: lines 673–676 -->
<!-- - deriveRoutesFromEntries: lines 1194–1233 -->
<!-- Callsites: -->
<!--   buildTreeForEntry line 844: collectChildrenSlotLines(cachedParse.ast) -->
<!--   buildRouteTree lines 898, 902: isPageFile(entries[i]), isLayoutFile(e) -->
<!--   buildUnionIR lines 968–984: discoverEntries + deriveRoutesFromEntries two-call pattern -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 3 new methods in NextJsAdapter</name>
  <files>src/adapters/next/NextJsAdapter.ts</files>
  <read_first>
    - src/adapters/next/NextJsAdapter.ts — full file; confirm current method count, import list, and object literal shape before adding methods
    - src/adapters/next/discover.ts — confirm exact export name of the discovery function (exported as `discoverEntries`, imported as `discoverNextEntries` in NextJsAdapter)
    - src/core/paths.ts — confirm `toForwardSlash` export signature
    - src/core/Analyzer.ts — lines 1194–1233: read `deriveRoutesFromEntries` function body verbatim before migrating it into enumerateRoutes
    - src/core/Analyzer.ts — lines 663–676: read `isPageFile`, `isSpecialFile`, `isLayoutFile` regex patterns verbatim before migrating them into classifyEntry
    - .planning/phases/10-interface-widening-analyzer-de-next-ification/10-PATTERNS.md — sections "classifyEntry implementation", "enumerateRoutes implementation", "slotMarker implementation"
    - .planning/phases/10-interface-widening-analyzer-de-next-ification/10-RESEARCH.md — "Pitfall 1: isSpecialFile catches layout files too" and "Pitfall 4: Snapshot divergence from enumerateRoutes ordering"
  </read_first>
  <action>
    Add three methods to the NextJsAdapter object literal in src/adapters/next/NextJsAdapter.ts, after the existing `mapRouteToEntry` method:

    classifyEntry(absPath: string): "page" | "layout" | "special" | "other" —
    Compute `base` via `toForwardSlash(absPath).split("/").pop() ?? ""`.
    Check in this exact order (CRITICAL — order matters, see Pitfall 1):
      1. If matches `/^page\.(tsx|jsx|ts|js)$/` → return "page"
      2. If matches `/^layout\.(tsx|jsx|ts|js)$/` → return "layout"  (MUST be before "special" check)
      3. If matches `/^(layout|template|loading|error|not-found|default)\.(tsx|jsx|ts|js)$/` → return "special"
      4. Otherwise → return "other"

    async enumerateRoutes(absRoot: string): Promise<string[]> —
    Call `const entries = await discoverNextEntries(absRoot)`. Then inline the full body of
    `deriveRoutesFromEntries` (Analyzer.ts lines 1194–1233) with these adaptations:
      - `fwdRoot` = `toForwardSlash(absRoot)` (parameter, not `toForwardSlash(this.root)`)
      - `entries` comes from the local `discoverNextEntries` call (D-01)
      - Keep the `Array.from(routes).sort()` final return verbatim (Pitfall 4 — sort order must be preserved)
      - The page-file check inside the loop uses the regex `/^page\.(tsx|jsx|ts|js)$/` directly (same as classifyEntry, but inlined to avoid `this.classifyEntry` — this is an object literal, not a class)
    Do NOT call `this.classifyEntry` inside enumerateRoutes — `this` in an object literal method does not have access to sibling methods via `this` in the same way as a class; use the inline regex instead, or extract a module-scope helper `function deriveRoutes(entries, fwdRoot)` if preferred.

    slotMarker(name: string, _importSource: string): boolean —
    Single expression: `return name === "children"`. Per D-05, importSource is ignored for Next.js.

    After adding the three methods, run `npx tsc --noEmit` to confirm no TypeScript errors.
    Then run `vitest run test/adapters/NextJsAdapter.test.ts` — all tests should now PASS (GREEN).
  </action>
  <verify>
    <automated>cd E:\ui-to-hierarch && npx vitest run test/adapters/NextJsAdapter.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/next/NextJsAdapter.ts contains `classifyEntry(absPath:` method
    - src/adapters/next/NextJsAdapter.ts contains `enumerateRoutes(absRoot:` method
    - src/adapters/next/NextJsAdapter.ts contains `slotMarker(name:` method
    - `vitest run test/adapters/NextJsAdapter.test.ts` exits 0 — all tests GREEN
    - NextJsAdapter.classifyEntry("app/layout.tsx") returns "layout" not "special" (Pitfall 1 guard passes)
    - NextJsAdapter.slotMarker("children", "react") returns true
    - NextJsAdapter.slotMarker("Slot", "expo-router") returns false
    - No TypeScript compile errors: `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>NextJsAdapter implements all 8 FrameworkAdapter methods; unit tests green; no TS errors.</done>
</task>

<task type="auto">
  <name>Task 2: De-Next-ify Analyzer.ts — remove 5 functions, delegate to adapter, re-lock snapshots</name>
  <files>src/core/Analyzer.ts</files>
  <read_first>
    - src/core/Analyzer.ts — read the following targeted ranges before editing:
        lines 480–520: collectChildrenSlotLines module-scope function (to delete and replace with private method)
        lines 655–685: isPageFile, isSpecialFile, isLayoutFile module-scope functions (to delete)
        lines 830–860: buildTreeForEntry callsite context around line 844
        lines 888–912: buildRouteTree where isPageFile/isLayoutFile are called (lines 898, 902)
        lines 960–1000: buildUnionIR where discoverEntries + deriveRoutesFromEntries are called
        lines 1185–1240: deriveRoutesFromEntries function body (to delete)
    - .planning/phases/10-interface-widening-analyzer-de-next-ification/10-PATTERNS.md — all six "After" code patterns for each migration point
    - .planning/phases/10-interface-widening-analyzer-de-next-ification/10-RESEARCH.md — "Pitfall 2: Warning message string assertion breakage" and "Pitfall 3: traverse visitor this context capture"
    - test/ — run `grep -r "discoverEntries error" test/` before editing to check if any test snapshot or assertion asserts the old warning string (assumption A1 from RESEARCH.md)
  </read_first>
  <action>
    Perform all five migrations in Analyzer.ts in this order to minimize intermediate TS errors:

    MIGRATION 1 — Delete `collectChildrenSlotLines` module-scope function (lines 491–507) and add it as a private class method. The private method body is identical to the module-scope function except:
    - Add `private` keyword
    - Move inside the Analyzer class body (after the last existing method, before the closing brace)
    - Add `const adapter = this.adapter;` before the `traverse()` call (Pitfall 3 — capture `this.adapter` before entering non-arrow visitor callback)
    - Inside the JSXExpressionContainer visitor: replace `expr.name === "children"` with `adapter.slotMarker(expr.name, "")` (per D-04/D-05)

    MIGRATION 2 — Update callsite in buildTreeForEntry (line 844):
    Replace `collectChildrenSlotLines(cachedParse.ast)` with `this.collectChildrenSlotLines(cachedParse.ast)`

    MIGRATION 3 — Delete `isPageFile` (lines 663–666), `isSpecialFile` (lines 668–671), `isLayoutFile` (lines 673–676) module-scope functions entirely.

    MIGRATION 4 — Update callsites in buildRouteTree (lines 898–902):
    Replace `isPageFile(entries[i]!)` with `this.adapter.classifyEntry(entries[i]!) === "page"`
    Replace `isLayoutFile(e)` with `this.adapter.classifyEntry(e) === "layout"`

    MIGRATION 5 — Update buildUnionIR (lines 968–984):
    Remove the `let entries: string[]` + `discoverEntries` try/catch block.
    Remove the `const routes = deriveRoutesFromEntries(entries, this.root)` line.
    Replace with:
      `let routes: string[];`
      try block calling `routes = await this.adapter.enumerateRoutes(this.root);`
      catch block pushing `this.ctx.warnings.push(\`enumerateRoutes error: ${message}\`)` (per RESEARCH Pattern 6)
    The `routes` variable is then used in the downstream `for (const route of routes)` loop — adjust variable references as needed.

    MIGRATION 6 — Delete `deriveRoutesFromEntries` module-scope function (lines 1194–1233).

    After all 6 migrations: Before running tests, grep for the old warning string. Run:
      `grep -n "discoverEntries error" src/core/Analyzer.ts` — must return 0 matches
    If any test file asserts "discoverEntries error" as a string (pre-scan indicated by Pitfall 2), update that test assertion to "enumerateRoutes error".

    Then run: `npx vitest run` (full suite). If snapshot divergences appear, run:
      `npx vitest run --update-snapshots`
    Review each divergence before accepting — no blind accepts. Divergence should be zero if migrations are byte-identical. If any diverge, investigate the root cause before re-locking.

    ISLAND RULE CHECK: Do NOT add any new `import` statements for adapter modules. All new delegation calls use `this.adapter` (already injected as a class field). No new value-level imports from `src/adapters/` are permitted.
  </action>
  <verify>
    <automated>cd E:\ui-to-hierarch && npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "isPageFile|isLayoutFile|isSpecialFile|deriveRoutesFromEntries" src/core/Analyzer.ts` returns 0 matches (no remaining function definitions or calls)
    - `grep -E '"_layout"|"page\."|"not-found"|"children"' src/core/Analyzer.ts` returns 0 matches (no Next.js strings as logic conditions — comments excluded)
    - `grep "attachParallelSlot" src/core/Analyzer.ts` returns at least 1 match (function NOT moved to adapter — SPEC req 8)
    - `grep "collectChildrenSlotLines" src/core/Analyzer.ts` returns at least 1 match as a private method definition (not a module-scope function)
    - `grep "adapter.slotMarker" src/core/Analyzer.ts` returns exactly 1 match (inside collectChildrenSlotLines private method)
    - `grep "adapter.classifyEntry" src/core/Analyzer.ts` returns exactly 2 matches (in buildRouteTree)
    - `grep "adapter.enumerateRoutes" src/core/Analyzer.ts` returns exactly 1 match (in buildUnionIR)
    - `grep "enumerateRoutes error" src/core/Analyzer.ts` returns 1 match (updated warning string)
    - `grep "discoverEntries error" src/core/Analyzer.ts` returns 0 matches (old warning string removed)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 with >= 360 passing tests
    - No diverging snapshots: `npx vitest run --update-snapshots` produces 0 new diffs (or reviewed diffs only)
  </acceptance_criteria>
  <done>Analyzer.ts is framework-agnostic; all 5 leak sites removed; full suite green (>=360); snapshots re-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Analyzer → adapter | Analyzer calls adapter methods via injected `this.adapter`; island rule forbids value-level imports |
| traverse visitor | Babel visitor callbacks use captured `adapter` reference — `this` context is not the Analyzer class inside non-arrow callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | Analyzer.ts — island rule | mitigate | No new value-level imports from src/adapters/; test/architecture/island.test.ts enforces this at CI level |
| T-10-03 | Tampering | Snapshot re-lock | mitigate | Review each divergence individually; do not blindly accept all diffs; zero divergences expected if migration is byte-identical |
</threat_model>

<verification>
Phase 10 complete when all of the following pass:

1. `grep -E "isPageFile|isLayoutFile|isSpecialFile|deriveRoutesFromEntries" src/core/Analyzer.ts` → 0 matches
2. `grep -E '"_layout"|"page\."|"not-found"|"children"' src/core/Analyzer.ts` → 0 matches (logic conditions only)
3. `grep "attachParallelSlot" src/core/Analyzer.ts` → >= 1 match
4. `vitest run` → exits 0, >= 360 tests passing
5. `vitest run test/adapters/FrameworkAdapter.test.ts` → exits 0
6. `vitest run test/adapters/NextJsAdapter.test.ts` → exits 0
7. `tsc --noEmit` → exits 0
8. NextJsAdapter implements all 8 FrameworkAdapter methods (TypeScript enforces via `Record<keyof FrameworkAdapter, true>` stub in locking test)
</verification>

<success_criteria>
- NextJsAdapter has 8 methods: all original 5 plus classifyEntry, enumerateRoutes, slotMarker
- Analyzer.ts: 0 Next.js string literals in logic conditions; 0 module-scope file-classification functions; 0 discoverEntries/deriveRoutesFromEntries calls
- NextJsAdapter.slotMarker("children", "react") returns true (SPEC acceptance criterion)
- Full vitest suite: >= 360 passing, 0 failures, 0 diverging snapshots
- tsc --noEmit: 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-interface-widening-analyzer-de-next-ification/10-02-SUMMARY.md`
</output>
