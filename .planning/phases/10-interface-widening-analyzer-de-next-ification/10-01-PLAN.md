---
phase: "10-interface-widening-analyzer-de-next-ification"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adapters/FrameworkAdapter.ts
  - test/adapters/FrameworkAdapter.test.ts
  - test/adapters/NextJsAdapter.test.ts
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02

must_haves:
  truths:
    - "FrameworkAdapter interface exposes exactly 8 methods (5 original + classifyEntry, enumerateRoutes, slotMarker)"
    - "The structural locking test fails to compile if any of the 8 methods is absent from the interface"
    - "Unit tests for NextJsAdapter's 3 new methods exist and fail in RED state (implementations not yet present)"
  artifacts:
    - path: "src/adapters/FrameworkAdapter.ts"
      provides: "8-method interface contract for Phase 10 and all downstream adapters"
      contains: "classifyEntry enumerateRoutes slotMarker"
    - path: "test/adapters/FrameworkAdapter.test.ts"
      provides: "Structural locking test asserting exactly 8 methods"
      contains: "toHaveLength(8)"
    - path: "test/adapters/NextJsAdapter.test.ts"
      provides: "Unit tests for classifyEntry, enumerateRoutes, slotMarker"
      contains: "NextJsAdapter"
  key_links:
    - from: "test/adapters/FrameworkAdapter.test.ts"
      to: "src/adapters/FrameworkAdapter.ts"
      via: "Record<keyof FrameworkAdapter, true> stub — compile-time exhaustive check"
      pattern: "Record<keyof FrameworkAdapter, true>"
---

<objective>
Widen the `FrameworkAdapter` interface from 5 to 8 methods, update the structural locking test to
assert the new method set, and create unit tests for the three new methods on `NextJsAdapter`
(in RED/failing state — implementations land in Plan 02).

Purpose: Establish the contract before implementation so that TypeScript compiler enforces
completeness throughout Plan 02. The locking test must be updated atomically with the interface
to prevent TS compile errors in subsequent plans.
Output: Updated `FrameworkAdapter.ts`, updated `FrameworkAdapter.test.ts`, new `NextJsAdapter.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:\ui-to-hierarch\.planning\PROJECT.md
@E:\ui-to-hierarch\.planning\ROADMAP.md
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-SPEC.md
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-CONTEXT.md
@E:\ui-to-hierarch\.planning\phases\10-interface-widening-analyzer-de-next-ification\10-PATTERNS.md

<interfaces>
<!-- Current FrameworkAdapter interface (src/adapters/FrameworkAdapter.ts, lines 1–47) -->
<!-- 5-method set: detect, discoverEntries, resolveModule, extractComponents, mapRouteToEntry -->
export interface FrameworkAdapter {
  detect(absRoot: string): Promise<boolean> | boolean;
  discoverEntries(absRoot: string): Promise<string[]> | string[];
  resolveModule(ctx: ParseContext, fromFile: string, specifier: string, importedName: string): ResolveResult;
  extractComponents(ctx: ParseContext, entryFiles: string[], opts?: { fullClasses?: boolean }): ComponentDefinition[];
  mapRouteToEntry(absRoot: string, route: string): Promise<RouteMatch> | RouteMatch;
}

<!-- Current locking test (test/adapters/FrameworkAdapter.test.ts) asserts exactly 5 methods -->
<!-- Record<keyof FrameworkAdapter, true> stub with 5 keys; toHaveLength(5) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen FrameworkAdapter interface to 8 methods and update structural locking test</name>
  <files>src/adapters/FrameworkAdapter.ts, test/adapters/FrameworkAdapter.test.ts</files>
  <read_first>
    - src/adapters/FrameworkAdapter.ts — full file (47 lines); read before editing to capture exact comment text and import list
    - test/adapters/FrameworkAdapter.test.ts — full file (31 lines); read before editing to capture current stub structure
    - .planning/phases/10-interface-widening-analyzer-de-next-ification/10-PATTERNS.md — sections "src/adapters/FrameworkAdapter.ts" and "test/adapters/FrameworkAdapter.test.ts" for exact target content
  </read_first>
  <behavior>
    - After edit: `vitest run test/adapters/FrameworkAdapter.test.ts` passes (not a RED test — this is a structural contract test)
    - After edit: `npx tsc --noEmit` has zero errors related to FrameworkAdapter
    - The `Record<keyof FrameworkAdapter, true>` stub in the test now has 8 keys; if any key is removed from the interface, the stub fails to type-check
  </behavior>
  <action>
    Edit src/adapters/FrameworkAdapter.ts: replace the doc comment block (lines 3–19) — change "Adding a 6th method to this interface requires a milestone amendment. The 5-key set is asserted at runtime by test/adapters/FrameworkAdapter.test.ts to catch accidental additions." to "8-method set locked by Phase 10 SPEC (10-SPEC.md). Asserted at runtime by test/adapters/FrameworkAdapter.test.ts."

    Append three method signatures to the FrameworkAdapter interface body after `mapRouteToEntry`:

    Method 1 — classifyEntry:
    doc: "Classify an entry file by its role in the framework's routing model."
    signature: classifyEntry(absPath: string): "page" | "layout" | "special" | "other"

    Method 2 — enumerateRoutes:
    doc: "Enumerate all route strings for the project root."
    signature: enumerateRoutes(absRoot: string): string[] | Promise<string[]>

    Method 3 — slotMarker:
    doc: "Return true if the identifier name (from source importSource) is a slot injection point for this framework. Next.js: name === 'children' (importSource ignored). Expo Router: name === 'Slot' && importSource === 'expo-router'."
    signature: slotMarker(name: string, importSource: string): boolean

    Edit test/adapters/FrameworkAdapter.test.ts: replace the entire file content with the 8-method locking test from 10-PATTERNS.md section "test/adapters/FrameworkAdapter.test.ts". Key changes:
    - it() description: "interface has exactly 8 methods (detect, discoverEntries, resolveModule, extractComponents, mapRouteToEntry, classifyEntry, enumerateRoutes, slotMarker)"
    - stub object: 8 keys (original 5 + classifyEntry, enumerateRoutes, slotMarker all set to true)
    - expect(Object.keys(stub).sort()).toEqual([...8 names alphabetically sorted...])
    - expect(Object.keys(stub)).toHaveLength(8)
  </action>
  <verify>
    <automated>cd E:\ui-to-hierarch && npx vitest run test/adapters/FrameworkAdapter.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/adapters/FrameworkAdapter.ts contains `classifyEntry(absPath: string): "page" | "layout" | "special" | "other"`
    - src/adapters/FrameworkAdapter.ts contains `enumerateRoutes(absRoot: string): string[] | Promise<string[]>`
    - src/adapters/FrameworkAdapter.ts contains `slotMarker(name: string, importSource: string): boolean`
    - src/adapters/FrameworkAdapter.ts does NOT contain "Adding a 6th method requires a milestone amendment"
    - src/adapters/FrameworkAdapter.ts contains "8-method set locked by Phase 10 SPEC"
    - test/adapters/FrameworkAdapter.test.ts contains `toHaveLength(8)`
    - test/adapters/FrameworkAdapter.test.ts does NOT contain `toHaveLength(5)`
    - `vitest run test/adapters/FrameworkAdapter.test.ts` exits 0
  </acceptance_criteria>
  <done>FrameworkAdapter has 8 declared methods; locking test asserts exactly 8 and passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NextJsAdapter unit tests for the 3 new methods (RED state)</name>
  <files>test/adapters/NextJsAdapter.test.ts</files>
  <read_first>
    - test/adapters/FrameworkAdapter.test.ts — import pattern and describe/it structure to replicate
    - src/adapters/next/NextJsAdapter.ts — to understand the import path and confirm NextJsAdapter is exported as a named const
    - .planning/phases/10-interface-widening-analyzer-de-next-ification/10-PATTERNS.md — "Wave 0 Gap" section listing all required test cases
    - .planning/phases/10-interface-widening-analyzer-de-next-ification/10-RESEARCH.md — "Validation Architecture / Wave 0 Gaps" section for the full list of 7 required test cases
  </read_first>
  <behavior>
    - classifyEntry("app/page.tsx") returns "page"
    - classifyEntry("app/layout.tsx") returns "layout"
    - classifyEntry("app/loading.tsx") returns "special"
    - classifyEntry("app/layout.tsx") does NOT return "special" (Pitfall 1 regression guard — layout must be classified "layout", not "special", even though isSpecialFile regex matches it)
    - slotMarker("children", "react") returns true
    - slotMarker("Slot", "expo-router") returns false
    - enumerateRoutes smoke test: given expo-basic fixture root, returns sorted string array with no parallel-route (@...) or private-folder (_...) entries
  </behavior>
  <action>
    Create test/adapters/NextJsAdapter.test.ts. This file does NOT exist — create it fresh.

    Import: `import { describe, expect, it } from "vitest"` and `import { NextJsAdapter } from "../../src/adapters/next/NextJsAdapter.js"`.

    Structure three describe blocks:

    describe("NextJsAdapter.classifyEntry"):
    - it("returns 'page' for page.tsx") — NextJsAdapter.classifyEntry("app/page.tsx") === "page"
    - it("returns 'layout' for layout.tsx") — NextJsAdapter.classifyEntry("app/layout.tsx") === "layout"
    - it("returns 'special' for loading.tsx") — NextJsAdapter.classifyEntry("app/loading.tsx") === "special"
    - it("returns 'special' for not-found.tsx") — NextJsAdapter.classifyEntry("app/not-found.tsx") === "special"
    - it("does NOT return 'special' for layout.tsx — Pitfall 1 regression guard") — expect(NextJsAdapter.classifyEntry("app/layout.tsx")).not.toBe("special")
    - it("returns 'other' for component.tsx") — NextJsAdapter.classifyEntry("src/components/Card.tsx") === "other"

    describe("NextJsAdapter.slotMarker"):
    - it("returns true for name 'children' (Next.js slot pattern)") — NextJsAdapter.slotMarker("children", "react") === true
    - it("returns false for name 'Slot' from expo-router") — NextJsAdapter.slotMarker("Slot", "expo-router") === false
    - it("returns false for name 'children' interpretation: importSource irrelevant — true regardless") — actually verify slotMarker("children", "") === true and slotMarker("children", "anything") === true (importSource is ignored per D-05)
    - it("returns false for any name that is not 'children'") — NextJsAdapter.slotMarker("Header", "") === false

    describe("NextJsAdapter.enumerateRoutes"):
    - it("returns sorted routes from expo-basic fixture") — use path.join(process.cwd(), "test/fixtures/expo-basic") as absRoot; await NextJsAdapter.enumerateRoutes(root); assert result is an array, sorted, and contains "/" (the index route); assert no entry starts with "@" or "_".

    All tests in this file will FAIL (RED) because NextJsAdapter does not yet implement classifyEntry, enumerateRoutes, or slotMarker. This is intentional — implementations land in Plan 02.
  </action>
  <verify>
    <automated>cd E:\ui-to-hierarch && npx vitest run test/adapters/NextJsAdapter.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - test/adapters/NextJsAdapter.test.ts file exists
    - File imports NextJsAdapter from "../../src/adapters/next/NextJsAdapter.js"
    - File contains test cases for classifyEntry with at least: "page", "layout", "special", "other" return values
    - File contains `not.toBe("special")` guard for layout.tsx (Pitfall 1 regression)
    - File contains slotMarker("children", "react") returning true
    - File contains slotMarker("Slot", "expo-router") returning false
    - File contains an enumerateRoutes test using the expo-basic fixture path
    - `vitest run test/adapters/NextJsAdapter.test.ts` exits non-zero (tests FAIL in RED state — methods not yet implemented)
  </acceptance_criteria>
  <done>Unit tests exist for all 3 new methods; tests fail in RED state confirming missing implementations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript compiler | Interface widening must not introduce compile errors in any file that imports FrameworkAdapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | FrameworkAdapter.ts — interface widening | accept | Pure type-level change; no runtime I/O; island rule already enforced by Biome + architecture test |
</threat_model>

<verification>
After both tasks complete:

1. `vitest run test/adapters/FrameworkAdapter.test.ts` exits 0 — locking test passes
2. `vitest run test/adapters/NextJsAdapter.test.ts` exits non-zero — unit tests fail in RED state (expected)
3. `grep "classifyEntry\|enumerateRoutes\|slotMarker" src/adapters/FrameworkAdapter.ts` returns 3 lines
4. `grep "toHaveLength(8)" test/adapters/FrameworkAdapter.test.ts` returns 1 line
5. `grep "toHaveLength(5)" test/adapters/FrameworkAdapter.test.ts` returns 0 lines
</verification>

<success_criteria>
- FrameworkAdapter interface has exactly 8 declared methods
- The structural locking test compiles and passes with the 8-key stub
- Unit test file for NextJsAdapter exists with 12+ test cases covering classifyEntry, slotMarker, enumerateRoutes
- Tests in NextJsAdapter.test.ts fail (RED) — no implementation yet
</success_criteria>

<output>
After completion, create `.planning/phases/10-interface-widening-analyzer-de-next-ification/10-01-SUMMARY.md`
</output>
