---
phase: 09-fixture-design-stub-packages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/expo-tabs-and-dynamic/tsconfig.json
  - test/fixtures/expo-tabs-and-dynamic/node_modules/react-native/package.json
  - test/fixtures/expo-tabs-and-dynamic/node_modules/react-native/index.d.ts
  - test/fixtures/expo-tabs-and-dynamic/node_modules/expo-router/package.json
  - test/fixtures/expo-tabs-and-dynamic/node_modules/expo-router/index.d.ts
  - test/fixtures/expo-tabs-and-dynamic/app/_layout.tsx
  - test/fixtures/expo-tabs-and-dynamic/app/+not-found.tsx
  - test/fixtures/expo-tabs-and-dynamic/app/(tabs)/_layout.tsx
  - test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx
  - test/fixtures/expo-tabs-and-dynamic/app/(tabs)/[id].tsx
  - test/fixtures/expo-tabs-and-dynamic/app/components/Button.ios.tsx
  - test/fixtures/expo-tabs-and-dynamic/app/components/Button.android.tsx
autonomous: true
requirements: [INTEG-01, INTEG-02]
tags: [fixtures, expo, react-native, nativewind, test-infrastructure]

must_haves:
  truths:
    - "test/fixtures/expo-tabs-and-dynamic/ exists as a complex Expo Router fixture with tab group + dynamic segment + not-found"
    - "Fixture exercises <Tabs>, <Tabs.Screen>, [id] dynamic segment, +not-found special, NativeWind className, and style array syntax"
    - "Local node_modules/ stubs match expo-basic stubs byte-for-byte (same minimal export surface)"
    - "Platform-suffix pair Button.ios.tsx + Button.android.tsx exist for Phase 14 probe target"
  artifacts:
    - path: "test/fixtures/expo-tabs-and-dynamic/tsconfig.json"
      provides: "Minimal tsconfig with @/* -> app/* alias"
      contains: '"@/*"'
    - path: "test/fixtures/expo-tabs-and-dynamic/node_modules/react-native/index.d.ts"
      provides: "Same minimal react-native stub as expo-basic"
      contains: "export declare const View"
    - path: "test/fixtures/expo-tabs-and-dynamic/node_modules/expo-router/index.d.ts"
      provides: "Same minimal expo-router stub as expo-basic"
      contains: "export declare const Tabs"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/_layout.tsx"
      provides: "Root layout with Slot"
      contains: "Slot"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/_layout.tsx"
      provides: "Tab group layout using <Tabs> and <Tabs.Screen>"
      contains: "Tabs.Screen"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx"
      provides: "Tab screen carrying NativeWind className AND style array syntax"
      contains: "className"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/[id].tsx"
      provides: "Dynamic segment screen"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/+not-found.tsx"
      provides: "Expo-prefixed special file"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/components/Button.ios.tsx"
      provides: "iOS platform-suffix probe target"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/components/Button.android.tsx"
      provides: "Android platform-suffix probe target"
  key_links:
    - from: "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/_layout.tsx"
      to: "expo-router"
      via: "import { Tabs } and use of <Tabs.Screen>"
      pattern: "Tabs.Screen"
    - from: "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx"
      to: "react-native"
      via: "import { View, Text, StyleSheet } + style={[styles.card, active && styles.bold]}"
      pattern: "style=\\{\\["
---

<objective>
Author the `expo-tabs-and-dynamic` fixture: complex Expo Router shape exercising tab groups, dynamic segments, `+not-found`, NativeWind `className`, and style array syntax. Includes the same stub node_modules surface as `expo-basic`.

Purpose: Second of two Expo fixtures required by INTEG-01/INTEG-02. Carries the routing/styling syntax variety Phases 12-13 will need to parse.
Output: 12 files under `test/fixtures/expo-tabs-and-dynamic/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fixture-design-stub-packages/09-SPEC.md
@.planning/phases/09-fixture-design-stub-packages/09-CONTEXT.md
@.planning/phases/09-fixture-design-stub-packages/09-RESEARCH.md
@.planning/phases/09-fixture-design-stub-packages/09-PATTERNS.md

<interfaces>
<!-- Same canonical stub shapes as Plan 01. Both fixtures use byte-identical stubs (same import surface). -->

Stub package.json: `{ "name": "<name>", "version": "0.0.0", "main": "index.js" }`

tsconfig: `{ "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["app/*"] } } }`

react-native/index.d.ts exports: `StyleProps` (className+style), `View`, `Text`, `ScrollView`, `TouchableOpacity`, `Pressable`, `StyleSheet`, `StyleProp<T>`, `ViewStyle`, `TextStyle`.

expo-router/index.d.ts exports: `Slot`, `Link`, `Tabs` (interface extends with `Screen` sub-component), `Stack` (same shape).

Full literal shapes are in `.planning/phases/09-fixture-design-stub-packages/09-PATTERNS.md` "Pattern Assignments" section — copy verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expo-tabs-and-dynamic stub node_modules + tsconfig</name>
  <files>
    test/fixtures/expo-tabs-and-dynamic/tsconfig.json,
    test/fixtures/expo-tabs-and-dynamic/node_modules/react-native/package.json,
    test/fixtures/expo-tabs-and-dynamic/node_modules/react-native/index.d.ts,
    test/fixtures/expo-tabs-and-dynamic/node_modules/expo-router/package.json,
    test/fixtures/expo-tabs-and-dynamic/node_modules/expo-router/index.d.ts
  </files>
  <read_first>
    test/fixtures/parser/resolver/shadcn-barrel/tsconfig.json,
    .planning/phases/09-fixture-design-stub-packages/09-PATTERNS.md,
    .planning/phases/09-fixture-design-stub-packages/09-CONTEXT.md
  </read_first>
  <action>
    Create the same five stub/config files as Plan 01 Task 1, but rooted at `test/fixtures/expo-tabs-and-dynamic/`. Files are byte-identical to `expo-basic` counterparts per SPEC REQ-3/REQ-4 (both fixtures import the same identifier set).

    Per D-03: `tsconfig.json` only `baseUrl: "."` + `paths: { "@/*": ["app/*"] }`. No other compiler options.

    Per D-02: `react-native/index.d.ts` uses shared `interface StyleProps { className?: string; style?: any }` and declares: `View`, `Text`, `ScrollView`, `TouchableOpacity`, `Pressable`, `StyleSheet` (with `create<T>(styles: T): T`), `StyleProp<T>`, `ViewStyle`, `TextStyle`. No `@ts-expect-error`. No separate `nativewind` stub.

    `expo-router/index.d.ts` exports `Slot`, `Link`, `Tabs` (with `Screen` via `TabsComponent` interface extends pattern), `Stack` (with `Screen` via `StackComponent`). Use interface-extends shape, NOT `declare namespace` (RESEARCH Pitfall 3).

    Both `package.json` files use `{ name, version: "0.0.0", main: "index.js" }`.
  </action>
  <verify>
    <automated>node -e "for (const p of ['test/fixtures/expo-tabs-and-dynamic/tsconfig.json','test/fixtures/expo-tabs-and-dynamic/node_modules/react-native/package.json','test/fixtures/expo-tabs-and-dynamic/node_modules/react-native/index.d.ts','test/fixtures/expo-tabs-and-dynamic/node_modules/expo-router/package.json','test/fixtures/expo-tabs-and-dynamic/node_modules/expo-router/index.d.ts']) { require('fs').accessSync(p); } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist and parse (JSON files JSON-parse; .d.ts files non-empty).
    - `tsconfig.json` has `compilerOptions.paths["@/*"]` deep-equal to `["app/*"]`.
    - `react-native/package.json` has `name === "react-native"`, `version === "0.0.0"`, `main === "index.js"`.
    - `expo-router/package.json` has `name === "expo-router"`, `version === "0.0.0"`, `main === "index.js"`.
    - `react-native/index.d.ts` contains `interface StyleProps`, `export declare const View`, `export declare const Text`, `export declare const StyleSheet`, and does NOT contain `@ts-expect-error`.
    - `expo-router/index.d.ts` contains `export declare const Slot`, `export declare const Tabs`, `export interface TabsComponent`, `export declare const Stack`, and does NOT contain `declare namespace`.
  </acceptance_criteria>
  <done>Stub node_modules + tsconfig committed; downstream fixture files in this plan can resolve `react-native` and `expo-router` imports.</done>
</task>

<task type="auto">
  <name>Task 2: Author expo-tabs-and-dynamic app/ fixture files (layouts, tab screens, dynamic, not-found, Button pair)</name>
  <files>
    test/fixtures/expo-tabs-and-dynamic/app/_layout.tsx,
    test/fixtures/expo-tabs-and-dynamic/app/+not-found.tsx,
    test/fixtures/expo-tabs-and-dynamic/app/(tabs)/_layout.tsx,
    test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx,
    test/fixtures/expo-tabs-and-dynamic/app/(tabs)/[id].tsx,
    test/fixtures/expo-tabs-and-dynamic/app/components/Button.ios.tsx,
    test/fixtures/expo-tabs-and-dynamic/app/components/Button.android.tsx
  </files>
  <read_first>
    .planning/phases/09-fixture-design-stub-packages/09-PATTERNS.md,
    .planning/phases/09-fixture-design-stub-packages/09-SPEC.md,
    test/fixtures/next-app-router/app/blog/[slug]/page.tsx
  </read_first>
  <action>
    Author seven fixture source files per D-04 (minimal: one import + one default-export function). Use shapes from PATTERNS.md verbatim. Concentrate the NativeWind `className` usage AND the style array syntax in a single file — `app/(tabs)/index.tsx` — per planner discretion (CONTEXT.md).

    `app/_layout.tsx`: import `Slot` from `expo-router`; `export default function RootLayout()` returns `<Slot />`.

    `app/+not-found.tsx`: import `View, Text` from `react-native`; `export default function NotFound()` returns `<View><Text>Not Found</Text></View>`. The `+` prefix in filename is the Expo special-file convention (no special syntax inside the file).

    `app/(tabs)/_layout.tsx`: import `Tabs` from `expo-router`; `export default function TabsLayout()` returns `<Tabs><Tabs.Screen name="index" options={{ title: "Home" }} /><Tabs.Screen name="[id]" options={{ title: "Detail" }} /></Tabs>`. This satisfies SPEC REQ-2 acceptance criterion "(tabs)/_layout.tsx uses <Tabs> from expo-router".

    `app/(tabs)/index.tsx`: import `View, Text, StyleSheet` from `react-native`; declare `const styles = StyleSheet.create({ card: { padding: 8 }, bold: { fontWeight: "bold" } })`; `export default function HomeTab({ active }: { active?: boolean })` returns `<View style={[styles.card, active && styles.bold]}><Text className="text-lg font-bold">Home</Text></View>`. This file simultaneously satisfies SPEC acceptance criteria "uses NativeWind className prop on an RN primitive" AND "uses style={[a, b, cond && c]} array syntax".

    `app/(tabs)/[id].tsx`: import `View, Text` from `react-native`; `export default function DetailScreen()` returns `<View><Text>Detail</Text></View>`. Filename carries the dynamic segment syntax — no `useLocalSearchParams` inside (out of v1.2 scope).

    `app/components/Button.ios.tsx` and `app/components/Button.android.tsx`: import `View` from `react-native`; `export default function Button()` returns `<View />`. Identical bodies; only filename suffix differs (Phase 14 probe target).

    Do NOT add: `useLocalSearchParams`, `useRouter`, `Link` href harvesting, `<Drawer>`, `tw\`...\`` tagged templates (those would emit a warning per Phase 13 design — out of scope here). Do NOT add a separate file for style-array syntax — keep it inside `(tabs)/index.tsx` so both signals concentrate in one fixture file (REQ-2 says "at least one file" for each).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const root='test/fixtures/expo-tabs-and-dynamic/app/';const layout=fs.readFileSync(root+'_layout.tsx','utf8');const tabsLayout=fs.readFileSync(root+'(tabs)/_layout.tsx','utf8');const tabsIndex=fs.readFileSync(root+'(tabs)/index.tsx','utf8');const dyn=fs.readFileSync(root+'(tabs)/[id].tsx','utf8');const nf=fs.readFileSync(root+'+not-found.tsx','utf8');const btnIos=fs.readFileSync(root+'components/Button.ios.tsx','utf8');const btnAnd=fs.readFileSync(root+'components/Button.android.tsx','utf8');if(!layout.includes('Slot'))throw 1;if(!tabsLayout.includes('Tabs.Screen'))throw 2;if(!tabsLayout.includes('from \"expo-router\"'))throw 3;if(!tabsIndex.includes('className='))throw 4;if(!tabsIndex.includes('style={['))throw 5;if(!tabsIndex.includes('StyleSheet.create'))throw 6;if(!dyn.includes('from \"react-native\"'))throw 7;if(!nf.includes('from \"react-native\"'))throw 8;if(!btnIos.includes('<View'))throw 9;if(!btnAnd.includes('<View'))throw 10;console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/_layout.tsx` contains `from "expo-router"` AND `Slot`.
    - `app/(tabs)/_layout.tsx` contains `from "expo-router"` AND `Tabs.Screen` AND at least two `name=` attributes on Tabs.Screen.
    - `app/(tabs)/index.tsx` contains all of: `StyleSheet.create`, `className=`, `style={[`, `from "react-native"`.
    - `app/(tabs)/[id].tsx` exists at exactly that path (square brackets in filename) and contains `from "react-native"`.
    - `app/+not-found.tsx` exists at exactly that path (plus prefix in filename) and contains `from "react-native"`.
    - `app/components/Button.ios.tsx` and `app/components/Button.android.tsx` both contain `from "react-native"` AND `<View` AND `export default function Button`.
    - None of the seven files contain `useLocalSearchParams`, `useRouter`, `tw\``, `"use client"`, or `@ts-expect-error`.
    - `vitest run` from project root exits 0 with no regressions (existing ≥356 test count preserved).
  </acceptance_criteria>
  <done>All seven fixture source files committed; `expo-tabs-and-dynamic` fixture tree complete and inert; exercises tab group, dynamic segment, special file, NativeWind className, and style array in a single fixture.</done>
</task>

</tasks>

<verification>
- All 12 files at the paths listed in `files_modified` exist and are committed.
- `vitest run` exits 0 with no regressions.
- Stub `index.d.ts` exports match the byte-identical surface declared in Plan 01.
- Filenames preserve special syntax: `(tabs)/` (parens), `[id].tsx` (square brackets), `+not-found.tsx` (plus prefix).
</verification>

<success_criteria>
- 12 files created exactly as listed in `files_modified`.
- The fixture exercises every routing/styling syntax shape v1.2 will need (tab group + dynamic + not-found + NativeWind + style array).
- Both NativeWind `className` AND style array `style={[...]}` are present in at least one fixture file each (REQ-2).
- Fixture is inert: running the existing analyzer against it does not throw.
- Plan 03's smoke test can `path.resolve("test/fixtures/expo-tabs-and-dynamic")` and `path.resolve("test/fixtures/expo-tabs-and-dynamic/app/_layout.tsx")` against real files.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fixture-design-stub-packages/09-02-SUMMARY.md`.
</output>
