---
phase: 09-fixture-design-stub-packages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/expo-basic/tsconfig.json
  - test/fixtures/expo-basic/node_modules/react-native/package.json
  - test/fixtures/expo-basic/node_modules/react-native/index.d.ts
  - test/fixtures/expo-basic/node_modules/expo-router/package.json
  - test/fixtures/expo-basic/node_modules/expo-router/index.d.ts
  - test/fixtures/expo-basic/app/_layout.tsx
  - test/fixtures/expo-basic/app/index.tsx
  - test/fixtures/expo-basic/app/components/HomeScreen.tsx
  - test/fixtures/expo-basic/app/components/Button.ios.tsx
  - test/fixtures/expo-basic/app/components/Button.android.tsx
autonomous: true
requirements: [INTEG-01, INTEG-02]
tags: [fixtures, expo, react-native, test-infrastructure]

must_haves:
  truths:
    - "test/fixtures/expo-basic/ exists as a self-contained Expo Router fixture tree"
    - "Fixture imports Slot from expo-router and View/Text/StyleSheet from react-native without TS errors"
    - "Local node_modules/ stub for react-native and expo-router resolves package types from inside the fixture"
    - "Platform-suffix pair Button.ios.tsx + Button.android.tsx exist as Phase 14 probe target"
  artifacts:
    - path: "test/fixtures/expo-basic/tsconfig.json"
      provides: "Minimal tsconfig with @/* -> app/* alias"
      contains: '"@/*"'
    - path: "test/fixtures/expo-basic/node_modules/react-native/package.json"
      provides: "Stub package manifest"
      contains: '"name": "react-native"'
    - path: "test/fixtures/expo-basic/node_modules/react-native/index.d.ts"
      provides: "Minimal type stubs for View/Text/ScrollView/TouchableOpacity/Pressable/StyleSheet/StyleProp/ViewStyle/TextStyle with shared StyleProps (className + style)"
      contains: "export declare const View"
    - path: "test/fixtures/expo-basic/node_modules/expo-router/package.json"
      provides: "Stub package manifest"
      contains: '"name": "expo-router"'
    - path: "test/fixtures/expo-basic/node_modules/expo-router/index.d.ts"
      provides: "Minimal type stubs for Slot/Link/Tabs/Stack"
      contains: "export declare const Slot"
    - path: "test/fixtures/expo-basic/app/_layout.tsx"
      provides: "Root layout importing Slot from expo-router"
      contains: 'from "expo-router"'
    - path: "test/fixtures/expo-basic/app/index.tsx"
      provides: "Home screen using View/Text"
    - path: "test/fixtures/expo-basic/app/components/HomeScreen.tsx"
      provides: "Screen using StyleSheet.create from react-native"
      contains: "StyleSheet.create"
    - path: "test/fixtures/expo-basic/app/components/Button.ios.tsx"
      provides: "iOS platform-suffix probe target"
    - path: "test/fixtures/expo-basic/app/components/Button.android.tsx"
      provides: "Android platform-suffix probe target"
  key_links:
    - from: "test/fixtures/expo-basic/app/_layout.tsx"
      to: "expo-router"
      via: "bare specifier import { Slot }"
      pattern: 'from "expo-router"'
    - from: "test/fixtures/expo-basic/app/components/HomeScreen.tsx"
      to: "react-native"
      via: "bare specifier import { View, Text, StyleSheet }"
      pattern: 'from "react-native"'
---

<objective>
Author the `expo-basic` fixture: a minimal Expo Router project tree with stub `react-native` + `expo-router` node_modules, a tsconfig with `@/*` alias, and a Button platform-suffix pair.

Purpose: First of two Expo fixtures required by INTEG-01/INTEG-02. Foundation for Phases 10-15 (no adapter logic exists here; fixture is inert from the analyzer's perspective).
Output: 10 files under `test/fixtures/expo-basic/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fixture-design-stub-packages/09-SPEC.md
@.planning/phases/09-fixture-design-stub-packages/09-CONTEXT.md
@.planning/phases/09-fixture-design-stub-packages/09-RESEARCH.md
@.planning/phases/09-fixture-design-stub-packages/09-PATTERNS.md

<interfaces>
<!-- Canonical patterns for stubs and fixtures. Use these verbatim. -->

Stub package.json shape (D-03):
```json
{ "name": "<package-name>", "version": "0.0.0", "main": "index.js" }
```

tsconfig shape (D-03, no src/ subdir → alias target is app/*):
```json
{ "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["app/*"] } } }
```

react-native stub exports (D-02 — shared StyleProps for NativeWind className):
```typescript
import * as React from "react";
interface StyleProps { className?: string; style?: any; }
export declare const View: React.ComponentType<StyleProps & { children?: React.ReactNode }>;
export declare const Text: React.ComponentType<StyleProps & { children?: React.ReactNode }>;
export declare const ScrollView: React.ComponentType<StyleProps & { children?: React.ReactNode }>;
export declare const TouchableOpacity: React.ComponentType<StyleProps & { children?: React.ReactNode; onPress?: () => void }>;
export declare const Pressable: React.ComponentType<StyleProps & { children?: React.ReactNode; onPress?: () => void }>;
export declare const StyleSheet: { create<T extends Record<string, any>>(styles: T): T };
export declare type StyleProp<T> = T | T[] | null | undefined;
export declare type ViewStyle = { [key: string]: any };
export declare type TextStyle = { [key: string]: any };
```

expo-router stub exports (PATTERNS.md — interface approach, not namespace, per RESEARCH Pitfall 3):
```typescript
import * as React from "react";
export declare const Slot: React.ComponentType<{ children?: React.ReactNode }>;
export declare const Link: React.ComponentType<{ href: string; children?: React.ReactNode }>;
export interface TabsComponent extends React.ComponentType<{ children?: React.ReactNode }> {
  Screen: React.ComponentType<{ name: string; options?: Record<string, any> }>;
}
export declare const Tabs: TabsComponent;
export interface StackComponent extends React.ComponentType<{ children?: React.ReactNode }> {
  Screen: React.ComponentType<{ name: string; options?: Record<string, any> }>;
}
export declare const Stack: StackComponent;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create expo-basic stub node_modules + tsconfig</name>
  <files>
    test/fixtures/expo-basic/tsconfig.json,
    test/fixtures/expo-basic/node_modules/react-native/package.json,
    test/fixtures/expo-basic/node_modules/react-native/index.d.ts,
    test/fixtures/expo-basic/node_modules/expo-router/package.json,
    test/fixtures/expo-basic/node_modules/expo-router/index.d.ts
  </files>
  <read_first>
    test/fixtures/parser/resolver/shadcn-barrel/tsconfig.json,
    .planning/phases/09-fixture-design-stub-packages/09-PATTERNS.md,
    .planning/phases/09-fixture-design-stub-packages/09-CONTEXT.md
  </read_first>
  <action>
    Create the fixture's configuration scaffolding per D-02 and D-03.

    Write `tsconfig.json` containing only `compilerOptions.baseUrl = "."` and `compilerOptions.paths = { "@/*": ["app/*"] }`. Do NOT add `jsx`, `module`, `strict`, or `moduleResolution` keys.

    Write `node_modules/react-native/package.json` with name=`react-native`, version=`0.0.0`, main=`index.js`.

    Write `node_modules/react-native/index.d.ts` with the shared `StyleProps` interface (`className?: string; style?: any`) and these declared exports: `View`, `Text`, `ScrollView`, `TouchableOpacity`, `Pressable`, `StyleSheet` (with `create<T>(styles: T): T`), `StyleProp<T>`, `ViewStyle`, `TextStyle`. Use the canonical shape from `<interfaces>` in `<context>`. Do NOT use `@ts-expect-error` (D-02). Do NOT add a separate `nativewind` stub — className lives on `StyleProps`.

    Write `node_modules/expo-router/package.json` with name=`expo-router`, version=`0.0.0`, main=`index.js`.

    Write `node_modules/expo-router/index.d.ts` with: `Slot`, `Link`, `Tabs` (with `Screen` sub-component via `TabsComponent` interface extends pattern), `Stack` (same shape via `StackComponent`). Use the `interface ... extends React.ComponentType<...> { Screen: ... }` pattern from `<interfaces>` — NOT `declare namespace` (RESEARCH Pitfall 3).

    Stub `index.d.ts` exports MUST be minimal — only the identifiers listed above (per SPEC REQ-3 / REQ-4). Do not add Animated, View.Modal, Image, FlatList, etc. unless this fixture's Task 2 imports them.
  </action>
  <verify>
    <automated>node -e "for (const p of ['test/fixtures/expo-basic/tsconfig.json','test/fixtures/expo-basic/node_modules/react-native/package.json','test/fixtures/expo-basic/node_modules/react-native/index.d.ts','test/fixtures/expo-basic/node_modules/expo-router/package.json','test/fixtures/expo-basic/node_modules/expo-router/index.d.ts']) { require('fs').accessSync(p); } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test/fixtures/expo-basic/tsconfig.json` exists and JSON-parses; `compilerOptions.baseUrl === "."`; `compilerOptions.paths["@/*"]` deep-equals `["app/*"]`.
    - `test/fixtures/expo-basic/node_modules/react-native/package.json` JSON-parses with `name === "react-native"`, `version === "0.0.0"`, `main === "index.js"`.
    - `test/fixtures/expo-basic/node_modules/react-native/index.d.ts` contains: `interface StyleProps`, `export declare const View`, `export declare const Text`, `export declare const ScrollView`, `export declare const TouchableOpacity`, `export declare const Pressable`, `export declare const StyleSheet`, `export declare type StyleProp`, `export declare type ViewStyle`, `export declare type TextStyle`.
    - `test/fixtures/expo-basic/node_modules/react-native/index.d.ts` does NOT contain `@ts-expect-error`.
    - `test/fixtures/expo-basic/node_modules/expo-router/package.json` JSON-parses with `name === "expo-router"`, `version === "0.0.0"`, `main === "index.js"`.
    - `test/fixtures/expo-basic/node_modules/expo-router/index.d.ts` contains: `export declare const Slot`, `export declare const Link`, `export interface TabsComponent`, `export declare const Tabs`, `export interface StackComponent`, `export declare const Stack`.
    - `test/fixtures/expo-basic/node_modules/expo-router/index.d.ts` does NOT contain `declare namespace`.
  </acceptance_criteria>
  <done>Stub node_modules + tsconfig committed; downstream fixture files can `import { ... } from "react-native"` / `"expo-router"` without TypeScript module-not-found errors.</done>
</task>

<task type="auto">
  <name>Task 2: Author expo-basic app/ fixture files (layout, index, HomeScreen, Button pair)</name>
  <files>
    test/fixtures/expo-basic/app/_layout.tsx,
    test/fixtures/expo-basic/app/index.tsx,
    test/fixtures/expo-basic/app/components/HomeScreen.tsx,
    test/fixtures/expo-basic/app/components/Button.ios.tsx,
    test/fixtures/expo-basic/app/components/Button.android.tsx
  </files>
  <read_first>
    test/fixtures/next-app-router/app/layout.tsx,
    test/fixtures/next-app-router/app/page.tsx,
    test/fixtures/parser/resolver/shadcn-barrel/src/components/ui/button.tsx,
    .planning/phases/09-fixture-design-stub-packages/09-PATTERNS.md
  </read_first>
  <action>
    Author the five fixture files for `expo-basic` using D-04 (minimal content) and PATTERNS.md verbatim shapes. Each file is one import line plus one default-export function with one return statement — no comments, no React import (JSX transform assumed), no `"use client"`.

    `app/_layout.tsx`: import `Slot` from `expo-router`; `export default function RootLayout()` returning `<Slot />`. This satisfies SPEC REQ-1 acceptance criterion "_layout.tsx exists and contains <Slot/> imported from expo-router".

    `app/index.tsx`: import `View, Text` from `react-native`; `export default function HomeIndex()` returning `<View><Text>Home</Text></View>`.

    `app/components/HomeScreen.tsx`: import `View, Text, StyleSheet` from `react-native`; declare `const styles = StyleSheet.create({ container: { padding: 16 } })`; `export default function HomeScreen()` returning `<View style={styles.container}><Text>HomeScreen</Text></View>`. This satisfies SPEC REQ-1 acceptance criterion "HomeScreen.tsx uses StyleSheet.create from react-native".

    `app/components/Button.ios.tsx`: import `View` from `react-native`; `export default function Button()` returning `<View />`. Platform suffix lives in the filename only — no runtime conditional inside.

    `app/components/Button.android.tsx`: identical body to `Button.ios.tsx` (only filename differs). Per SPEC REQ-6 these are deliberately identical minimal components — Phase 14 will use the filename suffix to test resolver preference order.

    Component names from CONTEXT.md discretion: `RootLayout`, `HomeIndex`, `HomeScreen`, `Button`. Keep names simple.

    Do NOT add: parallel slots, server actions, metadata, fetch, animations, hooks, or anything beyond a single return statement.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const layout=fs.readFileSync('test/fixtures/expo-basic/app/_layout.tsx','utf8');const home=fs.readFileSync('test/fixtures/expo-basic/app/components/HomeScreen.tsx','utf8');const btnIos=fs.readFileSync('test/fixtures/expo-basic/app/components/Button.ios.tsx','utf8');const btnAnd=fs.readFileSync('test/fixtures/expo-basic/app/components/Button.android.tsx','utf8');if(!layout.includes('from \"expo-router\"'))throw 1;if(!layout.includes('<Slot'))throw 2;if(!home.includes('StyleSheet.create'))throw 3;if(!btnIos.includes('from \"react-native\"'))throw 4;if(!btnAnd.includes('from \"react-native\"'))throw 5;console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/_layout.tsx` contains `from "expo-router"` AND `Slot` AND `<Slot` (self-closing or paired tag).
    - `app/index.tsx` contains `from "react-native"` AND a default-exported function.
    - `app/components/HomeScreen.tsx` contains `StyleSheet.create` AND `from "react-native"`.
    - `app/components/Button.ios.tsx` and `app/components/Button.android.tsx` both contain `from "react-native"` AND `<View` AND `export default function Button`.
    - None of the five files contain `"use client"`, `@ts-expect-error`, or `React` as a named import.
    - All five files compile shape: `vitest run` from project root still exits 0 (these fixture files are not executed by tests in this plan, but they must not break the existing test pipeline).
  </acceptance_criteria>
  <done>All five fixture source files committed; `expo-basic` fixture tree complete and inert from analyzer's perspective.</done>
</task>

</tasks>

<verification>
- All 10 files at the paths listed in `files_modified` exist and are committed.
- `vitest run` exits 0 with no regressions in the existing test suite (≥356 tests green).
- No file under `test/fixtures/expo-basic/` is referenced by `src/` production code.
</verification>

<success_criteria>
- 10 files created exactly as listed in `files_modified`.
- Stub `package.json` files are valid JSON and minimal.
- Stub `index.d.ts` files export only identifiers actually imported by the fixture's app/ files.
- The fixture is inert: running the existing analyzer against it does not throw (Phase 9 SPEC constraint).
- Plan 03's smoke test can `path.resolve("test/fixtures/expo-basic")` and `path.resolve("test/fixtures/expo-basic/app/_layout.tsx")` against real files.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fixture-design-stub-packages/09-01-SUMMARY.md`.
</output>
