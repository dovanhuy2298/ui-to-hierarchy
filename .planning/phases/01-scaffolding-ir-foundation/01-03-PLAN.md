---
phase: 01-scaffolding-ir-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/babel-shim.ts
  - test/core/babel-shim.test.ts
autonomous: true
requirements: []
tags: [core, babel, interop]
must_haves:
  truths:
    - "Importing traverse via src/core/babel-shim.js yields a callable function"
    - "Traverse shim works whether @babel/traverse resolves as the function directly or wrapped in { default }"
    - "Unit test fails loudly if a future Babel bump breaks ESM interop"
  artifacts:
    - path: "src/core/babel-shim.ts"
      provides: "Exports traverse = (import as any).default ?? import"
      contains: ".default ?? "
    - path: "test/core/babel-shim.test.ts"
      provides: "Two tests: typeof traverse === 'function'; traverse(minimalAst, {enter(){}}) does not throw"
  key_links:
    - from: "src/core/babel-shim.ts"
      to: "@babel/traverse"
      via: "import traverseImport from '@babel/traverse' + default-or-raw shim"
---

<objective>
Babel traverse ESM/CJS interop shim + loud unit test. Satisfies SC-4 and locks D-20.

Purpose: `@babel/traverse` ships as CJS with a default-export footgun (Babel issues #13855, #15269). A naive `import traverse from "@babel/traverse"` works in some bundlers and breaks in others. Phase 3 cannot begin safely without this shim tested.

Output: One 3-line shim + one 2-case unit test that fails loudly on regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffolding-ir-foundation/01-CONTEXT.md
@.planning/phases/01-scaffolding-ir-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write babel-shim.ts + unit test that fails loudly on interop regression</name>
  <files>src/core/babel-shim.ts, test/core/babel-shim.test.ts</files>
  <read_first>
    - .planning/phases/01-scaffolding-ir-foundation/01-RESEARCH.md §"Pattern 8: Babel traverse ESM interop shim" — exact code + exact test
    - CLAUDE.md §"What NOT to Use" — the "naive import traverse" entry and cited Babel issues
    - CONTEXT.md D-20
  </read_first>
  <behavior>
    - typeof traverse === "function" (first test)
    - traverse(minimalProgramAst, { enter(){} }) does not throw (second test — proves callable and accepts real traversal call shape)
  </behavior>
  <action>
    1. Write `src/core/babel-shim.ts` exactly as RESEARCH.md Pattern 8:
       ```ts
       import traverseImport from "@babel/traverse";
       export const traverse = (traverseImport as any).default ?? traverseImport;
       export default traverse;
       ```
       Add a code comment citing Babel issues #13855 and #15269 plus a note that naive `import traverse from "@babel/traverse"` is forbidden per CLAUDE.md.
    2. Write `test/core/babel-shim.test.ts` with the two test cases from RESEARCH.md Pattern 8. The minimal AST is:
       ```ts
       const programAst = {
         type: "File",
         program: { type: "Program", body: [], directives: [], sourceType: "module" },
       } as any;
       ```
       Assert `typeof traverse === "function"` and `expect(() => traverse(programAst, { enter() {} })).not.toThrow()`.
    3. Do NOT add any other utilities to `babel-shim.ts`. This file exists solely for the interop hop. Keep it minimal so regressions are obvious.
    4. Do NOT parse with `@babel/parser` in this phase — not installed, not needed. The shim is tested in isolation with a hand-constructed AST.
  </action>
  <verify>
    <automated>rtk pnpm vitest run test/core/babel-shim.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/babel-shim.ts` contains `.default ?? ` (grep-verifiable)
    - `src/core/babel-shim.ts` imports from `"@babel/traverse"` (grep-verifiable)
    - `src/core/babel-shim.ts` does NOT use `require(` (must be ESM)
    - `test/core/babel-shim.test.ts` contains `typeof traverse` and `not.toThrow`
    - `pnpm vitest run test/core/babel-shim.test.ts` passes both test cases
    - `pnpm typecheck` passes
    - `pnpm lint` passes (no island violations — core/ is allowed to import npm packages)
  </acceptance_criteria>
  <done>Shim exists, both tests pass, typecheck clean, SC-4 satisfied.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run test/core/babel-shim.test.ts` green (both tests)
- `pnpm typecheck` green
</verification>

<success_criteria>
SC-4 satisfied: Babel ESM/CJS interop shim covered by a unit test that fails loudly if interop regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-ir-foundation/01-03-SUMMARY.md`.
</output>
