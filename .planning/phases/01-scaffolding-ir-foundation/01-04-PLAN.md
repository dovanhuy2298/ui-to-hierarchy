---
phase: 01-scaffolding-ir-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/paths.ts
  - src/core/resolve-root.ts
  - test/core/paths.test.ts
  - test/core/resolve-root.test.ts
autonomous: true
requirements: [ARCH-03]
tags: [core, paths, resolve-root, arch-03]
must_haves:
  truths:
    - "toForwardSlash converts backslashes to forward slashes on any path"
    - "relFromRoot returns forward-slash relative path (e.g. 'app/page.tsx' even on Windows)"
    - "resolveRoot honors arg over UI_TO_HIERARCH_ROOT over process.cwd()"
    - "resolveRoot returns an absolute path with forward slashes (never backslashes)"
    - "resolveRoot normalizes a Windows-shaped absolute input like C:\\\\foo\\\\bar to C:/foo/bar"
  artifacts:
    - path: "src/core/paths.ts"
      provides: "toForwardSlash(p: string): string; relFromRoot(absFile: string, absRoot: string): string"
      contains: "path.sep"
    - path: "src/core/resolve-root.ts"
      provides: "resolveRoot(explicit?: string): string"
      contains: "UI_TO_HIERARCH_ROOT"
    - path: "test/core/paths.test.ts"
      provides: "Tests: toForwardSlash converts `\\\\` to `/`; idempotent on `/`-only input; relFromRoot on Windows path input returns forward-slash relative"
    - path: "test/core/resolve-root.test.ts"
      provides: "Tests: arg precedence, env precedence, cwd fallback, forward-slash normalization of Windows-shaped input"
  key_links:
    - from: "src/core/resolve-root.ts"
      to: "src/core/paths.ts"
      via: "imports toForwardSlash"
    - from: "src/core/resolve-root.ts"
      to: "process.env.UI_TO_HIERARCH_ROOT + process.cwd()"
      via: "?? fallback chain"
---

<objective>
Forward-slash path normalization + project-root resolver. Satisfies SC-5 and ARCH-03.

Purpose: D-07 mandates forward-slash paths in all IR output. D-21 fixes the resolution order arg > env > cwd. The envelope builder (Plan 05) reads `resolvedRoot` from here; Phase 3's parser will use `relFromRoot` for every `file:line`.

Output: Two tiny pure modules (~15 lines each) + exhaustive unit tests covering Windows path shapes without requiring a Windows runner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-scaffolding-ir-foundation/01-CONTEXT.md
@.planning/phases/01-scaffolding-ir-foundation/01-RESEARCH.md
@generate-component-hierarchy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: paths.ts (toForwardSlash + relFromRoot) + tests using path.win32/path.posix</name>
  <files>src/core/paths.ts, test/core/paths.test.ts</files>
  <read_first>
    - .planning/phases/01-scaffolding-ir-foundation/01-RESEARCH.md §"Pattern 6: Forward-slash path normalization" — exact code
    - generate-component-hierarchy.ts lines 117–119 — prototype `rel()` pattern
    - CONTEXT.md D-07 (forward-slash mandate)
    - .planning/phases/01-scaffolding-ir-foundation/01-RESEARCH.md §"Pitfall 4: Windows backslashes leaking into IR"
  </read_first>
  <behavior>
    - toForwardSlash("app\\page.tsx") === "app/page.tsx"
    - toForwardSlash("app/page.tsx") === "app/page.tsx" (idempotent on `/`-only input)
    - toForwardSlash("") === ""
    - relFromRoot computed with a Windows-shaped absRoot + absFile yields forward-slash relative (no backslashes anywhere in output) — test drives this using `path.win32.join` and manually replacing `path.sep` in an isolated code path (or by directly invoking toForwardSlash on a hand-crafted `\\`-separated string since toForwardSlash is the critical unit)
  </behavior>
  <action>
    1. Write `src/core/paths.ts` per RESEARCH.md Pattern 6 verbatim. Export `toForwardSlash` and `relFromRoot`. Use `import path from "node:path"` (OS-sensitive `path.sep` is the point — we split on it).
    2. Write `test/core/paths.test.ts` with these exact cases:
       - `toForwardSlash("app\\page.tsx")` on any OS must return `"app/page.tsx"`. NOTE: on macOS/Linux `path.sep === "/"`, so `"app\\page.tsx".split("/").join("/")` leaves the `\\` intact. To make this test OS-portable, the toForwardSlash implementation must ALSO replace literal backslashes. Update Pattern 6 accordingly: `return p.split(path.sep).join("/").replaceAll("\\", "/")`. Justify the addition in a code comment — "path.sep is `/` on POSIX, so an input containing `\\` must still normalize."
       - `toForwardSlash("app/page.tsx")` returns `"app/page.tsx"`
       - `toForwardSlash("")` returns `""`
       - `relFromRoot` case driven by POSIX: `relFromRoot("/root/app/page.tsx", "/root")` returns `"app/page.tsx"`
  </action>
  <verify>
    <automated>rtk pnpm vitest run test/core/paths.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/paths.ts` contains both `toForwardSlash` and `relFromRoot` as named exports (grep-verifiable)
    - `src/core/paths.ts` uses `path.sep` (honors OS) AND replaces literal `\\` (portable across runners)
    - `src/core/paths.ts` does NOT contain `adapters` or `mcp` imports
    - `pnpm vitest run test/core/paths.test.ts` passes all four test cases
    - `pnpm typecheck` passes
  </acceptance_criteria>
  <done>paths module exists, all tests pass on the runner OS; the double-normalization (path.sep + literal `\\` replace) guarantees correct output whether the runner is Windows or POSIX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolve-root.ts with arg > env > cwd precedence + Windows-shape normalization test</name>
  <files>src/core/resolve-root.ts, test/core/resolve-root.test.ts</files>
  <read_first>
    - .planning/phases/01-scaffolding-ir-foundation/01-RESEARCH.md §"Pattern 7: Project-root resolver (ARCH-03 / D-21)" — exact code
    - CONTEXT.md D-21, REQUIREMENTS.md ARCH-03
  </read_first>
  <behavior>
    - resolveRoot("/explicit") returns forward-slash absolute of "/explicit" when env is set to "/from-env" and cwd is "/cwd-dir" (arg wins)
    - resolveRoot() with UI_TO_HIERARCH_ROOT="/env-val" returns forward-slash absolute of "/env-val"
    - resolveRoot() with no arg and env unset returns toForwardSlash(process.cwd())
    - resolveRoot("C:\\foo\\bar") returns "C:/foo/bar" (Windows-shape normalization, via path.resolve + toForwardSlash)
    - Return value never contains a backslash
  </behavior>
  <action>
    1. Write `src/core/resolve-root.ts` per RESEARCH.md Pattern 7 verbatim:
       ```ts
       import path from "node:path";
       import { toForwardSlash } from "./paths.js";
       export function resolveRoot(explicit?: string): string {
         const candidate = explicit ?? process.env.UI_TO_HIERARCH_ROOT ?? process.cwd();
         return toForwardSlash(path.resolve(candidate));
       }
       ```
    2. Write `test/core/resolve-root.test.ts`. Use vitest `vi.stubEnv("UI_TO_HIERARCH_ROOT", "/env-val")` + `vi.unstubAllEnvs()` in `afterEach` to isolate env manipulation. Four tests matching the four behaviors above.
    3. For the Windows-shape test: call `resolveRoot("C:\\foo\\bar")` and assert the result does NOT contain `"\\"` and DOES contain `"foo/bar"`. Do not require the test to run on Windows — `path.resolve` on POSIX treats `C:\\foo\\bar` as a single relative segment, but once passed through `toForwardSlash` the output will still lack backslashes. Assert the `.includes("\\")` check is false; accept that the exact absolute prefix differs by OS (document this in a test comment).
    4. Do NOT import from `src/ir` or `src/renderers` — resolve-root is part of core and must stay dependency-light (only `node:path` and `./paths.js`).
  </action>
  <verify>
    <automated>rtk pnpm vitest run test/core/resolve-root.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/resolve-root.ts` contains the identifier `UI_TO_HIERARCH_ROOT` (grep-verifiable)
    - `src/core/resolve-root.ts` contains `process.cwd()` (grep-verifiable)
    - `src/core/resolve-root.ts` contains the `?? ` fallback chain (grep-verifiable: `count >= 2` on `"?? "`)
    - `src/core/resolve-root.ts` calls `path.resolve(` and `toForwardSlash(`
    - `pnpm vitest run test/core/resolve-root.test.ts` passes all four test cases
    - Return value of resolveRoot in every test never contains `"\\"` (asserted)
    - `pnpm typecheck` passes
    - `pnpm lint` passes
  </acceptance_criteria>
  <done>resolve-root module exists, all four precedence/normalization tests pass, SC-5 verified in isolation (envelope consumption lands in Plan 05).</done>
</task>

</tasks>

<verification>
- `pnpm vitest run test/core/paths.test.ts test/core/resolve-root.test.ts` green
- `pnpm typecheck` green
- `pnpm lint` green
</verification>

<success_criteria>
SC-5 core logic satisfied: resolveRoot honors arg > env > cwd and returns forward-slash absolute path. ARCH-03 requirement covered. Plan 05 consumes resolveRoot in the envelope builder.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-ir-foundation/01-04-SUMMARY.md`.
</output>
