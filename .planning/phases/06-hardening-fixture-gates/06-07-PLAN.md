---
phase: 06-hardening-fixture-gates
plan: 07
type: execute
wave: 3
depends_on: [04, 05, 06]
files_modified:
  - .planning/phases/06-hardening-fixture-gates/06-UAT.md
  - .planning/phases/06-hardening-fixture-gates/uat-evidence/inspector-transcript.md
  - .planning/phases/06-hardening-fixture-gates/uat-evidence/claude-code-transcript.md
autonomous: false
requirements: [ARCH-04]
covers_spec_ids: [R6]
must_haves:
  truths:
    - "An operator (human) drives MCP Inspector AND Claude Code against the nested-routes fixture, exercising all 4 tools in each client (8 invocations total)"
    - "06-UAT.md PASS Grid is filled with PASS or with a Findings row + block/defer flag for each FAIL"
    - "uat-evidence/ contains at least one transcript per client (Inspector + Claude Code) with USER_HOME redacted"
    - "06-UAT.md Findings table is complete (zero, one, or many rows depending on outcomes)"
    - "06-UAT.md frontmatter status flipped from pending to complete"
    - "Any block-flagged Finding (per D-14) is loudly visible — milestone close cannot proceed without operator awareness"
  artifacts:
    - path: ".planning/phases/06-hardening-fixture-gates/06-UAT.md"
      provides: "Filled UAT with 2×4 PASS grid + Findings table"
      contains: "PASS"
    - path: ".planning/phases/06-hardening-fixture-gates/uat-evidence/inspector-transcript.md"
      provides: "Captured MCP Inspector session transcript (≥4 tool invocations)"
    - path: ".planning/phases/06-hardening-fixture-gates/uat-evidence/claude-code-transcript.md"
      provides: "Captured Claude Code session transcript (≥4 tool invocations)"
  key_links:
    - from: "uat-evidence/*"
      to: "06-UAT.md PASS Grid checkboxes"
      via: "Each transcript demonstrates one row of the 2×4 grid"
      pattern: "transcript"
---

<objective>
Execute the SPEC R6 manual UAT. The operator (human) runs MCP Inspector and Claude Code against the `nested-routes` fixture, exercises all four tools in each client, captures transcripts to `uat-evidence/`, fills the PASS Grid in `06-UAT.md`, and records any unexpected behavior in the Findings table per D-13.

This plan is **autonomous: false** — it requires human-in-the-loop work that cannot be scripted: launching a GUI client (Inspector), pasting an agent transcript out of Claude Code, eyeballing tool output for correctness, exercising visual judgment about screenshots.

Per D-14, the operator categorizes each Finding's Defer/Block flag using the precise rule: `block` ONLY when a finding falsifies a prior phase's verification; everything else is `defer`. Per SPEC out-of-scope rule, the operator does NOT fix bugs in this plan — fixes are a follow-up phase if any block-flagged Finding emerges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-hardening-fixture-gates/06-SPEC.md
@.planning/phases/06-hardening-fixture-gates/06-CONTEXT.md
@.planning/phases/06-hardening-fixture-gates/06-UAT.md
@test/fixtures/phase-06/nested-routes/
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator UAT session — MCP Inspector + Claude Code against nested-routes</name>
  <what-built>
    By Wave 2 the following are in place: dist/cli.js (built), `pnpm test:integration` (Wave 1 gate), `pnpm perf` + 06-PERF.md baseline (Wave 2), and 06-UAT.md template with empty PASS Grid + Findings table (Wave 2). What's left is the human-driven UAT session that fills the grid.
  </what-built>
  <how-to-verify>
    Execute every step in `06-UAT.md`'s `## Runbook` section. Specifically:

    **Step A — Pre-flight:**
    1. `pnpm install` (if cloning fresh).
    2. `pnpm build` (must succeed; produces `dist/cli.js`).
    3. `pnpm test && pnpm test:integration && pnpm test:smoke` — all suites must be green. If `pnpm test:integration` fails, that is itself a candidate `block` Finding (R5 path gate or R4 isError contract failed); record it before proceeding.
    4. `pnpm perf` (commit `06-PERF.md` if hardware unchanged from baseline; otherwise note hardware drift in the runbook).
    5. **Stdout probe** (Pitfall 6): On Windows: `node dist/cli.js < NUL`. On POSIX: `node dist/cli.js < /dev/null`. Confirm zero stdout bytes before EOF. Any stdout bytes = `block` Finding (falsifies MCP-04 from Phase 2).

    **Step B — MCP Inspector session:**
    1. Launch: `npx @modelcontextprotocol/inspector node dist/cli.js`.
    2. In the Inspector UI, invoke each of the 4 tools against `<absolute path to test/fixtures/phase-06/nested-routes>`:
       - `get_full_hierarchy({ route: "/dashboard/123", format: "json", projectRoot: <abs> })` — verify response: `isError === false`; `content[0].text` parses as JSON; tree contains `Sidebar` (or slot kind) and does NOT contain `private-internal-marker`.
       - `focus_on({ component: "DashboardDetail", scope: "full", projectRoot: <abs> })` — verify `isError === false`; tree non-empty.
       - `find_by_text({ query: "Sidebar slot", projectRoot: <abs> })` — verify `isError === false`; at least one match.
       - `find_by_style({ class_or_prop: "grid-cols-3", projectRoot: <abs> })` — verify `isError === false`; at least one match.
    3. Capture session: copy Inspector's transcript / panel output into `.planning/phases/06-hardening-fixture-gates/uat-evidence/inspector-transcript.md`. Optional: 1–2 screenshots into `uat-evidence/inspector-*.png`.
    4. Apply path redaction: `<USER_HOME>` replaces any absolute home path in the transcript before commit.
    5. Tick the PASS Grid `MCP Inspector` column for each tool that produced correct output. For any that didn't: leave the box `[ ]` and add a Findings row.

    **Step C — Claude Code session:**
    1. Configure Claude Code MCP entry (`~/.claude.json` or workspace `.mcp.json`) with:
       ```json
       { "mcpServers": { "ui-to-hierarch": { "command": "node", "args": ["<abs path to dist/cli.js>"] } } }
       ```
    2. Restart Claude Code so it loads the new MCP entry.
    3. Prompt the agent (4 separate prompts, or one structured prompt) to invoke each tool against `<abs path to test/fixtures/phase-06/nested-routes>`. Suggested prompts:
       - "Use the ui-to-hierarch MCP tool `get_full_hierarchy` with route=/dashboard/123 and projectRoot=<abs>. Show the JSON envelope."
       - "Use `focus_on` with component=DashboardDetail, scope=full, projectRoot=<abs>."
       - "Use `find_by_text` with query='Sidebar slot' and projectRoot=<abs>."
       - "Use `find_by_style` with class_or_prop='grid-cols-3' and projectRoot=<abs>."
    4. Capture the agent's tool-use transcript (Claude Code surfaces tool calls + results inline) to `uat-evidence/claude-code-transcript.md`. Apply USER_HOME redaction.
    5. Tick the PASS Grid `Claude Code` column.

    **Step D — Findings categorization (D-14):**
    For every FAIL or unexpected behavior:
    1. Add a Findings row with sequential ID (F-01, F-02, …).
    2. Severity per inline guidance: blocker / major / minor.
    3. Defer/Block flag — **strict D-14 rule:**
       - Use `block` ONLY if the finding falsifies a prior phase's verification:
         - `isError: true` on a tool against `nested-routes` → `block` (falsifies Phase 5 R8 no-throw).
         - Backslash in any `file` field → `block` (falsifies Phase 1 D-07).
         - Stdout corruption (non-JSON-RPC bytes in stdout) → `block` (falsifies Phase 2 MCP-04).
         - Schema parse failure on the envelope → `block` (falsifies Phase 1 envelope contract).
       - Use `defer` for everything else: UX papercuts, slow but correct paths, deeper resolution gaps not covered by prior-phase acceptance, cosmetic markdown rendering quirks, etc.
    4. Repro: ≤1 line. Tool: tool name or `transport` / `stdio`. Ref: GitHub issue link if filed; else `TBD`.

    **Step E — Sign-off:**
    1. Update `06-UAT.md` frontmatter: `status: pending` → `status: complete`. Fill `started` and `updated` ISO timestamps.
    2. Tick all 5 Sign-off checkboxes (only after each is genuinely true; do not tick to ship).
    3. If any `block`-flagged Finding exists: include a single bold line at the top of `## Findings` reading `**WARNING — block-flagged Finding(s) present. v1 cannot ship until resolved in a follow-up phase.**`
  </how-to-verify>
  <resume-signal>
    Type "uat-complete" when all of the following are true:
    - `06-UAT.md` PASS Grid has 8 ticks OR each unticked cell has a corresponding Findings row.
    - `uat-evidence/` contains at least one transcript per client.
    - All evidence files have USER_HOME redacted.
    - `06-UAT.md` frontmatter `status: complete`.
    - Sign-off checklist all ticked.
    - Block-flagged Findings (if any) are explicitly called out.
    Or "uat-blocked: <description>" if a block-flagged Finding makes phase completion impossible — orchestrator will then plan a follow-up fix phase rather than mark Phase 6 complete.
  </resume-signal>
  <files>
    .planning/phases/06-hardening-fixture-gates/06-UAT.md,
    .planning/phases/06-hardening-fixture-gates/uat-evidence/inspector-transcript.md,
    .planning/phases/06-hardening-fixture-gates/uat-evidence/claude-code-transcript.md
  </files>
  <action>
    Operator follows the runbook in 06-UAT.md (Wave 2 deliverable) end-to-end against the nested-routes fixture. Drives MCP Inspector and Claude Code, captures transcripts to uat-evidence/ with USER_HOME redaction (D-12), fills the 2x4 PASS Grid, and records any unexpected behavior in the Findings table per D-13/D-14. See <how-to-verify> for the step-by-step procedure.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/06-hardening-fixture-gates'; const u=fs.readFileSync(p+'/06-UAT.md','utf8'); if(!/status:\s*complete/.test(u)){console.error('UAT not complete'); process.exit(1);} if(!fs.existsSync(p+'/uat-evidence/inspector-transcript.md')&&!fs.existsSync(p+'/uat-evidence/inspector-transcript.txt')){console.error('inspector evidence missing'); process.exit(1);} if(!fs.existsSync(p+'/uat-evidence/claude-code-transcript.md')&&!fs.existsSync(p+'/uat-evidence/claude-code-transcript.txt')){console.error('claude code evidence missing'); process.exit(1);} const ticks=(u.match(/\[x\]/gi)||[]).length; const findings=(u.match(/^\|\s*F-\d+/gm)||[]).length; if(ticks+findings<8){console.error('PASS grid + findings cover <8 cells'); process.exit(1);} console.log('ok');"</automated>
  </verify>
  <done>
    06-UAT.md frontmatter status:complete; PASS grid filled (every cell ticked or has a Findings reference); uat-evidence/ has at least one transcript per client; USER_HOME redacted; any block-flagged Findings surfaced.
  </done>

</task>

</tasks>

<threat_model>
This phase modifies test infrastructure only — no new runtime threat surface. Phase modifies test/UAT artifacts; references Phase 1 D-07 (forward-slash discipline) and Phase 2 MCP-04 (stdout discipline) as load-bearing prior gates that the UAT explicitly probes.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | uat-evidence transcripts could leak user home paths or local environment details | mitigate | D-12 mandatory USER_HOME redaction; sign-off checklist gates commit on redaction confirmation. |
| T-06-10 | Spoofing | Operator might pre-tick PASS Grid without genuine verification | accept | Solo developer + Claude workflow — trust model is single-operator self-reporting. Sign-off checklist + transcripts provide audit trail. |

Block on (per orchestrator security_block_on:high): user PII in evidence files. Mitigated by D-12 redaction rule + sign-off checklist.
</threat_model>

<verification>
- Wave 4 deliverable for SPEC R6.
- Sign-off list is the operator's self-attestation that the 8-tool/2-client grid is genuinely PASS or genuinely FAIL with a Findings row.
- A `block`-flagged Finding does NOT fail this plan — it transitions phase completion to "blocked, requires follow-up phase" and the orchestrator picks that up.
</verification>

<success_criteria>
- `06-UAT.md` PASS Grid is fully filled (8 cells: ticked OR linked to Findings row).
- `uat-evidence/` contains transcripts for both clients.
- All evidence path-redacted.
- Frontmatter `status: complete`.
- Block-flagged Findings (if any) are explicitly surfaced for orchestrator pickup.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-fixture-gates/06-07-SUMMARY.md` summarizing:
- 8-cell PASS Grid outcome (count of PASS vs FAIL).
- Findings count by Severity × Defer/Block.
- Whether any `block`-flagged Finding exists.
- Whether SPEC R6 acceptance is met (PASS for all 4 tools on both clients) OR blocked.
</output>
