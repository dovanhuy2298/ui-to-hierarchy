---
phase: 06-hardening-fixture-gates
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools/find-by-style.ts
  - src/mcp/tools/find-by-text.ts
  - src/mcp/tools/focus-on.ts
  - test/integration/mcp-e2e.test.ts
autonomous: true
gap_closure: true
requirements: [ARCH-04]
covers_spec_ids: [D-15, R4]
must_haves:
  truths:
    - "find_by_style, find_by_text, and focus_on accept an optional `format: 'markdown' | 'json'` input parameter (default 'markdown')"
    - "When `format: 'json'` is passed, the handler returns `JSON.stringify(renderJson(tree, envelope), null, 2)` in `content[0].text` — exactly matching the get_full_hierarchy precedent"
    - "When `format` is omitted or 'markdown', existing markdown behavior is preserved (no behavioral regression for legacy callers)"
    - "The integration test's `argsFor` blocks pass `format: 'json'` for find_by_style/find_by_text/focus_on against all four fixtures so `JSON.parse` no longer throws SyntaxError"
    - "The D-15 envelope contract (schemaVersion: '1') is honored end-to-end across all four MCP tools — symmetry restored"
  artifacts:
    - path: "src/mcp/tools/find-by-style.ts"
      provides: "find_by_style handler with format param + json branch"
      contains: "format:"
    - path: "src/mcp/tools/find-by-text.ts"
      provides: "find_by_text handler with format param + json branch"
      contains: "format:"
    - path: "src/mcp/tools/focus-on.ts"
      provides: "focus_on handler with format param + json branch"
      contains: "format:"
    - path: "test/integration/mcp-e2e.test.ts"
      provides: "argsFor blocks updated to pass format:'json' for the three previously-markdown-only tools"
      contains: "format: \"json\""
  key_links:
    - from: "src/mcp/tools/find-by-style.ts"
      to: "src/renderers/json.ts"
      via: "renderJson(tree, envelope) when format==='json'"
      pattern: "renderJson\\("
    - from: "src/mcp/tools/find-by-text.ts"
      to: "src/renderers/json.ts"
      via: "renderJson(tree, envelope) when format==='json'"
      pattern: "renderJson\\("
    - from: "src/mcp/tools/focus-on.ts"
      to: "src/renderers/json.ts"
      via: "renderJson(tree, envelope) when format==='json'"
      pattern: "renderJson\\("
    - from: "test/integration/mcp-e2e.test.ts"
      to: "src/mcp/tools/*.ts"
      via: "argsFor passes format:'json' so extractEnvelope's JSON.parse succeeds"
      pattern: "format: \"json\""
---

<objective>
Close gap #1 from 06-DEBUG.md: three of four MCP tool handlers (`find_by_style`, `find_by_text`, `focus_on`) currently return markdown-only in `content[0].text`, which makes the integration suite's `JSON.parse(text)` throw `SyntaxError: Unexpected token '<'` for 12 of 16 invocations (3 tools × 4 fixtures). Adopt the `get_full_hierarchy` precedent: add a `format: z.enum(["markdown","json"]).default("markdown")` schema field and a 4-line handler branch on each of the three tools, then update the integration test's `argsFor` to opt in to `json` for these tools.

Purpose: Restore D-15 envelope symmetry across all four MCP tools so the integration suite can observe — and thereby unblock — failures #2 and #3 (handled by Plans 06-09 and 06-10). Without this fix, the JSON.parse line short-circuits every other assertion.
Output: Schema + handler updates on three tool files, plus a small `argsFor` update in the integration test. No behavioral change for existing markdown-only callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-fixture-gates/06-SPEC.md
@.planning/phases/06-hardening-fixture-gates/06-DEBUG.md

# Reference implementation (the one tool that already does this correctly)
@src/mcp/tools/get-full-hierarchy.ts

# The three tools to modify
@src/mcp/tools/find-by-style.ts
@src/mcp/tools/find-by-text.ts
@src/mcp/tools/focus-on.ts

# Failing test that drives this fix
@test/integration/mcp-e2e.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add format param + json branch to the three tool handlers</name>
  <files>
    src/mcp/tools/find-by-style.ts,
    src/mcp/tools/find-by-text.ts,
    src/mcp/tools/focus-on.ts
  </files>
  <read_first>
    - src/mcp/tools/get-full-hierarchy.ts (entire file, ~48 lines) — this is the canonical pattern. Lines 10 (renderJson import), 26-31 (format schema field), 42-45 (handler branch) are the exact shapes to replicate.
    - src/mcp/tools/find-by-style.ts (entire file) — current shape, ~38 lines.
    - src/mcp/tools/find-by-text.ts (entire file) — current shape, ~38 lines.
    - src/mcp/tools/focus-on.ts (entire file) — current shape, ~43 lines.
  </read_first>
  <action>
    Apply the same three-edit pattern from `get-full-hierarchy.ts` to each of the three target tool files. Do NOT rewrite the handlers — only add: (a) one import, (b) one schema field, (c) one branch in the `text` assignment.

    For each of the three files (`find-by-style.ts`, `find-by-text.ts`, `focus-on.ts`):

    1. **Add the renderJson import.** Currently each file imports only `renderMarkdown`:
       ```ts
       import { renderMarkdown } from "../../renderers/markdown.js";
       ```
       Add directly after it:
       ```ts
       import { renderJson } from "../../renderers/json.js";
       ```
       (Mirror line 10 of `get-full-hierarchy.ts`.)

    2. **Add the `format` field to `inputSchema`.** Insert into the existing `z.object({...})` object before `projectRoot: projectRootSchema` — same shape as `get-full-hierarchy.ts:26-31`:
       ```ts
       format: z
         .enum(["markdown", "json"])
         .default("markdown")
         .describe(
           "Output format: markdown (default, LLM-friendly tree) or json (structured object for programmatic use).",
         ),
       ```
       Place locations:
       - `find-by-style.ts`: insert between current line 23 (`),` closing the `class_or_prop` describe) and line 24 (`projectRoot: projectRootSchema,`).
       - `find-by-text.ts`: insert between current line 23 (`),` closing the `query` describe) and line 24 (`projectRoot: projectRootSchema,`).
       - `focus-on.ts`: insert between current line 28 (`),` closing the `scope` describe) and line 29 (`projectRoot: projectRootSchema,`).

    3. **Branch the rendered output on `args.format`.** In each handler, replace the single line:
       ```ts
       const text = renderMarkdown(tree, envelope);
       ```
       with the ternary from `get-full-hierarchy.ts:42-45`:
       ```ts
       const text =
         args.format === "json"
           ? JSON.stringify(renderJson(tree, envelope), null, 2)
           : renderMarkdown(tree, envelope);
       ```
       Line locations of the line to replace: `find-by-style.ts:34`, `find-by-text.ts:34`, `focus-on.ts:39`.

    Invariants to preserve:
    - Default value of `format` MUST be `"markdown"` so existing callers (which omit it) see no change.
    - The `withErrorBoundary`, `resolveRoot`, `Analyzer` construction, and `buildEnvelope` lines are unchanged.
    - The `content: [{ type: "text" as const, text }]` return shape is unchanged.
    - `args` type widens automatically via `z.infer<typeof inputSchema>` — no manual type updates needed.
    - Do NOT modify `src/mcp/tools/get-full-hierarchy.ts` (already correct).
    - Do NOT touch `src/renderers/json.ts` or `src/renderers/markdown.ts`.

    Anti-patterns (per 06-DEBUG.md):
    - Do NOT pursue the "always-include-JSON-alongside-markdown" alternative (option 2 in DEBUG §1) — it couples response shape to test introspection. The `format` parameter is the chosen approach (DEBUG recommended option 1).
    - Do NOT add `format` to `tools/common.ts` as a shared schema — keep it inline so each tool's input schema stays self-describing in the MCP wire protocol (matches the get_full_hierarchy precedent).
  </action>
  <acceptance_criteria>
    - `grep -c 'renderJson' src/mcp/tools/find-by-style.ts` returns ≥ 2 (import + use).
    - `grep -c 'renderJson' src/mcp/tools/find-by-text.ts` returns ≥ 2.
    - `grep -c 'renderJson' src/mcp/tools/focus-on.ts` returns ≥ 2.
    - `grep -c 'format:' src/mcp/tools/find-by-style.ts` returns ≥ 1.
    - `grep -c 'format:' src/mcp/tools/find-by-text.ts` returns ≥ 1.
    - `grep -c 'format:' src/mcp/tools/focus-on.ts` returns ≥ 1.
    - `grep -c '"markdown"' src/mcp/tools/find-by-style.ts` returns ≥ 1 (default value present).
    - `pnpm build` exits 0 with no TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    All three tool handlers accept `format: "markdown" | "json"` (default "markdown"), import and use `renderJson` for the json branch, and produce identical output to the existing markdown path when format is omitted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update integration test argsFor to opt in to format:"json"</name>
  <files>test/integration/mcp-e2e.test.ts</files>
  <read_first>
    - test/integration/mcp-e2e.test.ts (entire file, ~301 lines) — focus on the four `argsFor` blocks at lines 80-92 (shadcn), 113-125 (nested), 154-166 (web), 194-206 (admin). Each currently passes `format: "json"` only on the `get_full_hierarchy` case.
    - 06-DEBUG.md §1 "Fix shape" — confirms test must pass `format: "json"` for the other three tools too.
  </read_first>
  <action>
    In each of the four `argsFor` blocks (one per fixture's `FixtureInvariants` object), add `format: "json"` to the args returned for `focus_on`, `find_by_text`, and `find_by_style`. Currently only `get_full_hierarchy` passes it.

    Concretely, for EACH of the four invariant objects (`shadcnInvariants`, `nestedInvariants`, `monorepoWebInvariants`, `monorepoAdminInvariants`):

    - In the `case "focus_on":` arm, add `format: "json"` to the returned object literal alongside `component`, `scope`, and `projectRoot`.
    - In the `case "find_by_text":` arm, add `format: "json"` alongside `query` and `projectRoot`.
    - In the `case "find_by_style":` arm, add `format: "json"` alongside `class_or_prop` and `projectRoot`.

    Example — `shadcnInvariants.argsFor`, after change:
    ```ts
    case "focus_on":
      return { component: "Button", scope: "full", format: "json", projectRoot: root };
    case "find_by_text":
      return { query: "Click me", format: "json", projectRoot: root };
    case "find_by_style":
      return { class_or_prop: "flex", format: "json", projectRoot: root };
    ```

    Apply the same three additions to the other three invariant blocks. Do NOT touch the `get_full_hierarchy` case (it already passes `format: "json"`).

    Invariants to preserve:
    - Do NOT modify `extractEnvelope`, `assert`, the suite factory `makeFixtureSuite`, or any walker/helper.
    - Do NOT change the existing assertion logic — Plans 06-09 and 06-10 will assume those assertions are the ones being unlocked, not rewritten.
    - The `ToolName` type does not need a change (`format` is on the args, not the tool name).
  </action>
  <acceptance_criteria>
    - `grep -c 'format: "json"' test/integration/mcp-e2e.test.ts` returns ≥ 16 (4 fixtures × 4 tools — was 4, now should be 16).
    - All four invariant objects (`shadcnInvariants`, `nestedInvariants`, `monorepoWebInvariants`, `monorepoAdminInvariants`) contain `format: "json"` in the focus_on, find_by_text, and find_by_style cases.
    - `pnpm build` (re-run from Task 1) still exits 0.
    - `pnpm test:integration` no longer reports `SyntaxError: Unexpected token '<'` for ANY of the previously failing tool calls. The 12 affected invocations (3 tools × 4 fixtures) now successfully reach `EnvelopeSchema.parse(extractEnvelope(result))`. Other failures (envelope.tree empty for nested/admin assertions, Button.file mismatch on monorepo, 'Manage users' absent on admin) MAY still appear — those are owned by Plans 06-09 (resolver wiring) and 06-10 (TreeNode.attributes). The success criterion for THIS plan is: zero JSON.parse SyntaxErrors across the 16 invocations.
    - `pnpm test` (unit suite) reports the same passing count as before this plan (no regressions). Existing unit tests do not pass `format` and rely on default behavior — must remain green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm test:integration 2>&amp;1 | grep -c "SyntaxError: Unexpected token" | grep -v '^#' | tee /dev/stderr | grep -q '^0$'</automated>
  </verify>
  <done>
    Test file passes `format: "json"` for all four tools across all four fixtures; running `pnpm test:integration` no longer produces any `SyntaxError: Unexpected token '<'` failures; remaining failures (if any) are the responsibility of Plans 06-09 and 06-10 and do not regress.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan modifies MCP tool input schemas (the wire-protocol surface) and the integration test. The trust boundary is the MCP stdio request: untrusted client supplies `format` value; zod validation enforces enum membership.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Tampering | `format` parameter on three tool input schemas | mitigate | `z.enum(["markdown","json"])` rejects any other string at the SDK validation layer before the handler runs; `.default("markdown")` makes omission safe. Same shape as the existing `get_full_hierarchy` precedent — no new attack surface beyond what was already accepted. |
| T-06-08-02 | Information Disclosure | JSON-rendered envelope vs markdown | accept | Both formats render the same `tree` content from the same `Analyzer`; JSON exposes no field that markdown does not. D-15 envelope schema already pins `schemaVersion: "1"` and is the canonical wire shape. |
</threat_model>

<verification>
- After both tasks: `pnpm build` succeeds, `pnpm test:integration` no longer reports SyntaxError for the 12 previously-failing JSON.parse sites.
- Remaining integration failures (Button.file mismatch on monorepo apps/web, 'Manage users' absent on apps/admin, possibly @sidebar/grid-cols-3 on nested-routes) are EXPECTED and owned by Plans 06-09 and 06-10. This plan must not introduce any NEW failure mode beyond those.
- `pnpm test` (unit suite, 241+ tests) remains fully green — existing callers that omit `format` see identical behavior.
- D-15 envelope contract is now honored across all four MCP tools (was 1 of 4, now 4 of 4).
</verification>

<success_criteria>
- All three tool handlers accept the `format` parameter and emit JSON when requested.
- Integration test's argsFor passes `format: "json"` for all 16 tool invocations.
- Zero `SyntaxError: Unexpected token '<'` failures in `pnpm test:integration`.
- Unit suite (`pnpm test`) remains at 241+ passing with no regressions.
- D-15 envelope symmetry restored across the public MCP API.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-fixture-gates/06-08-SUMMARY.md`.
</output>
