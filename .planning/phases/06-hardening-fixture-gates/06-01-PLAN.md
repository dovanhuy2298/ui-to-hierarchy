---
phase: 06-hardening-fixture-gates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/fixtures/phase-06/shadcn-barrels/tsconfig.json
  - test/fixtures/phase-06/shadcn-barrels/next.config.js
  - test/fixtures/phase-06/shadcn-barrels/app/layout.tsx
  - test/fixtures/phase-06/shadcn-barrels/app/page.tsx
  - test/fixtures/phase-06/shadcn-barrels/app/components/ui/index.ts
  - test/fixtures/phase-06/shadcn-barrels/app/components/ui/button.tsx
  - test/fixtures/phase-06/shadcn-barrels/app/components/ui/card.tsx
autonomous: true
requirements: [ARCH-04]
covers_spec_ids: [R1]
must_haves:
  truths:
    - "A hand-crafted shadcn-style App Router fixture exists at test/fixtures/phase-06/shadcn-barrels/"
    - "app/page.tsx imports { Button, Card } from '@/components/ui' (a barrel)"
    - "@/components/ui/index.ts re-exports Button and Card from per-component leaf files"
    - "Each leaf .tsx file carries realistic className tokens and visible text so find_by_style and find_by_text invariants have something to match"
    - "Fixture contains no node_modules/ — resolution is tsconfig-paths-only"
  artifacts:
    - path: "test/fixtures/phase-06/shadcn-barrels/tsconfig.json"
      provides: "@/* path alias mapping to ./app/* so @/components/ui resolves to ./app/components/ui"
      contains: "\"paths\""
    - path: "test/fixtures/phase-06/shadcn-barrels/next.config.js"
      provides: "Next.js detection marker"
    - path: "test/fixtures/phase-06/shadcn-barrels/app/layout.tsx"
      provides: "Root layout (html/body wrapper)"
    - path: "test/fixtures/phase-06/shadcn-barrels/app/page.tsx"
      provides: "Page component importing { Button, Card } from '@/components/ui'"
      contains: "from \"@/components/ui\""
    - path: "test/fixtures/phase-06/shadcn-barrels/app/components/ui/index.ts"
      provides: "Barrel re-exporting Button and Card"
      contains: "export { Button }"
    - path: "test/fixtures/phase-06/shadcn-barrels/app/components/ui/button.tsx"
      provides: "Leaf Button component"
    - path: "test/fixtures/phase-06/shadcn-barrels/app/components/ui/card.tsx"
      provides: "Leaf Card component"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/ui/button.tsx"
      via: "barrel @/components/ui → ./button"
      pattern: "export \\{ Button \\} from \"./button\""
---

<objective>
Author the `shadcn-barrels` fixture used by the Phase 6 integration suite to prove SPEC R1 acceptance: barrel-chain resolution lands on leaf component files (`components/ui/button.tsx`), not on the barrel `index.ts`. Per D-05 (no fake `node_modules`) the fixture is purely a tsconfig-paths-resolved hand-crafted Next.js App Router project. Per D-13/D-14 this fixture is a verification target only — bug fixes are out of scope.

Purpose: Provide the hand-crafted on-disk test target for SPEC R1 + R4 (one of three fixtures × four tools) and exercise the barrel-chase code path end-to-end through the spawned binary.
Output: `test/fixtures/phase-06/shadcn-barrels/` directory tree (7 files, all hand-written, no generation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-fixture-gates/06-SPEC.md
@.planning/phases/06-hardening-fixture-gates/06-CONTEXT.md
@.planning/phases/06-hardening-fixture-gates/06-RESEARCH.md
@.planning/phases/06-hardening-fixture-gates/06-PATTERNS.md

# Existing analogs (port shapes, do not import)
@test/fixtures/parser/resolver/shadcn-barrel/
@test/fixtures/phase-05/kitchen-sink/app/layout.tsx
@test/fixtures/phase-05/kitchen-sink/tsconfig.json
@test/fixtures/phase-05/kitchen-sink/next.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author shadcn-barrels fixture tree</name>
  <files>
    test/fixtures/phase-06/shadcn-barrels/tsconfig.json,
    test/fixtures/phase-06/shadcn-barrels/next.config.js,
    test/fixtures/phase-06/shadcn-barrels/app/layout.tsx,
    test/fixtures/phase-06/shadcn-barrels/app/page.tsx,
    test/fixtures/phase-06/shadcn-barrels/app/components/ui/index.ts,
    test/fixtures/phase-06/shadcn-barrels/app/components/ui/button.tsx,
    test/fixtures/phase-06/shadcn-barrels/app/components/ui/card.tsx
  </files>
  <read_first>
    - 06-RESEARCH.md "Fixture: shadcn barrel chain (R1 minimum-viable shape)" code blocks (lines 622-664) — lift exact shapes
    - 06-PATTERNS.md "test/fixtures/phase-06/shadcn-barrels/**" section (lines 178-226) — pattern assignments
    - test/fixtures/parser/resolver/shadcn-barrel/ — existing barrel chain analog (Phase 3)
    - test/fixtures/phase-05/kitchen-sink/tsconfig.json — App Router tsconfig shape to mirror
    - test/fixtures/phase-05/kitchen-sink/next.config.js — minimal next.config shape
    - test/fixtures/phase-05/kitchen-sink/app/layout.tsx — root layout shape
  </read_first>
  <action>
    Create the fixture directory tree below per D-05 (no node_modules). All files are committed source — no generation, no pnpm install.

    1. `tsconfig.json` — JSON, exactly:
       ```json
       {
         "compilerOptions": {
           "jsx": "preserve",
           "module": "esnext",
           "moduleResolution": "bundler",
           "target": "es2022",
           "baseUrl": ".",
           "paths": { "@/*": ["./app/*"] }
         }
       }
       ```
       (The mapping `"@/*": ["./app/*"]` makes `@/components/ui` resolve to `./app/components/ui` — required because the shadcn fixture's leaf files live under `app/components/ui/`. This mapping is committed at plan time; do not deviate. Mirrors `test/fixtures/phase-05/kitchen-sink/tsconfig.json` `paths` style.)

    2. `next.config.js` — exactly: `module.exports = {};`

    3. `app/layout.tsx` — root layout, exactly:
       ```tsx
       export default function RootLayout({ children }: { children: React.ReactNode }) {
         return <html><body>{children}</body></html>;
       }
       ```

    4. `app/page.tsx` — uses 2-hop barrel chain (CONTEXT.md "Claude's Discretion" default):
       ```tsx
       import { Button, Card } from "@/components/ui";

       export default function Home() {
         return (
           <Card>
             <Button>Click me</Button>
           </Card>
         );
       }
       ```

    5. `app/components/ui/index.ts` — barrel:
       ```ts
       export { Button } from "./button";
       export { Card } from "./card";
       ```

    6. `app/components/ui/button.tsx` — leaf with realistic className for find_by_style invariants:
       ```tsx
       export function Button({ children }: { children: React.ReactNode }) {
         return <button className="flex items-center gap-2 px-4 py-2">{children}</button>;
       }
       ```

    7. `app/components/ui/card.tsx` — leaf with realistic className:
       ```tsx
       export function Card({ children }: { children: React.ReactNode }) {
         return <div className="flex flex-col rounded-lg border p-4">{children}</div>;
       }
       ```

    Constraints (per D-05, anti-patterns in 06-RESEARCH.md):
    - NO `node_modules/` directory anywhere under this fixture.
    - NO `package.json` (per kitchen-sink precedent — fixture is a resolution target, not an installable package).
    - NO `.next/` build output.
    - All paths in code use forward slashes.
    - Visible text "Click me" is the find_by_text target (must appear in button.tsx output). Class tokens "flex" and "flex-col" are find_by_style targets.
  </action>
  <acceptance_criteria>
    - File `test/fixtures/phase-06/shadcn-barrels/tsconfig.json` exists.
    - `grep -c '"@/\*"' test/fixtures/phase-06/shadcn-barrels/tsconfig.json` returns ≥1 (path alias defined).
    - File `test/fixtures/phase-06/shadcn-barrels/next.config.js` exists and equals `module.exports = {};` (single line).
    - File `test/fixtures/phase-06/shadcn-barrels/app/layout.tsx` exports a default function.
    - `grep -c 'from "@/components/ui"' test/fixtures/phase-06/shadcn-barrels/app/page.tsx` returns 1.
    - `grep -c 'export { Button }' test/fixtures/phase-06/shadcn-barrels/app/components/ui/index.ts` returns 1.
    - `grep -c 'export { Card }' test/fixtures/phase-06/shadcn-barrels/app/components/ui/index.ts` returns 1.
    - `grep -c 'flex' test/fixtures/phase-06/shadcn-barrels/app/components/ui/button.tsx` returns ≥1 (find_by_style target).
    - `grep -c 'Click me' test/fixtures/phase-06/shadcn-barrels/app/page.tsx` returns 1 (find_by_text target).
    - No `node_modules` directory: `test ! -d test/fixtures/phase-06/shadcn-barrels/node_modules`.
    - All `.tsx` and `.ts` files parse with the project's existing parser (verified indirectly — typecheck not required since fixtures are intentionally outside `tsconfig.json` `include`).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='test/fixtures/phase-06/shadcn-barrels'; const must=['tsconfig.json','next.config.js','app/layout.tsx','app/page.tsx','app/components/ui/index.ts','app/components/ui/button.tsx','app/components/ui/card.tsx']; for(const f of must){if(!fs.existsSync(p+'/'+f)) {console.error('MISSING',f); process.exit(1);}} if(fs.existsSync(p+'/node_modules')){console.error('forbidden node_modules'); process.exit(1);} console.log('ok');"</automated>
  </verify>
  <done>
    Seven files exist with the exact shapes above; no node_modules; barrel chain `page.tsx → @/components/ui → index.ts → ./button → button.tsx` is wired; find_by_text and find_by_style targets are present.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase modifies test infrastructure only — no new runtime threat surface. The phase's load-bearing prior gate is Phase 1's IR + envelope input-validation surface; Phase 2's MCP-04 stdout discipline; Phase 3's resolver. No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Fixture file content committed to public repo | accept | Fixture content is hand-written, contains no PII or secrets — only synthetic component code (`<Button>Click me</Button>`). Privacy carve-out (D-12) applies to UAT evidence only, not fixtures. |

ASVS L1 baseline applies — no new threats introduced. Block on: any plan committing real `pnpm install` artifacts (D-05 forbidden) or any user PII into fixtures (none present).
</threat_model>

<verification>
- Wave 0 deliverable for SPEC R1; consumed by Wave 1 integration suite (06-04-PLAN).
- Path gate (R5) is verified by the integration suite, not this plan.
- No source code in `src/` is modified — fixtures are purely test infrastructure.
</verification>

<success_criteria>
- All 7 fixture files committed to `test/fixtures/phase-06/shadcn-barrels/`.
- Fixture tree matches the structure documented in 06-RESEARCH.md "Recommended Project Structure" (lines 198-211).
- No `node_modules/` present (D-05).
- find_by_text target ("Click me") and find_by_style target ("flex") are present in source.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-fixture-gates/06-01-SUMMARY.md`.
</output>
