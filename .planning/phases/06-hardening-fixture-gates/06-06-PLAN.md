---
phase: 06-hardening-fixture-gates
plan: 06
type: execute
wave: 2
depends_on: [04]
files_modified:
  - .planning/phases/06-hardening-fixture-gates/06-UAT.md
autonomous: true
requirements: [ARCH-04]
covers_spec_ids: [R6]
must_haves:
  truths:
    - "06-UAT.md template exists with 2×4 PASS grid (Claude Code × MCP Inspector × 4 tools) per D-15"
    - "Findings table schema matches D-13 exactly: ID | Severity | Tool | Repro | Defer/Block | Ref"
    - "Local Windows runbook section documents pnpm install → build → test → test:integration → test:smoke → perf → UAT order"
    - "Stdout cleanliness probe step is documented (RESEARCH Open Q4): node dist/cli.js < NUL on Windows"
    - "Evidence section explicitly references uat-evidence/ subdirectory + path-redaction rule (replace USER_HOME)"
    - "Defer/Block flag policy per D-14 is documented inline so the operator can categorize Findings correctly"
  artifacts:
    - path: ".planning/phases/06-hardening-fixture-gates/06-UAT.md"
      provides: "Human-driven UAT runbook + PASS grid template + Findings table template"
      min_lines: 80
      contains: "PASS Grid"
  key_links:
    - from: "06-UAT.md"
      to: "uat-evidence/ directory"
      via: "Evidence section enumerating expected files"
      pattern: "uat-evidence"
    - from: "06-UAT.md"
      to: "06-04-PLAN test:integration script"
      via: "runbook step 3"
      pattern: "test:integration"
---

<objective>
Author the `06-UAT.md` template that the Wave 4 plan operator will fill in by hand. The template is empty grids + a runbook + the Findings schema; it does NOT contain UAT execution results yet (those come in Wave 4 via `/gsd-uat`).

Per D-15 the UAT covers `nested-routes` only (the most representative fixture; other fixtures already have automated coverage in Wave 1). Per D-13 the Findings table schema is fixed (`ID | Severity | Tool | Repro | Defer/Block | Ref`). Per D-14 the Defer/Block flag must follow a strict rule: `block` only when a finding falsifies a prior phase's verification.

Purpose: Hand the human operator a fill-in-the-blank document that produces SPEC R6 evidence after a real Claude Code + MCP Inspector session.
Output: One file — `.planning/phases/06-hardening-fixture-gates/06-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-hardening-fixture-gates/06-SPEC.md
@.planning/phases/06-hardening-fixture-gates/06-CONTEXT.md
@.planning/phases/06-hardening-fixture-gates/06-RESEARCH.md
@.planning/phases/06-hardening-fixture-gates/06-PATTERNS.md
@.planning/phases/05-ir-queries-tool-wire-up/05-UAT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 06-UAT.md template</name>
  <files>.planning/phases/06-hardening-fixture-gates/06-UAT.md</files>
  <read_first>
    - 06-PATTERNS.md "06-UAT.md" section (lines 355-409) — body structure to lift
    - 06-RESEARCH.md "Pitfall 6: Inspector/Claude Code stdout corruption" (lines 582-588) — runbook stdout probe
    - 06-RESEARCH.md "Open Question 4" (lines 770-773) — stdout cleanliness probe rationale
    - 06-CONTEXT.md D-12, D-13, D-14, D-15 (verbatim)
    - .planning/phases/05-ir-queries-tool-wire-up/05-UAT.md — Phase 5 frontmatter shape (NOT body — Phase 5 is automated mode)
  </read_first>
  <action>
    Create `.planning/phases/06-hardening-fixture-gates/06-UAT.md` with this exact structure. Do NOT fill in PASS grid checkboxes or Findings rows — those are operator work in Wave 4.

    ### Frontmatter (mirror Phase 5 shape; mode = manual)

    ```yaml
    ---
    status: pending
    phase: 06-hardening-fixture-gates
    source:
      - .planning/phases/06-hardening-fixture-gates/06-SPEC.md
      - .planning/phases/06-hardening-fixture-gates/06-CONTEXT.md
    started:
    updated:
    mode: manual
    ---
    ```
    `started` and `updated` are blank — operator fills on session start/end.

    ### Body sections (in this order)

    #### `# Phase 6 — UAT (Hardening & Fixture Gates)`

    Brief 2-line preamble: This UAT verifies SPEC R6 — both Claude Code and MCP Inspector exercise all four MCP tools against the `nested-routes` fixture end-to-end. Coverage = 2×4 grid per D-15. Findings recorded per D-13 schema; `block` flag governed by D-14 policy.

    #### `## Runbook (Local Windows)`

    Numbered ordered list:
    1. `pnpm install`
    2. `pnpm build` — produces `dist/cli.js`
    3. `pnpm test` — unit suite (existing)
    4. `pnpm test:integration` — Phase 6 integration suite (R1–R5)
    5. `pnpm test:smoke` — existing MCP smoke (MCP-01, MCP-04)
    6. `pnpm perf` — generates/refreshes `06-PERF.md` (R7); commit if hardware unchanged
    7. **Stdout cleanliness probe** (defense in depth before connecting clients): On Windows: `node dist/cli.js < NUL` and confirm zero stdout output before EOF. POSIX equivalent: `node dist/cli.js < /dev/null`.
    8. UAT — MCP Inspector: `npx @modelcontextprotocol/inspector node dist/cli.js`. In the Inspector UI, exercise each of the 4 tools against the `nested-routes` fixture. Use these arguments:
       - `get_full_hierarchy` → `{ "route": "/dashboard/123", "format": "json", "projectRoot": "<absolute path to test/fixtures/phase-06/nested-routes>" }`
       - `focus_on` → `{ "component": "DashboardDetail", "scope": "full", "projectRoot": "<...>" }`
       - `find_by_text` → `{ "query": "Sidebar slot", "projectRoot": "<...>" }`
       - `find_by_style` → `{ "class_or_prop": "grid-cols-3", "projectRoot": "<...>" }`
       Capture the session transcript to `uat-evidence/inspector-transcript.md` (or `.txt`). Optional: 1–2 PNG screenshots to `uat-evidence/inspector-*.png`.
    9. UAT — Claude Code: configure your `~/.claude.json` (or per-project `.mcp.json`) with the entry:
       ```json
       {
         "mcpServers": {
           "ui-to-hierarch": { "command": "node", "args": ["<absolute path to dist/cli.js>"] }
         }
       }
       ```
       Restart Claude Code. Prompt the agent to invoke each of the 4 tools against `<absolute path to test/fixtures/phase-06/nested-routes>`. Capture the agent transcript (paste from Claude Code UI) to `uat-evidence/claude-code-transcript.md`. Optional 1–2 screenshots.
    10. **Path redaction (D-12):** before committing any transcript / screenshot, replace any absolute path containing your home directory with the literal `<USER_HOME>`. (Example: `C:/Users/jane/...` → `<USER_HOME>/...`.)
    11. Fill the PASS Grid below for each tool × client (8 checkboxes). For each FAIL or unexpected behavior, add a row to the Findings table with the appropriate Severity + Defer/Block flag.

    #### `## PASS Grid (D-15)`

    Markdown table (empty checkboxes — operator fills):

    ```markdown
    | Tool                | MCP Inspector | Claude Code |
    |---------------------|---------------|-------------|
    | get_full_hierarchy  | [ ]           | [ ]         |
    | focus_on            | [ ]           | [ ]         |
    | find_by_text        | [ ]           | [ ]         |
    | find_by_style       | [ ]           | [ ]         |
    ```

    #### `## Findings (D-13 schema)`

    Brief lead-in describing severity and Defer/Block policy, then the empty table:

    > **Severity:** `blocker` (cannot ship), `major` (degraded UX), `minor` (cosmetic / non-functional).
    > **Defer/Block (D-14):** `block` ONLY when the finding falsifies a prior phase's verification. Concrete examples:
    > - A tool returns `isError: true` on `nested-routes` where Phase 5 unit tests pass → `block` (falsifies Phase 5 R8 no-throw contract).
    > - A `file` field contains a backslash where Phase 1 D-07 forward-slash discipline says it cannot → `block` (falsifies Phase 1 D-07).
    > - Any other issue (UX papercut, slow-but-correct tool, deeper resolution gap not covered by prior phase acceptance) → `defer`.
    > **Ref:** GitHub issue link if filed, else `TBD`.

    ```markdown
    | ID    | Severity  | Tool        | Repro                                  | Defer/Block | Ref  |
    |-------|-----------|-------------|----------------------------------------|-------------|------|
    | F-01  |           |             |                                        |             |      |
    ```

    Include only one empty row template — operator adds more as needed.

    #### `## Evidence`

    Bullet list of expected paths (operator confirms files exist before marking status complete):

    - `uat-evidence/inspector-transcript.md` (or `.txt`)
    - `uat-evidence/claude-code-transcript.md` (or `.txt`)
    - `uat-evidence/<screenshot>.png` (zero or more; at least one per client recommended for SPEC R6 evidence)
    - All evidence files have user home directory paths redacted to `<USER_HOME>` per D-12.

    #### `## Sign-off`

    - [ ] All 8 PASS Grid checkboxes are checked OR each unchecked cell has a corresponding Finding row with `block`.
    - [ ] `06-PERF.md` is committed (R7 — generated by `pnpm perf`).
    - [ ] `uat-evidence/` directory contains at least one transcript per client.
    - [ ] Path redaction applied to all evidence files.
    - [ ] Frontmatter `status: pending` flipped to `status: complete`; `started` and `updated` populated.

    ### Constraints
    - DO NOT pre-tick any PASS Grid checkbox — that is operator work in Wave 4.
    - DO NOT add Findings rows other than the one empty placeholder.
    - DO NOT generate `06-PERF.md` from this plan (that is 06-05-PLAN's job).
    - Frontmatter `mode: manual` (NOT `automated` — Phase 5 was automated; Phase 6 is human-driven UAT per SPEC R6).
    - Use forward slashes in all example paths (CLAUDE.md non-negotiable).
  </action>
  <acceptance_criteria>
    - File `.planning/phases/06-hardening-fixture-gates/06-UAT.md` exists.
    - Frontmatter contains `status: pending` and `mode: manual`.
    - File contains a `## Runbook` heading.
    - File contains a `## PASS Grid` heading.
    - File contains a `## Findings` heading.
    - File contains a `## Evidence` heading.
    - File contains a `## Sign-off` heading.
    - PASS Grid table has exactly 4 tool rows × 2 client columns of `[ ]` checkboxes (8 total empty checkboxes).
    - Findings table has the exact column header `| ID    | Severity  | Tool        | Repro` (or grep-equivalent for `Defer/Block` and `Ref`).
    - Findings table has exactly 1 empty row (just `F-01` with empty cells) — no operator data pre-filled.
    - File mentions `nested-routes` in runbook (D-15 fixture target).
    - File mentions `npx @modelcontextprotocol/inspector` (Inspector invocation per RESEARCH).
    - File mentions `node dist/cli.js < NUL` OR `< /dev/null` (stdout cleanliness probe per Open Q4).
    - File mentions `<USER_HOME>` (D-12 redaction rule).
    - File documents the D-14 block-flag policy (string `"falsifies"` or equivalent).
    - File mentions `pnpm test:integration` and `pnpm perf` in runbook order.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/06-hardening-fixture-gates/06-UAT.md'; if(!fs.existsSync(p)){console.error('missing'); process.exit(1);} const c=fs.readFileSync(p,'utf8'); const must=['status: pending','mode: manual','## Runbook','## PASS Grid','## Findings','## Evidence','## Sign-off','nested-routes','@modelcontextprotocol/inspector','<USER_HOME>','pnpm test:integration','pnpm perf','falsif']; for(const m of must) if(!c.includes(m)){console.error('missing',m); process.exit(1);} const checkboxes=(c.match(/\\[ \\]/g)||[]).length; if(checkboxes<8){console.error('expected ≥8 empty checkboxes, got',checkboxes); process.exit(1);} if(!/<\\s*NUL|\\/dev\\/null/.test(c)){console.error('stdout probe missing'); process.exit(1);} console.log('ok');"</automated>
  </verify>
  <done>
    Template committed; operator can fill it during Wave 4 UAT execution. PASS Grid is empty; Findings table has schema row + one empty data row; runbook is complete and ordered correctly.
  </done>
</task>

</tasks>

<threat_model>
This phase modifies test infrastructure only — no new runtime threat surface. Phase modifies test infrastructure only (a planning document); references Phase 1's input-validation surface as the load-bearing prior gate.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Information Disclosure | UAT evidence committed to repo could leak user home paths | mitigate | D-12 redaction rule documented in runbook step 10; sign-off checklist requires path redaction confirmed before commit. |

ASVS L1 baseline applies — no new runtime threats.
</threat_model>

<verification>
- Wave 2 template — Wave 4 (operator UAT) consumes this file.
- Empty PASS Grid + empty Findings ensures no false signals; operator must explicitly tick boxes.
- Runbook ordering matches the test pyramid: unit → integration → smoke → perf → manual UAT.
</verification>

<success_criteria>
- File exists with frontmatter `status: pending`, `mode: manual`.
- All 5 sections present (Runbook, PASS Grid, Findings, Evidence, Sign-off).
- 8 empty PASS-grid checkboxes; 1 empty Findings row; D-14 policy explanation present.
- Runbook documents stdout cleanliness probe + path redaction.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-fixture-gates/06-06-SUMMARY.md`.
</output>
