---
phase: 08-v1-0-polish
plan: 04
type: execute
wave: 3
depends_on: [08-01-PLAN, 08-03-PLAN]
files_modified:
  - test/integration/mcp-markdown.test.ts
  - test/integration/_helpers.ts
autonomous: true
requirements: [POLISH-02]
must_haves:
  truths:
    - "Integration suite calls a tool with format:\"markdown\" against test/fixtures/phase-05/micro end-to-end via the spawned MCP binary"
    - "Integration suite calls a tool with format:\"markdown\" against test/fixtures/phase-05/kitchen-sink end-to-end via the spawned MCP binary"
    - "Each markdown integration case asserts: tree glyph present, every non-comment line contains ` @ ` separator, output contains no backslash"
  artifacts:
    - path: "test/integration/mcp-markdown.test.ts"
      provides: "Two integration cases (micro + kitchen-sink) exercising format:markdown"
      contains: "format: \"markdown\""
    - path: "test/integration/_helpers.ts"
      provides: "Shared spawn/client setup (optional per D-06)"
  key_links:
    - from: "test/integration/mcp-markdown.test.ts"
      to: "dist/cli.js"
      via: "StdioClientTransport spawning node dist/cli.js"
      pattern: "StdioClientTransport"
---

<objective>
POLISH-02 (D-05, D-06, D-07): Add an integration suite covering `format: "markdown"` end-to-end through the MCP tool boundary. Two fixtures, three assertions per case (glyph, `@` separator, no backslash). Lives in a new sibling file so existing mcp-e2e.test.ts stays JSON-focused.

Purpose: Markdown rendering has zero integration coverage today. After this plan, every release verifies the markdown wire output for both a minimal and a realistic Next.js project.
Output: New integration file + (per D-06) optional shared helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-v1-0-polish/08-SPEC.md
@.planning/phases/08-v1-0-polish/08-CONTEXT.md
@test/integration/mcp-e2e.test.ts

<interfaces>
From test/integration/mcp-e2e.test.ts — the established spawn pattern:
  - `assertFreshBuild()` guards `dist/cli.js` against staleness vs `src/cli.ts`.
  - `new StdioClientTransport({ command: "node", args: [distCli], stderr: "pipe" })`
  - `new Client({ name, version: "0.0.0" })` then `client.connect(transport)`.
  - `client.callTool({ name, arguments: { ..., format: "json", projectRoot: root } })`
  - Response shape: `{ isError?, content: Array<{ type: string; text?: string }> }`.
  - For format:"markdown", the tool returns markdown string in `content[0].text` (NOT JSON-parsed).

Fixtures (locked by D-07):
  - test/fixtures/phase-05/micro/mutation-test (smallest valid micro app)
  - test/fixtures/phase-05/kitchen-sink

Assertions per case (locked by D-07):
  (a) `expect(out).toMatch(/[├└│]/)` — at least one tree glyph present.
  (b) Every non-comment line (line that does not start with `<!--`) contains ` @ ` separator.
  (c) `expect(out).not.toContain("\\")` — backslash guard.

Tool choice: use `get_full_hierarchy` with `route: "/"` against both fixtures (mirrors mcp-e2e patterns; this is the simplest tool that exercises renderMarkdown end-to-end).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: (Conditional per D-06) Extract spawn helper if mcp-e2e.test.ts has >10 lines of setup</name>
  <files>test/integration/_helpers.ts, test/integration/mcp-e2e.test.ts</files>
  <read_first>
    - test/integration/mcp-e2e.test.ts lines 1-50 (imports, distCli/srcCli/fixturesRoot setup, assertFreshBuild)
    - test/integration/mcp-e2e.test.ts lines 225-282 (makeFixtureSuite factory — spawn lifecycle)
    - .planning/phases/08-v1-0-polish/08-CONTEXT.md §decisions D-06 (extract only if >10 lines; inline otherwise)
  </read_first>
  <action>
    Inspect the spawn setup in mcp-e2e.test.ts. The beforeAll/afterAll block (lines ~232-247) is 16 lines including transport + client construction, plus assertFreshBuild + path computation (~15 lines at the top). PER D-06 (threshold ~10 lines): EXTRACT into `test/integration/_helpers.ts`. Export `assertFreshBuild()`, `distCli` (path constant or factory), and `spawnMcpClient(): Promise<{ client: Client; transport: StdioClientTransport; stderrChunks: Buffer[] }>` (or a similar pair returning the connected client + transport + stderr accumulator). Refactor mcp-e2e.test.ts to import the helpers — preserve every existing behavior byte-for-byte (no test additions/removals here; only extraction). Run the full integration suite to confirm zero regression.

    If after reading you conclude the inline block is genuinely ≤10 lines OR the refactor risks destabilizing the existing suite, SKIP extraction and instead inline-duplicate the spawn block in plan 04 task 2 — document the decision in the task SUMMARY. Planner discretion is explicit per D-06.
  </action>
  <verify>
    <automated>pnpm vitest run test/integration/mcp-e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Either: `test/integration/_helpers.ts` exists AND exports `assertFreshBuild` + a spawn helper AND mcp-e2e.test.ts imports from it; OR: the SUMMARY explicitly states "inlined per D-06 — block ≤10 lines".
    - `pnpm vitest run test/integration/mcp-e2e.test.ts` exits 0 with the same test count as before this task.
    - No envelope-handling logic was moved into the helper — helper only owns spawn/teardown/stderr accumulation.
  </acceptance_criteria>
  <done>Spawn lifecycle is reusable (or knowingly duplicated per D-06); existing mcp-e2e suite unchanged in behavior.</done>
</task>

<task type="auto">
  <name>Task 2: New markdown integration suite covering micro + kitchen-sink</name>
  <files>test/integration/mcp-markdown.test.ts</files>
  <read_first>
    - test/integration/mcp-e2e.test.ts (entire file — patterns to mirror)
    - test/integration/_helpers.ts (if Task 1 created it)
    - test/fixtures/phase-05/micro/mutation-test/app/page.tsx (confirm the fixture root path; the directory used must contain a working app/layout.tsx + app/page.tsx — pick `mutation-test` if `micro` parent has multiple sub-projects)
    - test/fixtures/phase-05/kitchen-sink/app/page.tsx (confirm it parses cleanly with NextJsAdapter today)
    - .planning/phases/08-v1-0-polish/08-SPEC.md §Requirements item 2 (all three assertions are locked)
  </read_first>
  <action>
    Create test/integration/mcp-markdown.test.ts. Import from `_helpers.ts` if Task 1 created it; otherwise inline the spawn block matching mcp-e2e.test.ts. For each fixture (micro and kitchen-sink), define a describe block. In beforeAll: spawn `node dist/cli.js`, connect MCP client. In afterAll: close client + transport.

    For each fixture, write a single it-block titled `get_full_hierarchy: markdown output passes glyph + @-separator + no-backslash guards`. Inside:
      1. `const result = await client.callTool({ name: "get_full_hierarchy", arguments: { route: "/", format: "markdown", projectRoot: fixturePath } });`
      2. `expect(result.isError).toBeFalsy();`
      3. `const out = result.content.find(c => c.type === "text")?.text; expect(out).toBeDefined();`
      4. `expect(out).toMatch(/[├└│]/);` — at least one tree glyph.
      5. For every non-comment line (split by `\n`, skip lines starting with `<!--` and skip empty lines), assert `expect(line).toContain(" @ ")`.
      6. `expect(out).not.toContain("\\");` — backslash guard (D-07 critical on Windows).

    Resolve fixture paths the same way mcp-e2e.test.ts does (`resolve(__dirname, "../fixtures/phase-05/micro/mutation-test")` and `.../phase-05/kitchen-sink`). If micro/mutation-test does not contain a runnable app/ structure for `route: "/"`, fall back to the first `phase-05/micro/*` directory whose `app/page.tsx` exists; document the chosen path in the SUMMARY. Do NOT add new fixtures in this plan.

    Do NOT JSON.parse the result text — markdown is a string. Do NOT assert anything about envelope.warnings here (warnings prefix is plan 01's concern; this plan only asserts the markdown CONTRACT).
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm vitest run test/integration/mcp-markdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/integration/mcp-markdown.test.ts` exists.
    - `grep -c "format: \"markdown\"" test/integration/mcp-markdown.test.ts` >= 2 (one per fixture).
    - `grep -c "phase-05/micro" test/integration/mcp-markdown.test.ts` >= 1
    - `grep -c "phase-05/kitchen-sink" test/integration/mcp-markdown.test.ts` >= 1
    - `grep -E "not\.toContain\(['\"]\\\\\\\\['\"]\)" test/integration/mcp-markdown.test.ts` returns ≥1 match (backslash guard present).
    - `grep -E "toMatch\(/\[" test/integration/mcp-markdown.test.ts` shows the glyph regex.
    - `pnpm vitest run test/integration/mcp-markdown.test.ts` exits 0 after `pnpm build`.
    - Suite contains ≥2 markdown integration test cases (one per fixture) — verifiable from vitest reporter output.
  </acceptance_criteria>
  <done>Two markdown integration cases pass against micro + kitchen-sink fixtures; all three locked assertions (glyph, ` @ ` separator, no backslash) execute on each.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `pnpm vitest run test/integration` passes
- `grep -c "format: \"markdown\"" test/integration/mcp-markdown.test.ts` >= 2
- `grep -c "format: \"markdown\"" test/integration/mcp-e2e.test.ts` == 0 (JSON-only file preserved per D-05)
</verification>

<success_criteria>
- POLISH-02 satisfied: ≥2 fixtures × `format: "markdown"` × 3 assertions, all passing through the spawned MCP binary.
- D-05 preserved: existing mcp-e2e.test.ts stays JSON-focused; markdown lives in its own sibling file.
- D-07 enforced: backslash guard runs on every fixture (critical on Windows).
- All POLISH requirements in REQUIREMENTS.md can flip from [ ] to [x] (combined with plans 01, 02, 03).
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-0-polish/08-04-SUMMARY.md`.
</output>
