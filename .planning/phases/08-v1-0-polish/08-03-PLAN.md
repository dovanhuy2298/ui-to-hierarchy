---
phase: 08-v1-0-polish
plan: 03
type: execute
wave: 2
depends_on: [08-02-PLAN]
files_modified:
  - src/adapters/types.ts
  - src/core/resolver/index.ts
  - src/core/resolver/barrel.ts
  - src/core/Analyzer.ts
  - test/core/analyzer.test.ts
  - test/adapters/types.test.ts
  - test/fixtures/phase-05/micro/line-test
autonomous: true
requirements: [POLISH-03]
must_haves:
  truths:
    - "ResolveResult local variant carries a `line: number` field"
    - "resolveModule populates `line` by calling parseFile + declLines.get(importedName); falls back to 1 on undefined / parse error"
    - "Analyzer.ts writes result.line onto resolved local-kind TreeNodes instead of hard-coded 1"
    - "For a fixture component declared at line ≥ 3, the resolved TreeNode.line equals that line, not 1"
  artifacts:
    - path: "src/adapters/types.ts"
      provides: "ResolveResult.local variant with `line: number`"
      contains: "kind: \"local\""
    - path: "src/core/resolver/index.ts"
      provides: "resolveModule emitting local results with `line` populated via parseFile lookup"
      contains: "declLines.get"
    - path: "src/core/Analyzer.ts"
      provides: "resolveComponentCallsites writes result.line onto TreeNode"
      contains: "line: result.line"
  key_links:
    - from: "src/core/resolver/index.ts"
      to: "ParseResult.declLines (from plan 08-02)"
      via: "parseFile(ctx, absolutePath) -> declLines.get(importedName)"
      pattern: "declLines\\.get"
    - from: "src/core/Analyzer.ts:299-305"
      to: "ResolveResult.line"
      via: "spread result.line into TreeNode"
      pattern: "line:\\s*result\\.line"
---

<objective>
POLISH-03 part B (D-02, D-03, D-04): Propagate the declaration line discovered in plan 08-02 from the resolver through `ResolveResult` to `Analyzer.ts:299-305`, replacing the hard-coded `line: 1` placeholder. Add a regression fixture and test confirming a known-good true line.

Purpose: Resolved local-kind component nodes today always report `line: 1`, lying to AI consumers about where to make edits. After this plan, the line is the actual declaration line in the resolved file.
Output: Updated ResolveResult union, updated resolver emission sites, updated Analyzer write site, regression fixture, updated unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-v1-0-polish/08-SPEC.md
@.planning/phases/08-v1-0-polish/08-CONTEXT.md
@.planning/phases/08-v1-0-polish/08-02-PLAN.md
@src/adapters/types.ts
@src/core/resolver/index.ts
@src/core/resolver/barrel.ts
@src/core/Analyzer.ts
@src/adapters/next/NextJsAdapter.ts

<interfaces>
From src/adapters/types.ts:259 (current):
  | { ok: true; kind: "local"; absolutePath: string }

Target (D-02):
  | { ok: true; kind: "local"; absolutePath: string; line: number }

From src/core/resolver/index.ts:79, 97 — both emit `{ ok: true, kind: "local", absolutePath: fwd }`.
From src/core/resolver/barrel.ts:82 — emits `{ ok: true, kind: "local", absolutePath: startFile }`.

From src/core/Analyzer.ts:299-305 — the SINGLE write site for resolved local nodes (line 304 = `line: 1`).
The branch at line 312 (`// external or unresolved: preserve call-site file:line`) is UNCHANGED — D-04 explicitly preserves D-12/D-13 behavior for non-local variants.

From src/core/parser/index.ts (after plan 08-02): parseFile is cache-friendly; calling it from resolveModule for an already-parsed absolutePath is free (returns cached ParseResult with declLines).

Fallback rule (D-03):
  - if parseFile returns kind:"error" -> line: 1
  - if declLines.get(importedName) is undefined (default exports, anonymous, re-export indirection not recorded) -> line: 1
  - else -> declLines.get(importedName)!
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ResolveResult.local with `line: number`</name>
  <files>src/adapters/types.ts</files>
  <read_first>
    - src/adapters/types.ts lines 244-264 (ResolveResult union with JSDoc and ambiguous variant)
    - .planning/phases/08-v1-0-polish/08-CONTEXT.md §decisions D-02, D-03, D-04
  </read_first>
  <behavior>
    - `ResolveResult` local variant is exactly `{ ok: true; kind: "local"; absolutePath: string; line: number }`.
    - The `external`, `cycle`, `not-found`, `ambiguous` variants are byte-identical to today (D-04).
  </behavior>
  <action>
    Edit the discriminated union literal at src/adapters/types.ts:260. Change `{ ok: true; kind: "local"; absolutePath: string }` to `{ ok: true; kind: "local"; absolutePath: string; line: number }`. Leave every other arm of the union and the surrounding JSDoc untouched (the JSDoc may be lightly extended to mention "line: declaration line per D-02; 1 fallback per D-03"). Do not modify ParseResult, ParseContext, RouteMatch.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "kind: \"local\"; absolutePath: string; line: number" src/adapters/types.ts` returns ≥1 match.
    - `grep -c "kind: \"external\"; packageName: string" src/adapters/types.ts` is unchanged (1).
    - `pnpm tsc --noEmit` will fail in resolver/index.ts and barrel.ts (expected — fixed in Task 2).
  </acceptance_criteria>
  <done>ResolveResult.local carries `line: number`; other variants untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: resolveModule + barrel emit `line` via parseFile + declLines.get</name>
  <files>src/core/resolver/index.ts, src/core/resolver/barrel.ts</files>
  <read_first>
    - src/core/resolver/index.ts (full file — confirm both local-emission sites at lines 79 and 97)
    - src/core/resolver/barrel.ts (emission site near line 82)
    - src/core/parser/index.ts (the updated parseFile with declLines from plan 08-02)
    - .planning/phases/08-v1-0-polish/08-CONTEXT.md §decisions D-03 (fallback rule)
  </read_first>
  <behavior>
    - Every `{ ok: true, kind: "local", absolutePath, ... }` literal in resolver/index.ts and barrel.ts now also includes a `line` number.
    - For a known import `Foo` resolving to a file where parseFile returns `declLines.get("Foo") === 3`, the result has `line: 3`.
    - For an import name not present in declLines, the result has `line: 1` (D-03 fallback).
    - For a target file that fails to read or fails to parse (`ParseResult.kind === "error"`), the result has `line: 1` (D-03 fallback). resolveModule still does not throw (D-12).
    - The resolver's per-call cache (`ctx.resolverCache`) is keyed by `${fromFile}::${specifier}::${importedName}` — unchanged. Same cache key returns the same `line`.
  </behavior>
  <action>
    In src/core/resolver/index.ts at both local-emission sites (~lines 79 and 97), before constructing the result, compute:
      `const parsed = parseFile(ctx, fwd); const line = parsed.kind === "ok" ? (parsed.declLines.get(importedName) ?? 1) : 1;`
    Then emit `{ ok: true, kind: "local", absolutePath: fwd, line }`. In src/core/resolver/barrel.ts at the local-emission site (~line 82), do the same using `startFile` as the path and `importedName` (already in scope from the chase-barrel signature — verify by reading the function). parseFile is already imported in resolver/index.ts; if barrel.ts does not yet import it, add `import { parseFile } from "../parser/index.js";` (forward-slash-safe ESM import). Do NOT push to `ctx.warnings` on the undefined-name fallback (CONTEXT §code_context: expected behavior, not noise). DO preserve all existing behavior for non-local result emission.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit &amp;&amp; pnpm vitest run test/core</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "declLines.get" src/core/resolver/index.ts` >= 2
    - `grep -c "declLines.get" src/core/resolver/barrel.ts` >= 1
    - Every line in resolver/*.ts matching `kind: "local"` also contains `line` on the same line or adjacent (verified via `grep -A1 'kind: "local"' src/core/resolver/*.ts` showing `line` within 2 lines).
    - `pnpm tsc --noEmit` exits 0 (Analyzer.ts will still emit `line: 1` literal; that is fixed in Task 3 but type-checks fine).
    - `pnpm vitest run test/core` exits 0 (resolver-related tests still pass with the additive `line` field).
  </acceptance_criteria>
  <done>Every local emission in resolver/ carries a true `line` from declLines, falling back to 1 per D-03; cache key unchanged; no throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Analyzer writes result.line; regression fixture + test</name>
  <files>src/core/Analyzer.ts, test/core/analyzer.test.ts, test/adapters/types.test.ts, test/fixtures/phase-05/micro/line-test/app/layout.tsx, test/fixtures/phase-05/micro/line-test/app/page.tsx, test/fixtures/phase-05/micro/line-test/components/Foo.tsx</files>
  <read_first>
    - src/core/Analyzer.ts lines 280-320 (resolveComponentCallsites local-resolution block)
    - test/core/analyzer.test.ts (existing test style; understand how it constructs a ParseContext and adapter)
    - test/adapters/types.test.ts (if `line: 1` literal needs updating for the local ResolveResult)
    - test/fixtures/phase-05/micro/mutation-test/app/page.tsx (template for a minimal micro fixture)
    - .planning/phases/08-v1-0-polish/08-SPEC.md §acceptance criteria
  </read_first>
  <behavior>
    - When `result.ok && result.kind === "local"`, the returned TreeNode has `line: result.line` (not `1`).
    - On a new fixture `test/fixtures/phase-05/micro/line-test/` where `components/Foo.tsx` declares `export function Foo()` on line ≥ 3, running the analyzer against the fixture's entry produces a resolved component TreeNode for `Foo` whose `node.line` equals that exact line and whose `node.file` ends with `components/Foo.tsx`.
    - Existing analyzer test cases still pass; any unit test that previously asserted `line: 1` on a resolved local node is updated to assert the true line (or is migrated to use a `ResolveResult` mock with `line: 1` to preserve original intent).
  </behavior>
  <action>
    In src/core/Analyzer.ts at lines 299-305, replace `line: 1,` with `line: result.line,`. Do NOT change the spread order or any other field. Do NOT touch the unresolved/external branch at line 307+ — D-04 preserves call-site line for those.

    Create regression fixture under `test/fixtures/phase-05/micro/line-test/`:
      - `app/layout.tsx` — minimal Next.js layout: `export default function RootLayout({ children }) { return <html><body>{children}</body></html>; }` (line numbers don't matter here).
      - `app/page.tsx` — imports and uses `Foo`: `import { Foo } from "../components/Foo";\nexport default function Page() { return <Foo />; }`.
      - `components/Foo.tsx` — MUST declare Foo on a line ≥ 3 deterministically. Use two leading comment lines then `export function Foo() { return <div>foo</div>; }` so Foo is declared on line 3.

    Add a test in `test/core/analyzer.test.ts` (or a new sibling file `test/core/analyzer.line-resolution.test.ts` if isolation is preferred — planner's call):
      - Build a NextJsAdapter context against the line-test fixture root.
      - Run the existing analyzer entry function (mirror the patterns already used in analyzer.test.ts; do not reverse-engineer a new pipeline).
      - Locate the resolved `Foo` component TreeNode (file matches `components/Foo.tsx`, kind === "component", name === "Foo").
      - Assert `node.line === 3` (the deterministic declaration line) AND `node.line !== 1`.

    Update test/adapters/types.test.ts: if it constructs a `ResolveResult.local` literal without `line`, add `line: 1` to satisfy the new type. Do NOT update any existing assertion that the placeholder is intentional in mock data.
  </action>
  <verify>
    <automated>pnpm vitest run test/core test/adapters &amp;&amp; pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "line: 1," src/core/Analyzer.ts` returns 0 matches inside the resolved-local block (lines 295-310 specifically — confirm with `sed -n '295,310p' src/core/Analyzer.ts | grep -c 'line: 1'` == 0).
    - `grep -c "line: result.line" src/core/Analyzer.ts` >= 1.
    - The new fixture file `test/fixtures/phase-05/micro/line-test/components/Foo.tsx` exists and has `export function Foo` on line 3 (verifiable via `sed -n '3p' test/fixtures/phase-05/micro/line-test/components/Foo.tsx | grep -c 'function Foo'` == 1).
    - The new analyzer test passes and asserts `node.line === 3` for the resolved Foo TreeNode.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm vitest run test/core test/adapters` exits 0.
  </acceptance_criteria>
  <done>Analyzer.ts emits true declaration line for resolved local components; regression fixture + test prove it programmatically; D-04 contract for external/unresolved variants preserved.</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passes
- `pnpm vitest run test/core test/adapters` passes
- `grep -rn "line: 1" src/core/Analyzer.ts` returns no match inside the local-resolution branch (lines 295-310)
- EnvelopeSchema (src/ir/envelope.ts) is unmodified
</verification>

<success_criteria>
- POLISH-03 acceptance criteria from 08-SPEC.md satisfied: resolved local-kind component nodes report the true declaration line; regression test asserts this on a deterministic fixture where Foo is declared on line 3.
- D-04 preserved: external/cycle/not-found/ambiguous variants are byte-identical.
- No second parse pass introduced — parseFile inside resolveModule rides the existing ctx.astCache.
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-0-polish/08-03-SUMMARY.md`.
</output>
