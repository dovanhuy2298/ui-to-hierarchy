---
phase: 08-v1-0-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderers/markdown.ts
  - test/renderers/markdown.test.ts
autonomous: true
requirements: [POLISH-01]
must_haves:
  truths:
    - "When envelope.warnings is non-empty, markdown output begins with one HTML-comment line per warning"
    - "When envelope.warnings is empty, markdown output is byte-identical to v1.0 (existing snapshots unchanged)"
    - "Warning order in output matches array order"
  artifacts:
    - path: "src/renderers/markdown.ts"
      provides: "renderMarkdown that emits envelope.warnings as <!-- warning: ... --> prefix"
      contains: "envelope.warnings"
    - path: "test/renderers/markdown.test.ts"
      provides: "snapshot or assertion case for non-empty warnings"
  key_links:
    - from: "src/renderers/markdown.ts"
      to: "Envelope.warnings"
      via: "read envelope.warnings before walk()"
      pattern: "envelope\\.warnings"
---

<objective>
POLISH-01: Surface `envelope.warnings` in markdown output so the markdown format reaches parity with JSON. Empty-warnings output stays byte-identical to v1.0.

Purpose: Today `renderMarkdown(tree, _envelope)` discards warnings. JSON consumers see them via the envelope; markdown consumers silently lose them.
Output: Modified renderer + updated/added test asserting the new behavior and preserving existing snapshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-v1-0-polish/08-SPEC.md
@.planning/phases/08-v1-0-polish/08-CONTEXT.md
@src/renderers/markdown.ts
@src/ir/envelope.ts
@test/renderers/markdown.test.ts

<interfaces>
From src/ir/envelope.ts — EnvelopeSchema (MUST remain unchanged; only read warnings field):
  Envelope = { schemaVersion: "1", warnings: string[], tree: TreeNode, ... }

From src/renderers/markdown.ts:107:
  export function renderMarkdown(tree: TreeNode, _envelope: Envelope): string
  — internal `walk(node, prefix, isLast, isRoot, out)` populates `lines`.

Warning block format (locked by SPEC §1, CONTEXT §specifics):
  Per warning, in array order: `<!-- warning: {message} -->`
  Then exactly one blank line
  Then the existing tree output (unchanged).
  When warnings.length === 0: no prefix block, no leading blank line — byte-identical to v1.0.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renderMarkdown emits warning prefix block</name>
  <files>src/renderers/markdown.ts</files>
  <read_first>
    - src/renderers/markdown.ts (full file — only ~111 lines)
    - src/ir/envelope.ts (confirm Envelope.warnings is string[])
    - .planning/phases/08-v1-0-polish/08-SPEC.md §Requirements item 1 (acceptance: first three lines for warnings:["a","b"] are `<!-- warning: a -->`, `<!-- warning: b -->`, ``)
    - .planning/phases/08-v1-0-polish/08-CONTEXT.md §specifics (warning prefix format locked)
  </read_first>
  <behavior>
    - Given envelope.warnings = [], renderMarkdown returns the exact same string as before this change (byte-identical).
    - Given envelope.warnings = ["a", "b"], output lines[0..2] are `<!-- warning: a -->`, `<!-- warning: b -->`, `` (empty string) and lines[3] equals the previous lines[0] (root node line).
    - Warning order in output equals envelope.warnings array order.
  </behavior>
  <action>
    Rename the `_envelope` parameter to `envelope` in renderMarkdown. Before the existing `walk(tree, "", true, true, lines)` call, when `envelope.warnings.length > 0`: push one `<!-- warning: ${msg} -->` entry per warning into `lines` preserving array order, then push a single empty-string entry to produce the blank separator line. Do NOT add any leading/trailing blank when warnings is empty. Keep the existing `lines.join("\n")` return. Do not import new modules, do not alter `walk`/`lineFor`/`labelFor`, do not touch `Envelope` shape.
  </action>
  <verify>
    <automated>pnpm vitest run test/renderers/markdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "envelope.warnings" src/renderers/markdown.ts` >= 1
    - `grep -c "_envelope" src/renderers/markdown.ts` == 0 (parameter renamed)
    - Existing snapshot files under `test/renderers/__snapshots__/markdown-*.md` are unchanged after running vitest (no `--update`).
    - For an envelope with `warnings: ["a","b"]`, the rendered output starts with `<!-- warning: a -->\n<!-- warning: b -->\n\n` followed by the root tree line.
  </acceptance_criteria>
  <done>renderMarkdown emits the warning prefix block per SPEC; existing empty-warnings snapshots remain byte-identical; no Envelope schema change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit test for non-empty warnings prefix</name>
  <files>test/renderers/markdown.test.ts</files>
  <read_first>
    - test/renderers/markdown.test.ts (existing snapshot-driven cases; understand the empty/single-leaf/kitchen-sink fixtures)
    - test/fixtures/ir/index.ts (re-exports of the IR fixtures used by the test)
    - test/fixtures/ir/single-leaf.ts (smallest fixture envelope shape)
  </read_first>
  <behavior>
    - A new test case constructs an Envelope with `warnings: ["a", "b"]` and verifies output starts with `<!-- warning: a -->\n<!-- warning: b -->\n\n` followed by the root line of the tree.
    - A second assertion confirms `warnings: []` produces output starting with the root line (no leading `<!--`, no leading blank).
  </behavior>
  <action>
    Add a new `describe("renderMarkdown — warnings prefix")` block (or it() inside existing describe) in test/renderers/markdown.test.ts. Construct a minimal envelope by spreading `singleLeaf.envelope` (or equivalent existing fixture) and overriding `warnings`. Assert with `expect(out.startsWith("<!-- warning: a -->\n<!-- warning: b -->\n\n"))` and `expect(out).not.toContain("\\")`. Do NOT use file snapshots for the warnings case — use inline string assertions so the test is self-documenting.
  </action>
  <verify>
    <automated>pnpm vitest run test/renderers/markdown.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run test/renderers/markdown.test.ts` exits 0 with ≥1 new test case visible in output.
    - `grep -c "warning: a" test/renderers/markdown.test.ts` >= 1
    - Existing snapshot files unchanged.
  </acceptance_criteria>
  <done>New test asserts non-empty-warnings prefix shape and empty-warnings byte parity; full markdown renderer suite passes.</done>
</task>

</tasks>

<verification>
- `pnpm vitest run test/renderers` passes
- No changes under `src/ir/envelope.ts` (schema frozen)
- `grep -rn "envelope.warnings" src/renderers/markdown.ts` returns ≥1 match
</verification>

<success_criteria>
- POLISH-01 acceptance criteria from 08-SPEC.md satisfied: non-empty warnings produce `<!-- warning: {msg} -->` lines + blank line + tree; empty warnings produce byte-identical v1.0 output.
- All existing markdown snapshot files unchanged.
- Envelope schema (`schemaVersion: "1"`) untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-v1-0-polish/08-01-SUMMARY.md`.
</output>
