---
phase: 12-exporouteradapter-routing-rn-primitives
plan: 04
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - test/adapters/expo/ExpoRouterAdapter.test.ts
  - test/adapters/expo/__snapshots__/expo-basic.md
  - test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md
autonomous: true
requirements: [ROUTE-02, ROUTE-03, ROUTE-04, ROUTE-05, RN-01, RN-02, RN-03]
must_haves:
  truths:
    - "`expo-basic` full-hierarchy markdown snapshot is locked at `test/adapters/expo/__snapshots__/expo-basic.md`; subsequent runs match exactly"
    - "`expo-tabs-and-dynamic` full-hierarchy markdown snapshot is locked at `test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md`; subsequent runs match exactly"
    - "Both snapshots contain `app/_layout.tsx` as a root entry and demonstrate `<Slot/>` injection of child page files"
    - "The `expo-tabs-and-dynamic` snapshot demonstrates: (a) `(tabs)` group folder transparent in URL labeling, (b) `[id]` segment marked as dynamic, (c) `+not-found.tsx` present as special sibling, (d) `<Tabs.Screen>` children enumerated with `name` and `options` attributes"
    - "No emitted path in either snapshot contains a backslash (`\\\\`) — forward-slash invariant preserved"
    - "Full vitest suite is green and total test count strictly exceeds the Plan 12-03 baseline"
  artifacts:
    - path: "test/adapters/expo/__snapshots__/expo-basic.md"
      provides: "Locked baseline tree for the expo-basic fixture"
    - path: "test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md"
      provides: "Locked baseline tree for the expo-tabs-and-dynamic fixture"
  key_links:
    - from: "test/adapters/expo/ExpoRouterAdapter.test.ts"
      to: "test/adapters/expo/__snapshots__/*.md"
      via: "toMatchFileSnapshot(path)"
      pattern: "toMatchFileSnapshot"
---

<objective>
Wave 3 (final): lock the markdown-tree snapshots for both Expo fixtures using vitest's `toMatchFileSnapshot`, and verify the full suite is green and the test count has strictly increased relative to Plan 12-03. This plan does NOT add new behavior — it captures the locked baseline so future regressions in the Analyzer pipeline are caught.

Purpose: SPEC.md Acceptance Criteria require an end-to-end snapshot for both `expo-basic` and `expo-tabs-and-dynamic` fixtures. CONTEXT.md and VALIDATION.md identify snapshot re-lock as the terminal task of the phase. By isolating snapshot lock into its own plan, accidental drift during Wave 2 implementation iterations cannot poison the baseline.

Output: Two locked markdown snapshot files committed under `test/adapters/expo/__snapshots__/`, plus snapshot-asserting tests added to `ExpoRouterAdapter.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-exporouteradapter-routing-rn-primitives/12-SPEC.md
@.planning/phases/12-exporouteradapter-routing-rn-primitives/12-RESEARCH.md
@.planning/phases/12-exporouteradapter-routing-rn-primitives/12-VALIDATION.md
@.planning/phases/12-exporouteradapter-routing-rn-primitives/12-03-SUMMARY.md

@src/adapters/expo/ExpoRouterAdapter.ts
@src/core/Analyzer.ts
@test/adapters/expo/ExpoRouterAdapter.test.ts

@test/fixtures/expo-basic/app/_layout.tsx
@test/fixtures/expo-basic/app/index.tsx
@test/fixtures/expo-tabs-and-dynamic/app/(tabs)/_layout.tsx
@test/fixtures/expo-tabs-and-dynamic/app/(tabs)/[id].tsx
@test/fixtures/expo-tabs-and-dynamic/app/+not-found.tsx

<interfaces>
<!-- The exact rendering pipeline to invoke for snapshots -->
Use the SAME Analyzer / tree-formatter pipeline that existing Next.js snapshot tests use. Search `test/` for prior `toMatchFileSnapshot` usages (e.g., the Next.js fixture snapshots from Phase 4/Phase 10) and mirror the invocation shape verbatim — Analyzer construction, adapter selection (instantiate `ExpoRouterAdapter` directly; do NOT route through `selectAdapter` so the test stays deterministic), full-hierarchy formatter call, then `await expect(markdown).toMatchFileSnapshot(snapshotPath)`.

<!-- Snapshot file naming convention -->
Place snapshot files under `test/adapters/expo/__snapshots__/` with one file per fixture:
- `test/adapters/expo/__snapshots__/expo-basic.md`
- `test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md`

<!-- Forward-slash invariant -->
Snapshot content must contain only forward slashes in paths. After running the snapshot test, the file content MUST NOT contain a backslash character. Assert this as a non-snapshot test in the same file: `expect(readSnapshot(path)).not.toContain("\\\\")` (read the file via `node:fs.readFileSync` after the snapshot lock).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock expo-basic and expo-tabs-and-dynamic markdown snapshots</name>
  <files>test/adapters/expo/ExpoRouterAdapter.test.ts, test/adapters/expo/__snapshots__/expo-basic.md, test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md</files>
  <read_first>
    - test/adapters/expo/ExpoRouterAdapter.test.ts (current GREEN test file from Plan 12-03 — extend, don't replace)
    - test/adapters/next/ for an existing Next.js snapshot test using `toMatchFileSnapshot` — mirror the Analyzer + formatter invocation shape (search via `grep -rn "toMatchFileSnapshot" test/`)
    - src/core/Analyzer.ts (Analyzer construction signature — same as Next.js test usage)
    - src/adapters/expo/ExpoRouterAdapter.ts (instantiate directly with `new ExpoRouterAdapter()`)
    - test/fixtures/expo-basic/ (full fixture tree)
    - test/fixtures/expo-tabs-and-dynamic/ (full fixture tree)
    - .planning/phases/12-exporouteradapter-routing-rn-primitives/12-SPEC.md (Acceptance Criteria — snapshot must demonstrate Slot injection, group transparency, `[id]` dynamic label, `+not-found` special sibling, `<Tabs.Screen>` enumeration)
  </read_first>
  <behavior>
    - Running `npx vitest run test/adapters/expo/ExpoRouterAdapter.test.ts` writes (on first run) or matches (on subsequent runs) the two snapshot files.
    - The locked `expo-basic.md` snapshot contains the tokens: `app/_layout.tsx`, `app/index.tsx`, and either a `slot` or `<Slot/>` marker indicating injection (exact format depends on the existing tree formatter — assert via substring match, not literal equality).
    - The locked `expo-tabs-and-dynamic.md` snapshot contains the tokens: `(tabs)` (group folder name appears in file paths), `[id]` (dynamic segment label rendered), `+not-found.tsx` (special sibling node present), and at least one `Tabs.Screen` reference (enumerated navigation child).
    - Both snapshot files use only forward slashes — `readFileSync(snapshotPath, "utf8")` returns a string with zero backslash characters.
    - The full vitest suite remains green and the total test count strictly exceeds the Plan 12-03 baseline (per VALIDATION.md sampling rule).
  </behavior>
  <action>
    Extend `test/adapters/expo/ExpoRouterAdapter.test.ts` with two additional `describe` blocks (or `it` cases inside an existing "snapshots" block) — do NOT rewrite the file:

    1. "expo-basic full-hierarchy snapshot":
       - Construct the Analyzer with `new ExpoRouterAdapter()` as the adapter (mirror the exact construction pattern used by the existing Next.js snapshot tests under `test/`; find via `grep -rn "toMatchFileSnapshot" test/`).
       - Compute the absolute path to `test/fixtures/expo-basic/` using `path.resolve(process.cwd(), "test/fixtures/expo-basic")` (or `__dirname`-relative if the existing convention uses that — match prior tests).
       - Run the full-hierarchy markdown formatter against that root (same pipeline the MCP `get_full_hierarchy` tool uses internally — invoke whatever helper the existing Next.js tests use, e.g., `buildFullHierarchyMarkdown` or the Analyzer's tree-render method).
       - Await `expect(markdown).toMatchFileSnapshot("./__snapshots__/expo-basic.md")` (path relative to the test file as required by vitest v4).
       - After the snapshot assertion, read the snapshot file via `fs.readFileSync` and assert `expect(content).not.toContain("\\\\")` (no backslashes) AND `expect(content).toMatch(/app\/_layout\.tsx/)` AND `expect(content).toMatch(/app\/index\.tsx/)`.

    2. "expo-tabs-and-dynamic full-hierarchy snapshot":
       - Same pattern, root = `test/fixtures/expo-tabs-and-dynamic/`, snapshot path `./__snapshots__/expo-tabs-and-dynamic.md`.
       - After lock, assert substring matches for: `(tabs)` (group folder path), `[id]` (dynamic segment label), `+not-found.tsx` (special), `Tabs.Screen` (enumeration), and `.not.toContain("\\\\")` (forward-slash invariant).

    First-time execution writes the snapshot files (vitest v4 `toMatchFileSnapshot` semantics). Re-run `npx vitest run test/adapters/expo/ExpoRouterAdapter.test.ts` immediately after to confirm the lock matches (second run must exit 0 with no snapshot write).

    Do NOT modify `src/adapters/expo/ExpoRouterAdapter.ts`, `src/core/Analyzer.ts`, or any source file in this plan. If a snapshot reveals a bug, STOP and surface the discrepancy in the SUMMARY rather than papering over with a snapshot update — Wave 2 owns implementation fixes.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - File `test/adapters/expo/__snapshots__/expo-basic.md` exists and contains substrings `app/_layout.tsx` AND `app/index.tsx`.
    - File `test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md` exists and contains substrings `(tabs)`, `[id]`, `+not-found.tsx`, AND `Tabs.Screen`.
    - Neither snapshot file contains a backslash: `grep -c '\\\\' test/adapters/expo/__snapshots__/expo-basic.md` returns 0 (note: grep counts comments — read the file directly to confirm, since markdown headers may contain `#` but never `\\`).
    - Running `npx vitest run test/adapters/expo/ExpoRouterAdapter.test.ts` TWICE in a row both exit 0 (idempotent snapshot match).
    - `npx vitest run` exits 0 with total test count strictly greater than Plan 12-03's baseline.
    - The test file does NOT modify any `src/**` file (use only `test/` directory paths in writes).
  </acceptance_criteria>
  <done>Both fixtures have locked markdown snapshots; tests are idempotent; full suite green; test count increased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture filesystem → Analyzer pipeline | Pure read-only static analysis; identical boundary to all prior phases' snapshot tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | Locked snapshot files | accept | Snapshots are baseline artifacts under version control; any unintended drift surfaces as a failing test in CI before merge |
| T-12-SC | Tampering | npm installs | mitigate | Zero new dependencies in this plan |
</threat_model>

<verification>
1. `npx vitest run` exits 0 with full suite green.
2. Both snapshot files exist under `test/adapters/expo/__snapshots__/` with required substrings.
3. Re-running the snapshot test exits 0 with no write (idempotent lock).
4. Test count strictly exceeds the Plan 12-03 baseline (recorded in 12-03-SUMMARY.md).
</verification>

<success_criteria>
- Locked snapshots exist for both Expo fixtures.
- Snapshot tests are idempotent (two consecutive runs both green).
- Full vitest suite remains green; total test count strictly greater than Plan 12-03 baseline.
- Forward-slash invariant verified on both snapshot files.
- No source code under `src/**` was modified in this plan.
</success_criteria>

<output>
Create `.planning/phases/12-exporouteradapter-routing-rn-primitives/12-04-SUMMARY.md` listing: (a) the two locked snapshot file paths and their sizes, (b) the substrings asserted on each snapshot, (c) full-suite test counts (before Plan 12-01 baseline, after each of Plan 12-01..12-04), (d) confirmation that no source file was modified in Wave 3.
</output>
