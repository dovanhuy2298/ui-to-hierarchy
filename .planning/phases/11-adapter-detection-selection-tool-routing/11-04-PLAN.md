---
phase: 11-adapter-detection-selection-tool-routing
plan: 04
type: execute
wave: 2
depends_on:
  - 11-02
  - 11-03
files_modified:
  - src/adapters/select.ts
  - src/init/argv.ts
  - src/cli.ts
autonomous: true
requirements:
  - ADAPT-03
  - ADAPT-04
  - ADAPT-05

must_haves:
  truths:
    - "selectAdapter(root) runs detectNextJs and detectExpoRouter in parallel via Promise.all"
    - "selectAdapter returns NextJsAdapter when only Next.js detected"
    - "selectAdapter returns new ExpoRouterAdapter() when only Expo Router detected"
    - "selectAdapter returns { isError: true } with named signal paths on conflict"
    - "selectAdapter returns { isError: true } with --framework hint on zero-match"
    - "setFrameworkOverride(value) module singleton skips probes when set"
    - "--framework nextjs|expo-router CLI flag validated before server starts"
    - "Invalid --framework exits with code 1 before spawning server"
  artifacts:
    - path: "src/adapters/select.ts"
      provides: "selectAdapter + setFrameworkOverride exports"
      exports: ["selectAdapter", "setFrameworkOverride"]
    - path: "src/init/argv.ts"
      provides: "framework added to INIT_OPTION_SCHEMA"
      contains: "framework"
    - path: "src/cli.ts"
      provides: "--framework validation + setFrameworkOverride call in server mode"
      contains: "setFrameworkOverride"
  key_links:
    - from: "src/adapters/select.ts"
      to: "src/adapters/next/detect.ts"
      via: "import { detectNextJs }"
      pattern: "detectNextJs"
    - from: "src/adapters/select.ts"
      to: "src/adapters/expo/detect.ts"
      via: "import { detectExpoRouter }"
      pattern: "detectExpoRouter"
    - from: "src/cli.ts"
      to: "src/adapters/select.ts"
      via: "import { setFrameworkOverride } — calls after parseArgs"
      pattern: "setFrameworkOverride"
    - from: "src/init/argv.ts"
      to: "src/cli.ts"
      via: "INIT_OPTION_SCHEMA shared — framework field captured by loose pre-parse"
      pattern: "framework"
---

<objective>
Create src/adapters/select.ts with selectAdapter and setFrameworkOverride. Add --framework flag to INIT_OPTION_SCHEMA and wire validation + setFrameworkOverride call in cli.ts. After this plan, test/adapters/select.test.ts turns GREEN for all selectAdapter cases (Wave 2 select tests from Plan 01 stubs).

Purpose: selectAdapter is the integration point that routes tool handlers to the correct adapter. CLI flag gives users an escape hatch for CI/monorepo disambiguation.

Output: src/adapters/select.ts (new), src/init/argv.ts (framework field added), src/cli.ts (framework validation + setFrameworkOverride wired).
</objective>

<execution_context>
@E:/ui-to-hierarch/.claude/get-shit-done/workflows/execute-plan.md
@E:/ui-to-hierarch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/ui-to-hierarch/.planning/PROJECT.md
@E:/ui-to-hierarch/.planning/ROADMAP.md
@E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-SPEC.md
@E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-CONTEXT.md

<interfaces>
From src/mcp/errors.ts (ToolResponse type):
  type ToolResponse = CallToolResult  (from @modelcontextprotocol/sdk/types.js)
  ToolResponse shape on error: { isError: true, content: [{ type: "text", text: string }] }

From src/adapters/FrameworkAdapter.ts:
  interface FrameworkAdapter (8 methods)

From src/adapters/next/NextJsAdapter.ts:
  export const NextJsAdapter: FrameworkAdapter  (object literal — note: not a class)

From src/adapters/expo/ExpoRouterAdapter.ts (created in Plan 02):
  export class ExpoRouterAdapter implements FrameworkAdapter

From src/adapters/next/detect.ts (after Plan 03):
  export async function detectNextJs(absRoot: string): Promise<{ detected: boolean; signals: string[] }>

From src/adapters/expo/detect.ts (created in Plan 02):
  export async function detectExpoRouter(absRoot: string): Promise<{ detected: boolean; signals: string[] }>

From src/init/argv.ts (current INIT_OPTION_SCHEMA — read before editing):
  export const INIT_OPTION_SCHEMA = {
    init: { type: "boolean" as const },
    global: { type: "boolean" as const },
    target: { type: "string" as const },
    "dry-run": { type: "boolean" as const },
    force: { type: "boolean" as const },
    help: { type: "boolean" as const, short: "h" as const },
    version: { type: "boolean" as const, short: "v" as const },
  }

From src/cli.ts (current dispatch structure — read before editing):
  Loose pre-parse uses INIT_OPTION_SCHEMA with strict: false
  if (meta.init) { ... } else { startServer().catch(...) }
  Framework validation goes in the else branch, BEFORE startServer()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/adapters/select.ts with selectAdapter + setFrameworkOverride</name>
  <files>src/adapters/select.ts</files>
  <read_first>
    - E:/ui-to-hierarch/src/mcp/errors.ts (ToolResponse type — needed as return type for error cases)
    - E:/ui-to-hierarch/src/adapters/FrameworkAdapter.ts (FrameworkAdapter interface — return type on success)
    - E:/ui-to-hierarch/src/adapters/next/NextJsAdapter.ts lines 1-20 (confirm it is an object literal, not a class — affects return statement)
    - E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-RESEARCH.md (Pattern 2 — selectAdapter with module singleton; exact conflict and zero-match error text format)
    - E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-CONTEXT.md (D-01 return type; D-02 isError check; D-03 singleton; D-04 validation location)
    - E:/ui-to-hierarch/test/adapters/select.test.ts (test stubs from Plan 01 — verify exact assertions the implementation must satisfy)
  </read_first>
  <behavior>
    - Module-level: let _frameworkOverride: string | undefined = undefined
    - setFrameworkOverride(v: string): void sets _frameworkOverride = v
    - selectAdapter(projectRoot: string, override: string | undefined = _frameworkOverride): Promise<FrameworkAdapter | ToolResponse>
    - Override "nextjs": return NextJsAdapter (no probes)
    - Override "expo-router": return new ExpoRouterAdapter() (no probes)
    - No override: run Promise.all([detectNextJs(projectRoot), detectExpoRouter(projectRoot)])
    - Both detected true (conflict): return { isError: true, content: [{ type: "text", text: "Detected Next.js ([next signals]) AND Expo Router ([expo signals]). Use --framework to disambiguate." }] }
    - Both detected false (zero-match): return { isError: true, content: [{ type: "text", text: "No framework detected at [projectRoot]. Use --framework nextjs|expo-router to specify." }] }
    - Only nextResult.detected: return NextJsAdapter
    - Only expoResult.detected: return new ExpoRouterAdapter()
    - Error text for conflict names the actual signal paths (e.g., signals.join(", "))
  </behavior>
  <action>
    Create src/adapters/select.ts (new file) with the following structure:

    Imports:
    - import type { FrameworkAdapter } from "./FrameworkAdapter.js"
    - import type { ToolResponse } from "../mcp/errors.js"
    - import { NextJsAdapter } from "./next/NextJsAdapter.js"
    - import { ExpoRouterAdapter } from "./expo/ExpoRouterAdapter.js"
    - import { detectNextJs } from "./next/detect.js"
    - import { detectExpoRouter } from "./expo/detect.js"

    Module singleton (D-03):
    - let _frameworkOverride: string | undefined
    - export function setFrameworkOverride(v: string): void { _frameworkOverride = v }

    selectAdapter function (D-01 return type):
    - Signature: export async function selectAdapter(projectRoot: string, override: string | undefined = _frameworkOverride): Promise<FrameworkAdapter | ToolResponse>
    - Override checks first (skips all fs I/O per D-03 RESEARCH anti-pattern note)
    - Promise.all for parallel probes (ADAPT-03 constraint — must be concurrent, not sequential)
    - Conflict error text: "Detected Next.js (${nextResult.signals.join(", ")}) AND Expo Router (${expoResult.signals.join(", ")}). Use --framework to disambiguate."
    - Zero-match error text: "No framework detected at ${projectRoot}. Use --framework nextjs|expo-router to specify."

    File header comment: reference Phase 11 SPEC requirements 3 and 4; note island rule (select.ts is in adapters layer, not core).
  </action>
  <verify>
    <automated>rtk vitest run test/adapters/select.test.ts</automated>
  </verify>
  <done>
    rtk vitest run test/adapters/select.test.ts — all 8 test cases GREEN; selectAdapter(expo-basic-path) returns ExpoRouterAdapter instance; selectAdapter(next-app-router-path) returns NextJsAdapter; zero-match returns isError:true with "Use --framework"; override skips probes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --framework flag to argv schema and wire in cli.ts</name>
  <files>
    src/init/argv.ts,
    src/cli.ts
  </files>
  <read_first>
    - E:/ui-to-hierarch/src/init/argv.ts (current INIT_OPTION_SCHEMA — read entire file before editing; understand strict mode note)
    - E:/ui-to-hierarch/src/cli.ts (current dispatch structure — read entire file before editing; else branch location)
    - E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-RESEARCH.md (Pattern 4 — CLI flag addition; Pitfall 2 — INIT_OPTION_SCHEMA vs parseInitArgs strict mode interaction)
    - E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-CONTEXT.md (D-03 singleton threading; D-04 validation in else branch before startServer)
  </read_first>
  <behavior>
    - src/init/argv.ts: INIT_OPTION_SCHEMA gains framework: { type: "string" as const } as a new field (per D-04/Pitfall 2 — must be in shared schema for loose pre-parse to capture it in server mode)
    - src/cli.ts: HELP_TEXT updated to document --framework flag with valid values nextjs|expo-router
    - src/cli.ts: in the else branch (server mode), BEFORE startServer(): check meta.framework value; if defined, validate against ["nextjs", "expo-router"] allowlist; if invalid → process.stderr.write error + process.exit(1); if valid → call setFrameworkOverride(meta.framework as string)
    - Invalid --framework value: stderr message format "[framework] error: unknown value \"${val}\". Valid values: nextjs, expo-router" + process.exit(1)
    - Valid --framework value: setFrameworkOverride is called, then startServer() runs normally
    - --framework is a server-mode flag only — validation does NOT run in the if (meta.init) branch
    - parseInitArgs strict mode: framework field in INIT_OPTION_SCHEMA means strict parseInitArgs will accept --framework when --init is used (not an error); the framework value is ignored in init mode (no validation needed there)
  </behavior>
  <action>
    Edit src/init/argv.ts:
    - Add framework: { type: "string" as const } to INIT_OPTION_SCHEMA (alongside the existing string field "target")
    - No other changes to this file — do not modify parseInitArgs, InitFlags, or any other exports

    Edit src/cli.ts:
    - Add "import { setFrameworkOverride } from "./adapters/select.js"" to the import section
    - Update HELP_TEXT to include "--framework nextjs|expo-router  Force adapter selection (skips auto-detect)." under the server usage section (before or after the --help line)
    - In the else branch, BEFORE the startServer() call, add framework validation block:
      const frameworkVal = meta.framework as string | undefined;
      const VALID_FRAMEWORKS = ["nextjs", "expo-router"] as const;
      if (frameworkVal !== undefined) {
        if (!(VALID_FRAMEWORKS as readonly string[]).includes(frameworkVal)) {
          process.stderr.write("[framework] error: unknown value \"" + frameworkVal + "\". Valid values: nextjs, expo-router\n");
          process.exit(1);
        }
        setFrameworkOverride(frameworkVal);
      }
    - Then startServer().catch(...) remains unchanged

    Do NOT add --framework validation inside the if (meta.init) branch — the flag is server-mode only.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit 2>&1 | grep -E "argv|cli" | head -20</automated>
  </verify>
  <done>
    src/init/argv.ts INIT_OPTION_SCHEMA contains framework field; src/cli.ts imports setFrameworkOverride and calls it in server mode; HELP_TEXT mentions --framework; TypeScript compiles clean; running "node dist/cli.js --framework invalid" exits code 1 (verifiable after build); full vitest suite still ≥371 passing
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| --framework argv → selectAdapter | User-controlled string must be validated before forwarding to module singleton |
| selectAdapter → filesystem | Reads package.json at project root; malformed content must not crash tool handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | --framework argv value | mitigate | Allowlist ["nextjs", "expo-router"] + process.exit(1) before server start (D-04, ADAPT-05) |
| T-11-06 | Information Disclosure | conflict error message naming signal paths | accept | Paths are local filesystem paths already visible to the user running the CLI |
| T-11-07 | DoS | Promise.all both probes timeout | accept | Both probes use fs.access only — bounded by OS; no network calls |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
After Wave 2 Plan 04 completes:
- rtk vitest run test/adapters/select.test.ts — all 8 tests GREEN
- rtk npx tsc --noEmit — no TypeScript errors across all modified files
- grep "setFrameworkOverride" src/cli.ts — returns 1+ result
- grep "framework" src/init/argv.ts — returns 1+ result in INIT_OPTION_SCHEMA
- rtk vitest run — full suite ≥371 passing (select.test.ts tests add to total)
</verification>

<success_criteria>
- src/adapters/select.ts: exports selectAdapter + setFrameworkOverride; Promise.all parallel probes; conflict error names both signal paths; zero-match error contains "Use --framework"; override skips probes
- src/init/argv.ts: INIT_OPTION_SCHEMA has framework: { type: "string" as const }
- src/cli.ts: imports setFrameworkOverride; validates --framework in else branch before startServer(); HELP_TEXT documents --framework; invalid value exits code 1
- test/adapters/select.test.ts: all 8 tests GREEN
- Full vitest suite: ≥371 tests passing
</success_criteria>

<output>
Create `.planning/phases/11-adapter-detection-selection-tool-routing/11-04-SUMMARY.md` when done
</output>
