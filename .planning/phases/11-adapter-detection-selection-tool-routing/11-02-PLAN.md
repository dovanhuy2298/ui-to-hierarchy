---
phase: 11-adapter-detection-selection-tool-routing
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - src/adapters/expo/ExpoRouterAdapter.ts
  - src/adapters/expo/detect.ts
autonomous: true
requirements:
  - ADAPT-03

must_haves:
  truths:
    - "ExpoRouterAdapter class implements all 8 FrameworkAdapter methods with no TypeScript compile errors"
    - "detectExpoRouter returns { detected: true } for expo-basic fixture"
    - "detectExpoRouter returns { detected: false } for next-app-router fixture"
    - "detectExpoRouter returns { detected: false } when only one signal matches"
    - "signals[] is always an array even when detected is false"
  artifacts:
    - path: "src/adapters/expo/ExpoRouterAdapter.ts"
      provides: "ExpoRouterAdapter class implementing FrameworkAdapter"
      exports: ["ExpoRouterAdapter"]
    - path: "src/adapters/expo/detect.ts"
      provides: "detectExpoRouter two-signal probe"
      exports: ["detectExpoRouter"]
  key_links:
    - from: "src/adapters/expo/ExpoRouterAdapter.ts"
      to: "src/adapters/FrameworkAdapter.ts"
      via: "implements FrameworkAdapter"
      pattern: "implements FrameworkAdapter"
    - from: "src/adapters/expo/ExpoRouterAdapter.ts"
      to: "src/core/resolver/index.ts"
      via: "import { resolveModule as coreResolveModule }"
      pattern: "coreResolveModule"
    - from: "test/adapters/expo/detect.test.ts"
      to: "src/adapters/expo/detect.ts"
      via: "import detectExpoRouter — now resolves (RED → GREEN)"
---

<objective>
Create the two new source files in src/adapters/expo/: the ExpoRouterAdapter stub class (satisfies FrameworkAdapter at compile time, all 8 methods return empty/stub results) and the detectExpoRouter two-signal probe. After this plan, the detect test stubs from Plan 01 turn GREEN for detectExpoRouter cases.

Purpose: ExpoRouterAdapter must exist for Wave 2 (selectAdapter imports it) and tests must pass for detection logic before wiring the full selection layer.

Output: src/adapters/expo/ExpoRouterAdapter.ts (class, 8 methods), src/adapters/expo/detect.ts (detectExpoRouter function).
</objective>

<execution_context>
@E:/ui-to-hierarch/.claude/get-shit-done/workflows/execute-plan.md
@E:/ui-to-hierarch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/ui-to-hierarch/.planning/PROJECT.md
@E:/ui-to-hierarch/.planning/ROADMAP.md
@E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-SPEC.md
@E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-CONTEXT.md

<interfaces>
From src/adapters/FrameworkAdapter.ts (8-method interface — ExpoRouterAdapter must implement all):
  detect(absRoot: string): Promise<boolean> | boolean
  discoverEntries(absRoot: string): Promise<string[]> | string[]
  resolveModule(ctx: ParseContext, fromFile: string, specifier: string, importedName: string): ResolveResult
  extractComponents(ctx: ParseContext, entryFiles: string[], opts?: { fullClasses?: boolean }): ComponentDefinition[]
  mapRouteToEntry(absRoot: string, route: string): Promise<RouteMatch> | RouteMatch
  classifyEntry(absPath: string): "page" | "layout" | "special" | "other"
  enumerateRoutes(absRoot: string): string[] | Promise<string[]>
  slotMarker(name: string, importSource: string): boolean

From src/adapters/next/detect.ts (existing detect() — model detectExpoRouter on its exists() helper pattern):
  uses: import { access } from "node:fs/promises"; import { join } from "node:path";
  helper: async function exists(p: string): Promise<boolean> { try { await access(p); return true; } catch { return false; } }

From src/adapters/types.ts (types consumed by FrameworkAdapter):
  ComponentDefinition, ParseContext, ResolveResult, RouteMatch

From src/core/resolver/index.ts (for ExpoRouterAdapter.resolveModule delegation — D-08):
  export function resolveModule(ctx: ParseContext, fromFile: string, specifier: string, importedName: string): ResolveResult
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ExpoRouterAdapter stub class (all 8 FrameworkAdapter methods)</name>
  <files>src/adapters/expo/ExpoRouterAdapter.ts</files>
  <read_first>
    - E:/ui-to-hierarch/src/adapters/FrameworkAdapter.ts (8-method interface — exact signatures to implement)
    - E:/ui-to-hierarch/src/adapters/types.ts (ComponentDefinition, ParseContext, ResolveResult, RouteMatch types)
    - E:/ui-to-hierarch/src/adapters/next/NextJsAdapter.ts lines 1-45 (import pattern — resolveModule delegation to core resolver)
    - E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-RESEARCH.md (Pattern 5 — ExpoRouterAdapter stub shape; D-07, D-08)
    - E:/ui-to-hierarch/test/architecture/island.test.ts (island rule — adapters can import from core; verify direction is legal)
  </read_first>
  <behavior>
    - ExpoRouterAdapter implements FrameworkAdapter interface (TypeScript compile check)
    - detect(absRoot) returns false (Phase 12 wires detectExpoRouter here)
    - discoverEntries(_absRoot) returns []
    - resolveModule(ctx, fromFile, specifier, importedName) delegates to coreResolveModule(ctx, fromFile, specifier, importedName) — per D-08
    - extractComponents(_ctx, _entryFiles, _opts?) returns []
    - mapRouteToEntry(_absRoot, _route) returns { matched: false }
    - classifyEntry(_absPath) returns "other"
    - enumerateRoutes(_absRoot) returns []
    - slotMarker(name, importSource) returns name === "Slot" && importSource === "expo-router"
    - FrameworkAdapter locking test (test/adapters/FrameworkAdapter.test.ts) stays green — the test checks method count and structural shape, not the adapter class itself
  </behavior>
  <action>
    Create src/adapters/expo/ExpoRouterAdapter.ts as a class (not object literal — required for instanceof checks in tests per Pitfall 5 in RESEARCH.md):

    Import section:
    - import type { FrameworkAdapter } from "../FrameworkAdapter.js"
    - import type { ComponentDefinition, ParseContext, ResolveResult, RouteMatch } from "../types.js"
    - import { resolveModule as coreResolveModule } from "../../core/resolver/index.js"

    Class: export class ExpoRouterAdapter implements FrameworkAdapter

    Method implementations (per D-07 stub contract):
    - detect: async method returning false
    - discoverEntries: async method returning []
    - resolveModule: sync method delegating to coreResolveModule(ctx, fromFile, specifier, importedName) and returning the result
    - extractComponents: sync method returning []
    - mapRouteToEntry: async method returning { matched: false }
    - classifyEntry: sync method returning "other"
    - enumerateRoutes: async method returning []
    - slotMarker: sync method returning name === "Slot" && importSource === "expo-router"

    File header comment: reference Phase 11 SPEC requirement 1 and note that Phase 12 fills real parsing logic.

    Do NOT create discover.ts, route-map.ts, or segments.ts — those are Phase 12 per D-07.
  </action>
  <verify>
    <automated>rtk npx tsc --noEmit --project tsconfig.json 2>&1 | grep -i "expo" | head -20</automated>
  </verify>
  <done>
    src/adapters/expo/ExpoRouterAdapter.ts compiles without TypeScript errors; exports ExpoRouterAdapter class; implements all 8 FrameworkAdapter methods; slotMarker("Slot", "expo-router") returns true; slotMarker("children", "react") returns false
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create detectExpoRouter two-signal probe</name>
  <files>src/adapters/expo/detect.ts</files>
  <read_first>
    - E:/ui-to-hierarch/src/adapters/next/detect.ts (existing detect() — copy exists() helper, adapt signal pattern)
    - E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-RESEARCH.md (Pattern 1 — detectExpoRouter symmetric shape; D-05, D-06 decisions)
    - E:/ui-to-hierarch/test/fixtures/expo-basic/ (verify package.json now exists after Plan 01 Task 1)
    - E:/ui-to-hierarch/test/fixtures/next-app-router/ (verify package.json exists after Plan 01 Task 1)
    - E:/ui-to-hierarch/test/adapters/expo/detect.test.ts (test stubs created in Plan 01 — implement against these test contracts)
  </read_first>
  <behavior>
    - detectExpoRouter(absRoot) returns Promise<{ detected: boolean; signals: string[] }>
    - Signal 1: "expo-router" key in dependencies OR devDependencies of absRoot/package.json; if found, push "package.json#expo-router" to signals[]
    - Signal 2: absRoot/app/_layout.tsx OR absRoot/src/app/_layout.tsx exists; if found, push the matched path (e.g., "app/_layout.tsx") to signals[]
    - detected is true only when BOTH signals are found (signals.length === 2)
    - Missing or malformed package.json is caught in try/catch — no signal emitted, no throw
    - signals[] is always an array (never undefined) — contains partial matches when detected is false (D-06)
    - detectExpoRouter(expo-basic-fixture-path) returns { detected: true, signals: ["package.json#expo-router", "app/_layout.tsx"] }
    - detectExpoRouter(next-app-router-fixture-path) returns { detected: false, signals: [] } (no expo-router dep, no _layout.tsx)
  </behavior>
  <action>
    Create src/adapters/expo/detect.ts modeled on the pattern from src/adapters/next/detect.ts:

    Imports:
    - import { access, readFile } from "node:fs/promises"
    - import { join } from "node:path"

    Copy the private exists(p) helper from next/detect.ts (same implementation).

    Function: export async function detectExpoRouter(absRoot: string): Promise<{ detected: boolean; signals: string[] }>

    Signal 1 (package.json deps check):
    - try { readFile(join(absRoot, "package.json"), "utf8") → JSON.parse → check "expo-router" in { ...pkg.dependencies, ...pkg.devDependencies } → if found, signals.push("package.json#expo-router") }
    - catch { /* missing or malformed — no signal */ }

    Signal 2 (layout file check):
    - for each candidate of ["app/_layout.tsx", "src/app/_layout.tsx"]: if exists(join(absRoot, candidate)) → signals.push(candidate); break

    Return: { detected: signals.length === 2, signals }

    File header comment: reference Phase 11 SPEC requirement 2; note symmetric shape with detectNextJs.
  </action>
  <verify>
    <automated>rtk vitest run test/adapters/expo/detect.test.ts</automated>
  </verify>
  <done>
    npx vitest run test/adapters/expo/detect.test.ts passes; detectExpoRouter(expo-basic-path) returns { detected: true }; detectExpoRouter(next-app-router-path) returns { detected: false }; all 4 test cases in detect.test.ts are GREEN
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detectExpoRouter → filesystem | Reads arbitrary package.json at project root; malformed JSON must not propagate as throw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | package.json JSON.parse in detectExpoRouter | mitigate | try/catch around JSON.parse; no signal emitted on malformed input |
| T-11-03 | DoS | fs.access loop for layout candidates | accept | Fixed 2-candidate loop, no unbounded glob |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
After Wave 1 Plan 02 completes:
- rtk npx tsc --noEmit — no TypeScript errors for src/adapters/expo/ExpoRouterAdapter.ts or src/adapters/expo/detect.ts
- rtk vitest run test/adapters/expo/detect.test.ts — all 4 tests GREEN
- grep -r "NextJsAdapter" src/adapters/expo/ — returns zero results
- rtk vitest run — ≥371 existing tests still pass (detect tests add to total)
</verification>

<success_criteria>
- src/adapters/expo/ExpoRouterAdapter.ts: exports ExpoRouterAdapter class; implements all 8 FrameworkAdapter methods; TypeScript compiles clean
- src/adapters/expo/detect.ts: exports detectExpoRouter; returns { detected: true } for expo-basic; returns { detected: false } for next-app-router; signals[] always an array
- test/adapters/expo/detect.test.ts: all tests GREEN after Wave 1
- Full vitest suite: ≥371 tests passing (detect.test.ts adds new passing tests)
</success_criteria>

<output>
Create `.planning/phases/11-adapter-detection-selection-tool-routing/11-02-SUMMARY.md` when done
</output>
