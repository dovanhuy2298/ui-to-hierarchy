---
phase: 11-adapter-detection-selection-tool-routing
plan: 03
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - src/adapters/next/detect.ts
autonomous: true
requirements:
  - ADAPT-03

must_haves:
  truths:
    - "detectNextJs exported from src/adapters/next/detect.ts alongside existing detect()"
    - "detectNextJs returns { detected: true } for next-app-router fixture (has next in deps AND next.config.mjs)"
    - "detectNextJs returns { detected: false } for expo-basic fixture"
    - "detectNextJs signals[] is always an array with matched paths even when detected is false"
    - "Original detect() function is unchanged — no backward-compat breakage"
  artifacts:
    - path: "src/adapters/next/detect.ts"
      provides: "detectNextJs export alongside existing detect()"
      exports: ["detect", "detectNextJs"]
  key_links:
    - from: "src/adapters/select.ts"
      to: "src/adapters/next/detect.ts"
      via: "import { detectNextJs } — consumed by Wave 2 selectAdapter"
      pattern: "detectNextJs"
---

<objective>
Add detectNextJs(absRoot) export to the existing src/adapters/next/detect.ts file. This is a sibling export alongside the existing detect() function — detect() is unchanged. detectNextJs follows the symmetric { detected: boolean; signals: string[] } return shape so selectAdapter can process both probes uniformly.

Purpose: selectAdapter (Wave 2) needs detectNextJs to run the Next.js probe in parallel with detectExpoRouter.

Output: src/adapters/next/detect.ts with detectNextJs added; existing detect() unchanged; all existing next detect tests still pass.
</objective>

<execution_context>
@E:/ui-to-hierarch/.claude/get-shit-done/workflows/execute-plan.md
@E:/ui-to-hierarch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/ui-to-hierarch/.planning/PROJECT.md
@E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-SPEC.md
@E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-CONTEXT.md

<interfaces>
From src/adapters/next/detect.ts (current state — read before editing):
  import { access } from "node:fs/promises"
  import { join } from "node:path"
  NEXT_CONFIGS = ["next.config.js", "next.config.mjs", "next.config.cjs", "next.config.ts"]
  export async function detect(absRoot: string): Promise<boolean>
  private async function exists(p: string): Promise<boolean>

Target after edit:
  export async function detect(absRoot: string): Promise<boolean>   ← unchanged
  export async function detectNextJs(absRoot: string): Promise<{ detected: boolean; signals: string[] }>  ← new
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add detectNextJs export to src/adapters/next/detect.ts</name>
  <files>src/adapters/next/detect.ts</files>
  <read_first>
    - E:/ui-to-hierarch/src/adapters/next/detect.ts (current implementation — read the entire file before editing; detect() must remain byte-identical)
    - E:/ui-to-hierarch/.planning/phases/11-adapter-detection-selection-tool-routing/11-RESEARCH.md (Pattern 1 — detectNextJs code example; D-05, D-06 decisions)
    - E:/ui-to-hierarch/test/fixtures/next-app-router/ (verify next.config.mjs exists — this is Signal 2 for detectNextJs)
    - E:/ui-to-hierarch/test/fixtures/next-app-router/package.json (verify "next" in dependencies — created by Plan 01)
  </read_first>
  <behavior>
    - detectNextJs(absRoot) returns Promise<{ detected: boolean; signals: string[] }>
    - Signal 1: "next" key in dependencies OR devDependencies of absRoot/package.json; push "package.json#next" to signals when found
    - Signal 2: any next.config.{js,mjs,cjs,ts} file exists at absRoot; push the matched filename to signals when found (short-circuit on first match)
    - detected is true only when BOTH signals are found (signals.length === 2)
    - Missing or malformed package.json: try/catch — no signal, no throw
    - signals[] is always an array; partial matches are included even when detected is false (D-06)
    - detectNextJs(next-app-router-fixture-path) returns { detected: true, signals: ["package.json#next", "next.config.mjs"] }
    - detectNextJs(expo-basic-fixture-path) returns { detected: false } (no next dep, no next.config.*)
    - Original detect() function: not modified — same lines, same logic, same signature — no backward compat impact
  </behavior>
  <action>
    Read src/adapters/next/detect.ts first to see current content.

    Add detectNextJs as a new exported function AFTER the existing detect() function. Do NOT modify the detect() function or the exists() helper. Also add readFile to the existing import from "node:fs/promises".

    detectNextJs implementation:
    - signals: string[] = []
    - Signal 1 try/catch block: readFile(join(absRoot, "package.json"), "utf8") → JSON.parse → spread dependencies and devDependencies → check "next" in merged object → if found, signals.push("package.json#next")
    - Signal 2 loop: iterate NEXT_CONFIGS array; for each name, if await exists(join(absRoot, name)) → signals.push(name); break
    - return { detected: signals.length === 2, signals }

    NEXT_CONFIGS constant is already defined in the file — reuse it. Do not duplicate it.

    Important: The exists() function is private (not exported) — detectNextJs can call it directly because it lives in the same file.

    After edit, the file exports: detect (existing) and detectNextJs (new). No other exports added.
  </action>
  <verify>
    <automated>rtk vitest run test/adapters/next/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    src/adapters/next/detect.ts exports both detect and detectNextJs; all pre-existing tests in test/adapters/next/ still pass; detectNextJs returns { detected: true } when both signals match; TypeScript compiles clean (tsc --noEmit shows no errors for this file)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detectNextJs → filesystem | Reads project root package.json; malformed JSON must not propagate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | package.json JSON.parse in detectNextJs | mitigate | try/catch block — same pattern as detectExpoRouter; no signal emitted on malformed input |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
After Wave 1 Plan 03 completes:
- rtk vitest run test/adapters/next/ — all pre-existing next detect tests still pass
- rtk npx tsc --noEmit — no new TypeScript errors in src/adapters/next/detect.ts
- node -e "import('./src/adapters/next/detect.js').then(m => console.log(Object.keys(m)))" — outputs both 'detect' and 'detectNextJs'
- rtk vitest run — full suite still ≥371 passing
</verification>

<success_criteria>
- src/adapters/next/detect.ts: exports detect (unchanged) AND detectNextJs (new)
- detectNextJs signature: (absRoot: string) => Promise<{ detected: boolean; signals: string[] }>
- detectNextJs signals include matched paths per D-06
- All pre-existing next/detect tests remain GREEN
- Full vitest suite: ≥371 tests passing (no regressions)
</success_criteria>

<output>
Create `.planning/phases/11-adapter-detection-selection-tool-routing/11-03-SUMMARY.md` when done
</output>
