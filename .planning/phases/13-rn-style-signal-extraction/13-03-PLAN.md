---
phase: 13-rn-style-signal-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/adapters/expo/ExpoRouterAdapter.ts
  - test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx
  - test/adapters/expo/__snapshots__/expo-basic.md
  - test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md
autonomous: true
requirements:
  - RN-04
  - RN-05
  - RN-06
  - RN-07
  - RN-08
tags:
  - phase-13
  - wave-2
  - rn-styles
  - adapter-wiring
  - integration

must_haves:
  truths:
    - "find_by_style('container') on expo-basic returns the <View style={styles.container}> node with correct file:line"
    - "find_by_style('card') on expo-tabs-and-dynamic returns the <View style={[styles.card, ...]}> node"
    - "find_by_style('fontWeight') on expo-tabs-and-dynamic returns the <Text style={{ fontWeight: 'bold' }}> node"
    - "find_by_style('bold') on expo-tabs-and-dynamic returns the View with conditional && styles.bold member"
    - "find_by_style('p-4') on expo-tabs-and-dynamic returns the node with className='ios:p-4 android:p-2 text-lg' after platform-variant stripping"
    - "ExpoRouterAdapter builds a per-file styleIndex via parseStyleSheetCreate and a project-scoped globalStyleIndex for one-hop imports"
    - "All globalStyleIndex Map keys pass through toForwardSlash before insertion or lookup"
    - "Full vitest suite stays green with no failing tests in test/adapters/expo/* after snapshot re-lock"
    - "Island rule (test/architecture/island.test.ts) still passes after wiring"
  artifacts:
    - path: "src/adapters/expo/ExpoRouterAdapter.ts"
      provides: "Wired buildComponentDefinition that calls extractRNInlineStyle, extractNativeWindClassNames, flattenStyleArray; one-hop StyleSheet import resolution"
      contains: "parseStyleSheetCreate"
    - path: "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx"
      provides: "NativeWind className fixture line"
      contains: "ios:p-4 android:p-2 text-lg"
    - path: "test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md"
      provides: "Re-locked snapshot showing className tokens + inline styles + style-array keys"
  key_links:
    - from: "src/adapters/expo/ExpoRouterAdapter.ts"
      to: "src/core/styles/rn/stylesheet-create.ts"
      via: 'import { parseStyleSheetCreate } from "../../core/styles/rn/stylesheet-create.js"'
      pattern: "parseStyleSheetCreate"
    - from: "src/adapters/expo/ExpoRouterAdapter.ts"
      to: "src/core/styles/rn/style-prop.ts"
      via: 'import { extractRNInlineStyle, extractNativeWindClassNames } from "../../core/styles/rn/style-prop.js"'
      pattern: "extractRNInlineStyle"
    - from: "src/adapters/expo/ExpoRouterAdapter.ts"
      to: "src/core/styles/rn/index.ts"
      via: 'import { flattenStyleArray } from "../../core/styles/rn/index.js"'
      pattern: "flattenStyleArray"
    - from: "ExpoRouterAdapter.buildComponentDefinition"
      to: "ComponentDefinition.classNames + .inlineStyles"
      via: "fill from extract* calls instead of hardcoded [] / {}"
      pattern: "classNames:|inlineStyles:"
---

<objective>
Wire the three Wave 1 RN style extractors into ExpoRouterAdapter and update the expo-tabs-and-dynamic fixture with a NativeWind className line. Build a project-scoped globalStyleIndex during extractComponents to support one-hop StyleSheet imports per CONTEXT.md D-02/D-03. Re-lock the two Expo snapshot baselines so they reflect the newly populated classNames/inlineStyles/style-array keys.

Purpose: Delivers RN-04 (one-hop import resolution), RN-05/RN-06/RN-07 at integration level. Completes Phase 13 — find_by_style works on Expo Router projects.

Output: ExpoRouterAdapter calls all three core extractors; expo-tabs-and-dynamic fixture has NativeWind line; snapshot baselines re-locked; full suite green with all SPEC acceptance criteria met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-rn-style-signal-extraction/13-SPEC.md
@.planning/phases/13-rn-style-signal-extraction/13-CONTEXT.md
@.planning/phases/13-rn-style-signal-extraction/13-RESEARCH.md
@.planning/phases/13-rn-style-signal-extraction/13-PATTERNS.md
@.planning/phases/13-rn-style-signal-extraction/13-01-SUMMARY.md
@.planning/phases/13-rn-style-signal-extraction/13-02-SUMMARY.md
@src/adapters/expo/ExpoRouterAdapter.ts
@src/core/styles/rn/stylesheet-create.ts
@src/core/styles/rn/style-prop.ts
@src/core/styles/rn/index.ts
@src/core/paths.ts
@src/core/resolver/index.ts
@src/core/import-bindings.ts
@src/adapters/expo/rn-primitives.ts
@test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx
@test/adapters/expo/__snapshots__/expo-basic.md
@test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md

<interfaces>
<!-- Adapter wiring contract — see PATTERNS.md "ExpoRouterAdapter" section for exact insertion points -->

Existing buildComponentDefinition signature (~line 315):
  private buildComponentDefinition(
    comp: DiscoveredComponent,
    ast: t.File,
    source: string,
    file: string,
    bindings: Map<string, ImportBinding>,
  ): ComponentDefinition

New signature (adds fileStyleIndex):
  private buildComponentDefinition(
    comp: DiscoveredComponent,
    ast: t.File,
    source: string,
    file: string,
    bindings: Map<string, ImportBinding>,
    fileStyleIndex: Map<string, string[]>,
  ): ComponentDefinition

extractComponents loop additions (per file, after parseFile success):
  1. const fileStyleIndex = parseStyleSheetCreate(parsed.ast, parsed.source, warningsLocal, fwdFile)
  2. For each binding in bindings where !fileStyleIndex.has(localName) AND binding.source.startsWith("."):
     - coreResolveModule(ctx, fwdFile, binding.source, binding.importedName)
     - if local: targetPath = toForwardSlash(resolved.absolutePath)
     - if !globalStyleIndex.has(targetPath):
         parseFile(ctx, targetPath) → parseStyleSheetCreate → globalStyleIndex.set(targetPath, subMap)
     - targetSubMap.get(binding.importedName === "default" ? <varName-in-target> : binding.importedName)?
       (D-04: two-hop and missing-key both degrade to fileStyleIndex unchanged for that localName — warning emitted)

buildComponentDefinition style-extraction logic (per RN-primitive JSX element discovered in renderFlow):
  - isRNPrimitive(name, importSource) guard FIRST (preserves island rule: this stays adapter-side)
  - inlineStyles = extractRNInlineStyle(jsxElement, source) → spread into ComponentDefinition.inlineStyles
  - classNames = extractNativeWindClassNames(jsxElement, warnings, file, line) → assign to ComponentDefinition.classNames
  - For `style={[...]}`, locate the JSXExpressionContainer attr value, call flattenStyleArray(node, fileStyleIndex, source, warnings, file) → merge keys into a synthetic styleArrayKeys field OR into ComponentDefinition.classNames (per SPEC: find_by_style searches across all signal channels — confirm channel by reading current find_by_style impl)

ParseContext warning channel:
  - All warnings: string[] arrays collected by core utilities get flushed into ctx.warnings after each call
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NativeWind className fixture line to expo-tabs-and-dynamic</name>
  <files>test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx</files>
  <read_first>
    - test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx (current file content — single export, View + Text + StyleSheet.create)
    - .planning/phases/13-rn-style-signal-extraction/13-PATTERNS.md (exact target line)
    - .planning/phases/13-rn-style-signal-extraction/13-SPEC.md (Req 6 — fixture must have at least one `className="..."` usage on an RN primitive)
  </read_first>
  <action>
    Edit the file. The current `<Text>` opening tag is `<Text style={{ fontWeight: "bold" }}>`. Change it to add the className attribute: `<Text style={{ fontWeight: "bold" }} className="ios:p-4 android:p-2 text-lg">`. Keep the closing `</Text>` and the text content "Home" unchanged. Do NOT change any other line, do NOT change the StyleSheet.create object, do NOT change the View opening tag. The fixture must still parse cleanly with @babel/parser.

    After the edit, validate the fixture parses by running the parser smoke test (vitest run test/adapters/expo).
  </action>
  <verify>
    <automated>grep -c 'className="ios:p-4 android:p-2 text-lg"' test/fixtures/expo-tabs-and-dynamic/app/\(tabs\)/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'className="ios:p-4 android:p-2 text-lg"' "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx"` returns exactly 1
    - `grep -c 'style={{ fontWeight: "bold" }}' "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx"` still returns 1 (inline style preserved)
    - `grep -c 'StyleSheet.create' "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx"` still returns 1 (StyleSheet.create call preserved)
    - `grep -c 'style={\[styles.card, active && styles.bold\]}' "test/fixtures/expo-tabs-and-dynamic/app/(tabs)/index.tsx"` still returns 1 (style array preserved)
    - File parses without error — `vitest run test/adapters/expo` does not fail with parse error
    - <done>Fixture has NativeWind line on a Text RN primitive, all other signals (inline style, style array, StyleSheet.create) preserved.</done>
  </acceptance_criteria>
  <done>NativeWind className="ios:p-4 android:p-2 text-lg" added to the Text primitive in expo-tabs-and-dynamic; all existing fixture signals preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ExpoRouterAdapter — build styleIndex, call three extractors in buildComponentDefinition, support one-hop imports</name>
  <files>src/adapters/expo/ExpoRouterAdapter.ts</files>
  <read_first>
    - src/adapters/expo/ExpoRouterAdapter.ts (full file — focus on imports block lines 1–43, extractComponents lines 186–308, buildComponentDefinition lines 315–354)
    - src/core/styles/rn/stylesheet-create.ts (signature: ast, source, warnings, file)
    - src/core/styles/rn/style-prop.ts (signatures: extractRNInlineStyle, extractNativeWindClassNames)
    - src/core/styles/rn/index.ts (signature: flattenStyleArray)
    - src/core/paths.ts (toForwardSlash — already imported in adapter)
    - src/core/resolver/index.ts (coreResolveModule contract — already imported)
    - src/adapters/expo/rn-primitives.ts (isRNPrimitive guard — already imported)
    - src/adapters/types.ts (ComponentDefinition shape — confirm classNames and inlineStyles field types)
    - .planning/phases/13-rn-style-signal-extraction/13-PATTERNS.md (exact wiring snippets for ExpoRouterAdapter — insertion points, fwdFile invariant)
    - .planning/phases/13-rn-style-signal-extraction/13-RESEARCH.md (Pattern 4 + Pitfall 6: toForwardSlash on all Map keys)
  </read_first>
  <action>
    Edit src/adapters/expo/ExpoRouterAdapter.ts. Add three new core imports near the existing core imports block (after `import { collectImportBindings } from "../../core/import-bindings.js";`): `import { parseStyleSheetCreate } from "../../core/styles/rn/stylesheet-create.js";`, `import { extractRNInlineStyle, extractNativeWindClassNames } from "../../core/styles/rn/style-prop.js";`, `import { flattenStyleArray } from "../../core/styles/rn/index.js";`.

    In extractComponents: declare `const globalStyleIndex = new Map<string, Map<string, string[]>>();` as a local variable at the top of the method (scoped per call — see RESEARCH Open Question 1: local, not instance). Inside the existing per-file loop, after parseFile succeeds and after `const bindings = collectImportBindings(parsed.ast)` is computed, add:
    (a) `const fileWarnings: string[] = []`
    (b) `const fileStyleIndex = parseStyleSheetCreate(parsed.ast, parsed.source, fileWarnings, fwdFile)`
    (c) Cache: `globalStyleIndex.set(fwdFile, fileStyleIndex)` (so the same file is not reparsed if referenced via one-hop later)
    (d) One-hop loop: `for (const [localName, binding] of bindings) { if (fileStyleIndex.has(localName)) continue; if (!binding.source.startsWith(".")) continue; const resolved = coreResolveModule(ctx, fwdFile, binding.source, binding.importedName); if (!resolved.ok || resolved.kind !== "local") continue; const targetPath = toForwardSlash(resolved.absolutePath); let targetIndex = globalStyleIndex.get(targetPath); if (!targetIndex) { const targetParsed = parseFile(ctx, targetPath); if (targetParsed.kind === "error") { ctx.warnings.push(\`Failed to parse StyleSheet import at ${targetPath}: ${targetParsed.message}\`); continue; } targetIndex = parseStyleSheetCreate(targetParsed.ast, targetParsed.source, fileWarnings, targetPath); globalStyleIndex.set(targetPath, targetIndex); } const importedKeys = targetIndex.get(binding.importedName); if (importedKeys) fileStyleIndex.set(localName, importedKeys); else fileWarnings.push(\`StyleSheet '${binding.importedName}' not found in imported file ${targetPath} — D-04 fallback at ${fwdFile}\`); }`
    (e) After the loop: `for (const w of fileWarnings) ctx.warnings.push(w);`
    (f) Pass fileStyleIndex through to buildComponentDefinition: update the existing call site near line 301 to add `fileStyleIndex` as the 6th argument.

    Modify buildComponentDefinition signature: add 6th parameter `fileStyleIndex: Map<string, string[]>`. The current return literal assigns `classNames: [], inlineStyles: {}`. Replace with extraction logic that runs ONLY for RN primitives (preserves island rule — isRNPrimitive stays adapter-side):

    Inside the existing JSXElement walking inside buildComponentDefinition (where renderFlow is built), for each JSXElement that is also an RN primitive (use the existing isRNPrimitive(name, importSource) check pattern from Phase 12), accumulate per-element: inline styles via `extractRNInlineStyle(jsxElement, source)`; className tokens via `extractNativeWindClassNames(jsxElement, localWarnings, file, line)`; style-array keys by locating the style JSXAttribute, and if its value is a JSXExpressionContainer wrapping an ArrayExpression, calling `flattenStyleArray(attr.value, fileStyleIndex, source, localWarnings, file)`. Merge results into the component's classNames and inlineStyles fields. For the style-array keys (which are StyleSheet variable names being unioned), append to ComponentDefinition.classNames so find_by_style can match them by key name — this is the v1.2 simplification per SPEC Req 5 (key-union into the single style-signal channel that find_by_style queries).

    Flush localWarnings into ctx.warnings at the end of buildComponentDefinition. Keep cssModuleRefs and styledTemplates at []/[] (out of v1.2 RN scope).

    Important guard rails:
    - Do NOT modify src/core/Analyzer.ts (EXPO-SLOT-01 already fixed in commit 7b80ae0 per D-05 — verify-only)
    - Do NOT change ComponentDefinition's type shape — only fill fields that already exist
    - Apply toForwardSlash to every absPath used as a Map key (Pitfall 6)
    - Component itself (the layout/page component) is NOT necessarily an RN primitive — only descend to RN primitives in renderFlow for the style extraction; the existing buildComponentDefinition logic already walks renderFlow
  </action>
  <verify>
    <automated>vitest run test/adapters/expo test/architecture/island.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'parseStyleSheetCreate' src/adapters/expo/ExpoRouterAdapter.ts` returns ≥ 2 (import + at least one call)
    - `grep -c 'extractRNInlineStyle' src/adapters/expo/ExpoRouterAdapter.ts` returns ≥ 2
    - `grep -c 'extractNativeWindClassNames' src/adapters/expo/ExpoRouterAdapter.ts` returns ≥ 2
    - `grep -c 'flattenStyleArray' src/adapters/expo/ExpoRouterAdapter.ts` returns ≥ 2
    - `grep -c 'globalStyleIndex' src/adapters/expo/ExpoRouterAdapter.ts` returns ≥ 3 (declaration + set + get)
    - `grep -c 'toForwardSlash' src/adapters/expo/ExpoRouterAdapter.ts` returns ≥ 2 (existing fwdFile + new targetPath normalization)
    - `vitest run test/architecture/island.test.ts` still passes (zero src/adapters/ imports in src/core/styles/rn/)
    - `git diff src/core/Analyzer.ts` returns empty (no edits — Pitfall 2)
    - `npx tsc --noEmit` clean
    - <done>Adapter imports + calls all three Wave 1 extractors; one-hop import resolution implemented with toForwardSlash invariant; Analyzer.ts untouched.</done>
  </acceptance_criteria>
  <done>ExpoRouterAdapter calls parseStyleSheetCreate, extractRNInlineStyle, extractNativeWindClassNames, flattenStyleArray; globalStyleIndex caches one-hop targets with forward-slash keys; ComponentDefinition.classNames and inlineStyles populated from RN primitives.</done>
</task>

<task type="auto">
  <name>Task 3: Re-lock Expo snapshots + verify full SPEC acceptance criteria</name>
  <files>test/adapters/expo/__snapshots__/expo-basic.md, test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md</files>
  <read_first>
    - test/adapters/expo/__snapshots__/expo-basic.md (current snapshot — already shows EXPO-SLOT-01 injected tree per commit 7b80ae0)
    - test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md (same)
    - test/adapters/expo/*.test.ts (the test files that produce these snapshots — confirm they use toMatchFileSnapshot)
    - .planning/phases/13-rn-style-signal-extraction/13-SPEC.md (Acceptance Criteria checklist — all 13 items)
  </read_first>
  <action>
    Run the focused Expo test suite to surface snapshot diffs caused by the newly populated classNames/inlineStyles in Plan 02+03: `vitest run test/adapters/expo`. Expect snapshot mismatches in expo-basic.md and expo-tabs-and-dynamic.md because the snapshots were locked when classNames=[] and inlineStyles={}; now they will contain real values.

    Review the diffs to confirm the new content reflects:
    (a) expo-basic: the `<View style={styles.container}>` node now shows the "container" key in its style signal channel
    (b) expo-tabs-and-dynamic: the `<View style={[styles.card, active && styles.bold]}>` node shows "card" and "bold" keys; the `<Text style={{ fontWeight: "bold" }} className="ios:p-4 android:p-2 text-lg">` shows fontWeight in inlineStyles AND p-4, p-2, text-lg in classNames

    If the diffs match expectations (i.e. only newly-populated fields appear, no structural regressions), update snapshots by running `vitest run test/adapters/expo -u`. If the diffs show structural regressions (missing tree nodes, lost children, kind: "component" where slot was expected before), STOP and report — do NOT update snapshots blindly. Structural regressions indicate the wiring broke EXPO-SLOT-01 indirectly.

    After re-lock, run the full test suite: `vitest run`. Confirm:
    - Total passing ≥ 494 + Wave 1 new tests (Plan 02 added ≥ 18)
    - All tests in test/adapters/expo green
    - All tests in test/core/styles/rn green
    - test/architecture/island.test.ts green
    - test/core/extractors/inline-style.test.ts still green (v1.0 regression)

    Run an end-to-end smoke check on the SPEC acceptance criteria (these may already be covered by the test files, but verify by reading the snapshot diffs):
    - container, card, fontWeight, bold, p-4 keys all appear in the expected nodes of the re-locked snapshots
    - No backslashes appear in any file:line output in either snapshot (`grep -c '\\\\' test/adapters/expo/__snapshots__/*.md` returns 0 — Windows path invariant)
  </action>
  <verify>
    <automated>vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run` exits 0; total passing count ≥ (494 + Plan 02 additions, around 512+)
    - All tests in test/adapters/expo pass
    - `grep -c '"container"' test/adapters/expo/__snapshots__/expo-basic.md` returns ≥ 1 (StyleSheet key surfaced in snapshot)
    - `grep -c '"card"' test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md` returns ≥ 1
    - `grep -c '"bold"' test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md` returns ≥ 1
    - `grep -c '"p-4"' test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md` returns ≥ 1
    - `grep -c '"text-lg"' test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md` returns ≥ 1
    - `grep -c 'fontWeight' test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md` returns ≥ 1
    - `grep -c '\\\\' test/adapters/expo/__snapshots__/expo-basic.md test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md` returns 0 (no backslashes — Windows path invariant)
    - `git diff src/core/Analyzer.ts` empty (Pitfall 2)
    - `vitest run test/architecture/island.test.ts` passes
    - <done>All 13 SPEC acceptance criteria observable in snapshot content + test count; phase complete.</done>
  </acceptance_criteria>
  <done>Both Expo snapshot baselines re-locked with new style signals; full vitest suite green at ≥ 512 passing; all 13 SPEC acceptance criteria met; Phase 13 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-source files → ExpoRouterAdapter | User's app/*.tsx and one-hop StyleSheet files parsed and traversed |
| filesystem → parseFile / coreResolveModule | Adapter performs filesystem reads on resolved one-hop paths |
| Map keys → globalStyleIndex | absPath strings used as cache keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering / Path Injection | globalStyleIndex Map keys | mitigate | toForwardSlash applied to every absPath before set/get (Pitfall 6) |
| T-13-07 | Tampering / Input Validation | one-hop target file parsing | mitigate | parseFile returns { kind: "error" } on parse failure → adapter pushes ctx.warnings + continue; no throw |
| T-13-08 | DoS / Cyclic Imports | one-hop graph traversal | accept | One-hop only (D-04 — two-hop degrades to {raw}+warning); cycles cannot recurse |
| T-13-09 | Information Disclosure | warning strings include absPaths | accept | Paths are project-internal; same level of disclosure as existing Phase 12 warnings |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed |
</threat_model>

<verification>
- `vitest run test/adapters/expo` — all Expo adapter tests pass with re-locked snapshots
- `vitest run test/core/styles/rn` — Wave 1 unit tests still pass (no regressions)
- `vitest run test/architecture/island.test.ts` — island rule holds
- `vitest run test/core/extractors/inline-style.test.ts` — v1.0 regression green
- `vitest run` — full suite ≥ 512 passing (494 baseline + ~18 new in Plan 02)
- `git diff src/core/Analyzer.ts` — empty
- `grep -c '\\\\' test/adapters/expo/__snapshots__/*.md` — 0 (Windows forward-slash invariant)
- `npx tsc --noEmit` — clean
</verification>

<success_criteria>
- ExpoRouterAdapter imports and calls all three Wave 1 extractors
- globalStyleIndex caches one-hop StyleSheet imports with toForwardSlash keys
- buildComponentDefinition populates classNames and inlineStyles for RN primitives via the new extractors
- expo-tabs-and-dynamic fixture has className="ios:p-4 android:p-2 text-lg" on Text
- Both Expo snapshot baselines re-locked with new style signals (container, card, bold, p-4, text-lg, fontWeight all visible)
- Full vitest suite green at ≥ 512 passing
- All 13 SPEC acceptance criteria met
- Zero edits to src/core/Analyzer.ts (Pitfall 2 honored — EXPO-SLOT-01 was already fixed in 7b80ae0)
- Island rule preserved (test/architecture/island.test.ts passes)
- No backslashes in snapshot file:line output (Windows invariant)
</success_criteria>

<output>
Create `.planning/phases/13-rn-style-signal-extraction/13-03-SUMMARY.md` when done, recording: final vitest count, snapshot file sizes before/after, confirmation of every SPEC acceptance criterion, and a one-line "Phase 13 complete" status that /gsd:verify-work can read.
</output>
