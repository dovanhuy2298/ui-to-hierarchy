---
phase: 13-rn-style-signal-extraction
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/core/styles/rn/stylesheet-create.ts
  - src/core/styles/rn/style-prop.ts
  - src/core/styles/rn/index.ts
  - test/core/styles/rn/stylesheet-create.test.ts
  - test/core/styles/rn/style-prop.test.ts
  - test/core/styles/rn/index.test.ts
autonomous: true
requirements:
  - RN-04
  - RN-05
  - RN-06
  - RN-07
  - RN-08
tags:
  - phase-13
  - wave-0
  - scaffolding
  - rn-styles

must_haves:
  truths:
    - "Directory src/core/styles/rn/ exists with three stub files exporting placeholder functions"
    - "Directory test/core/styles/rn/ exists with three test files containing empty describe blocks (or .skip-marked) for each requirement"
    - "EXPO-SLOT-01 is verified: `vitest run` produces 494 passing tests with NO edits to src/core/Analyzer.ts or snapshot files"
  artifacts:
    - path: "src/core/styles/rn/stylesheet-create.ts"
      provides: "parseStyleSheetCreate stub export"
      contains: "export function parseStyleSheetCreate"
    - path: "src/core/styles/rn/style-prop.ts"
      provides: "extractRNInlineStyle + extractNativeWindClassNames stub exports"
      contains: "export function extractRNInlineStyle"
    - path: "src/core/styles/rn/index.ts"
      provides: "flattenStyleArray export + barrel"
      contains: "export function flattenStyleArray"
    - path: "test/core/styles/rn/stylesheet-create.test.ts"
      provides: "RN-04 + RN-08 test stubs"
    - path: "test/core/styles/rn/style-prop.test.ts"
      provides: "RN-05 + RN-07 test stubs"
    - path: "test/core/styles/rn/index.test.ts"
      provides: "RN-06 flattenStyleArray test stubs (≥ 8 case stubs)"
  key_links:
    - from: "src/core/styles/rn/stylesheet-create.ts"
      to: "src/core/babel-shim.ts"
      via: 'import { traverse } from "../../babel-shim.js"'
      pattern: 'babel-shim'
    - from: "src/core/styles/rn/style-prop.ts"
      to: "src/core/extractors/inline-style.ts"
      via: 'import { extractInlineStyle } from "../../extractors/inline-style.js"'
      pattern: 'extractors/inline-style'
---

<objective>
Create Wave 0 scaffolding for Phase 13: three new core utility stub files under src/core/styles/rn/, three matching test files under test/core/styles/rn/ with describe blocks naming every requirement scenario, and verify EXPO-SLOT-01 is already green from commit 7b80ae0.

Purpose: Provides the file skeleton + failing test references so Wave 1 implementation plans have concrete targets. Also confirms that EXPO-SLOT-01 (Req 1 of the phase) needs zero code edits — only a verification run.

Output: 3 source stubs + 3 test stubs + a verified-green full test suite at 494 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-rn-style-signal-extraction/13-SPEC.md
@.planning/phases/13-rn-style-signal-extraction/13-CONTEXT.md
@.planning/phases/13-rn-style-signal-extraction/13-RESEARCH.md
@.planning/phases/13-rn-style-signal-extraction/13-PATTERNS.md
@src/core/babel-shim.ts
@src/core/import-bindings.ts
@src/core/extractors/inline-style.ts
@test/core/extractors/inline-style.test.ts

<interfaces>
<!-- Reuse contracts already present in the codebase -->

From src/core/extractors/inline-style.ts:
- export function extractInlineStyle(jsxElement: t.JSXElement, source: string): Record<string, string | { raw: string }>

From src/core/import-bindings.ts:
- export interface ImportBinding { source: string; importedName: string }
- export function collectImportBindings(ast: t.File): Map<string, ImportBinding>

From src/core/babel-shim.ts:
- export const traverse  // ESM/CJS-safe traverse function

Phase 13 new contracts (stub signatures — implementation in Wave 1):
- parseStyleSheetCreate(ast: t.File, source: string, warnings: string[], file: string): Map<string, string[]>
  (warnings/file forwarded for RN-08 raw-emit on non-literal-object args)
- extractRNInlineStyle(jsxElement: t.JSXElement, source: string): Record<string, string | { raw: string }>
  (thin delegation to extractInlineStyle)
- extractNativeWindClassNames(jsxElement: t.JSXElement, warnings: string[], file: string, line: number): string[]
- flattenStyleArray(node: t.JSXExpressionContainer, fileStyleIndex: Map<string, string[]>, source: string, warnings: string[], file: string): string[]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/core/styles/rn/ stub source files with exported signatures</name>
  <files>src/core/styles/rn/stylesheet-create.ts, src/core/styles/rn/style-prop.ts, src/core/styles/rn/index.ts</files>
  <read_first>
    - src/core/extractors/inline-style.ts (reuse contract for extractRNInlineStyle delegation)
    - src/core/import-bindings.ts (analog file shape: header doc-block + traverse import pattern)
    - src/core/babel-shim.ts (the only legal traverse import point)
    - .planning/phases/13-rn-style-signal-extraction/13-PATTERNS.md (signatures + island-rule comment template)
    - .planning/phases/13-rn-style-signal-extraction/13-RESEARCH.md (verified AST patterns)
  </read_first>
  <behavior>
    - parseStyleSheetCreate is exported as a named function with signature (ast: t.File, source: string, warnings: string[], file: string) => Map&lt;string, string[]&gt; — body returns new Map() (stub)
    - extractRNInlineStyle is exported as named function with signature (jsxElement: t.JSXElement, source: string) => Record&lt;string, string | { raw: string }&gt; — body delegates `return extractInlineStyle(jsxElement, source)`
    - extractNativeWindClassNames is exported as named function with signature (jsxElement: t.JSXElement, warnings: string[], file: string, line: number) => string[] — body returns []
    - flattenStyleArray is exported as named function with signature (node: t.JSXExpressionContainer, fileStyleIndex: Map&lt;string, string[]&gt;, source: string, warnings: string[], file: string) => string[] — body returns []
    - index.ts re-exports parseStyleSheetCreate, extractRNInlineStyle, extractNativeWindClassNames in addition to defining flattenStyleArray
    - Every file has an island-rule comment block at the top stating "Island rule: ZERO imports from src/adapters/"
    - No file under src/core/styles/rn/ contains the substring "src/adapters" or "adapters/"
  </behavior>
  <action>
    Create three new TypeScript files. File 1 src/core/styles/rn/stylesheet-create.ts: header doc-block matching the format used in src/core/import-bindings.ts (purpose, support matrix placeholder listing supported = in-file literal object + one-hop import, unsupported = computed keys / factory call / hook return / two-hop import → all degrade to `{ raw }` + warning, island rule line stating "ZERO imports from src/adapters/", traverse-must-come-from-babel-shim line). Import `* as t from "@babel/types"` and `{ traverse } from "../../babel-shim.js"`. Export `parseStyleSheetCreate(ast: t.File, source: string, warnings: string[], file: string): Map&lt;string, string[]&gt;` returning `new Map()` with TODO comment "// Wave 1: implement per RESEARCH Pattern 1".

    File 2 src/core/styles/rn/style-prop.ts: same header doc-block style. Import `* as t from "@babel/types"` and `{ extractInlineStyle } from "../../extractors/inline-style.js"`. Export `extractRNInlineStyle(jsxElement: t.JSXElement, source: string)` returning `extractInlineStyle(jsxElement, source)` (thin delegation — this is the SPEC-mandated reuse). Export `extractNativeWindClassNames(jsxElement: t.JSXElement, warnings: string[], file: string, line: number): string[]` returning `[]` with TODO comment "// Wave 1: implement per RESEARCH Pattern 3 (regex /(ios|android|web|native):/g)".

    File 3 src/core/styles/rn/index.ts: same header doc-block style. Import `* as t from "@babel/types"`. Export `flattenStyleArray(node: t.JSXExpressionContainer, fileStyleIndex: Map&lt;string, string[]&gt;, source: string, warnings: string[], file: string): string[]` returning `[]` with TODO comment "// Wave 1: implement per RESEARCH Pattern 2 (null-check before t.is*; ≥ 8 cases)". Then barrel re-export: `export { parseStyleSheetCreate } from "./stylesheet-create.js"; export { extractRNInlineStyle, extractNativeWindClassNames } from "./style-prop.js";`.

    All files use forward-slash `.js` extension imports (ESM-aware). Do NOT import from `src/adapters/` anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; vitest run test/architecture/island.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/core/styles/rn/stylesheet-create.ts exists; `grep -c 'export function parseStyleSheetCreate' src/core/styles/rn/stylesheet-create.ts` returns 1
    - File src/core/styles/rn/style-prop.ts exists; `grep -c 'export function extractRNInlineStyle' src/core/styles/rn/style-prop.ts` returns 1; `grep -c 'export function extractNativeWindClassNames' src/core/styles/rn/style-prop.ts` returns 1
    - File src/core/styles/rn/index.ts exists; `grep -c 'export function flattenStyleArray' src/core/styles/rn/index.ts` returns 1
    - Barrel: `grep -c 'export { parseStyleSheetCreate }' src/core/styles/rn/index.ts` returns 1
    - Island rule: `grep -rE '(adapters/|src/adapters)' src/core/styles/rn/ | grep -v '^#'` returns nothing (exit code 1)
    - Test command `vitest run test/architecture/island.test.ts` passes
    - `npx tsc --noEmit` passes with zero errors in src/core/styles/rn/**
    - extractRNInlineStyle source contains the call `extractInlineStyle(jsxElement, source)` (no reimplementation)
    - <done>Three stub files exist, all exports are typed and callable, tsc clean, island test green.</done>
  </acceptance_criteria>
  <done>Three new files in src/core/styles/rn/ committed, all exports compile, island rule test passes, extractRNInlineStyle delegates (not reimplements) per SPEC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/core/styles/rn/ stub files with describe/it for every requirement scenario</name>
  <files>test/core/styles/rn/stylesheet-create.test.ts, test/core/styles/rn/style-prop.test.ts, test/core/styles/rn/index.test.ts</files>
  <read_first>
    - test/core/extractors/inline-style.test.ts (analog test structure, ctx() helper, JSXElement traversal helper)
    - src/core/styles/rn/stylesheet-create.ts (the stub created in Task 1 — confirm signature)
    - src/core/styles/rn/style-prop.ts (Task 1 stub)
    - src/core/styles/rn/index.ts (Task 1 stub)
    - .planning/phases/13-rn-style-signal-extraction/13-PATTERNS.md (test cases enumerated for each module)
    - .planning/phases/13-rn-style-signal-extraction/13-VALIDATION.md (Wave 0 gaps table)
  </read_first>
  <behavior>
    - stylesheet-create.test.ts contains a describe("RN-04 + RN-08 parseStyleSheetCreate") with `it.todo` (or `it` body throwing not-implemented) entries for: literal in-file StyleSheet.create extraction, computed-key argument graceful degrade, factory-call argument graceful degrade (RN-08), one-hop import resolved by caller (caller-passes-AST contract), two-hop fallback to empty + warning
    - style-prop.test.ts contains a describe("RN-05 extractRNInlineStyle") with `it.todo` entries for: style={{fontWeight:"bold"}} extraction parity with v1.0; and describe("RN-07 extractNativeWindClassNames") with `it.todo` for: strip ios:/android:/web:/native: prefixes, tokenize on whitespace, tagged-template `tw\`text-lg\`` emits warning + returns []
    - index.test.ts contains a describe("RN-06 flattenStyleArray") with ≥ 8 `it.todo` entries: plain MemberExpression styles.card, two members, conditional &&, conditional ||, StringLiteral element, null/false element skip, CallExpression element warning + skip, nested ArrayExpression warn + skip, varName not in fileStyleIndex warning
    - All three test files import their target functions and reference them in at least one `it.todo` description string so the test files trivially compile
    - Wave 0 invariant: Tests can use `it.todo` (vitest pending markers) OR `it.skip` so the full vitest suite stays at 494 passing (no new RED tests in Wave 0 — Wave 1 plans flip these to real assertions)
  </behavior>
  <action>
    Create three new vitest test files. File 1 test/core/styles/rn/stylesheet-create.test.ts: ESM imports — `import { describe, it, expect } from "vitest"`, `import { parseStyleSheetCreate } from "../../../src/core/styles/rn/stylesheet-create.js"`, `import { parse } from "@babel/parser"`. One describe block "RN-04 + RN-08 parseStyleSheetCreate" with `it.todo` entries titled exactly: "extracts literal keys from in-file StyleSheet.create({...}) into varName→keys map", "returns empty map and emits raw warning when argument is computed key {[k]:{}} — RN-08", "returns empty map and emits raw warning when argument is factory call StyleSheet.create(getStyles()) — RN-08", "resolves one-hop import when caller passes pre-parsed AST (D-03 contract)", "two-hop import returns empty and emits warning — RN-08 fallback".

    File 2 test/core/styles/rn/style-prop.test.ts: imports `extractRNInlineStyle, extractNativeWindClassNames` from style-prop.js, `parse` from `@babel/parser`. Two describe blocks. "RN-05 extractRNInlineStyle" with it.todo entries: "delegates to v1.0 extractInlineStyle for style={{fontWeight:'bold'}}", "returns __raw__ sentinel for computed expr style={x}". "RN-07 extractNativeWindClassNames" with it.todo entries: "strips ios:/android:/web:/native: variants from className string and tokenizes on whitespace — yields p-4 p-2 text-lg", "returns [] and pushes warning when className={tw\\`text-lg\\`} tagged template", "returns [] for missing className attribute".

    File 3 test/core/styles/rn/index.test.ts: imports `flattenStyleArray` from index.js, `parse` from `@babel/parser`. One describe block "RN-06 flattenStyleArray" with exactly these 8 it.todo entries: "resolves MemberExpression styles.card to fileStyleIndex keys", "unions keys from two MemberExpression members styles.a + styles.b", "includes right-side keys from LogicalExpression && conditional", "includes right-side keys from LogicalExpression || conditional", "passes StringLiteral element through as a key", "skips null sparse-array hole and BooleanLiteral false element", "warns on CallExpression element and skips it", "warns on nested ArrayExpression element and skips it", "warns when MemberExpression varName not present in fileStyleIndex".

    All `it.todo` calls — no failing assertions in Wave 0. This keeps suite at 494 green; Wave 1 plans convert each it.todo to a real `it(...)` with full assertions.
  </action>
  <verify>
    <automated>vitest run test/core/styles/rn</automated>
  </verify>
  <acceptance_criteria>
    - File test/core/styles/rn/stylesheet-create.test.ts exists with at least 5 `it.todo` calls — `grep -c 'it\.todo' test/core/styles/rn/stylesheet-create.test.ts` returns ≥ 5
    - File test/core/styles/rn/style-prop.test.ts exists with at least 5 `it.todo` calls — `grep -c 'it\.todo' test/core/styles/rn/style-prop.test.ts` returns ≥ 5
    - File test/core/styles/rn/index.test.ts exists with at least 9 `it.todo` calls (≥ 8 flattenStyleArray cases + 1 buffer) — `grep -c 'it\.todo' test/core/styles/rn/index.test.ts` returns ≥ 9
    - All three test files import their respective module via `from "../../../src/core/styles/rn/...js"`
    - Command `vitest run test/core/styles/rn` exits 0 (todo items are not failures)
    - <done>Test stubs exist, vitest accepts them, no new failures introduced.</done>
  </acceptance_criteria>
  <done>Three test stub files in test/core/styles/rn/ committed; each requirement scenario from SPEC has a named it.todo placeholder ready for Wave 1 to fill in.</done>
</task>

<task type="auto">
  <name>Task 3: Verify EXPO-SLOT-01 is already green — run full vitest suite, confirm 494 passing, NO edits to Analyzer.ts</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/13-rn-style-signal-extraction/13-RESEARCH.md (Pitfall 2: Do not rework EXPO-SLOT-01)
    - .planning/phases/13-rn-style-signal-extraction/13-CONTEXT.md (D-05: EXPO-SLOT-01 already fixed in commit 7b80ae0)
    - test/adapters/expo/__snapshots__/expo-basic.md (confirm snapshot already shows injected tree)
    - test/adapters/expo/__snapshots__/expo-tabs-and-dynamic.md (confirm snapshot already shows injected tree)
  </read_first>
  <action>
    Run the full vitest suite once: `vitest run`. Confirm:
    (a) total tests passing is ≥ 494 (baseline from STATE.md post-Phase-12)
    (b) zero tests in test/adapters/expo/* fail
    (c) no edits are made to src/core/Analyzer.ts at any point in Phase 13 — this task explicitly forbids touching that file
    (d) `git status` shows no modifications to src/core/Analyzer.ts or test/adapters/expo/__snapshots__/*.md beyond the Wave 0 stubs added in Task 1 and Task 2

    If vitest reports any failing test in test/adapters/expo/, STOP and report — do NOT auto-update snapshots. The EXPO-SLOT-01 fix and snapshot lock both landed in commit 7b80ae0 per D-05; any failure indicates a regression introduced elsewhere, not a planned re-lock.

    Record the exact test count (e.g. "494 passed | 9 todo") in the plan SUMMARY for downstream waves.
  </action>
  <verify>
    <automated>vitest run --reporter=default</automated>
  </verify>
  <acceptance_criteria>
    - `vitest run` exits 0
    - Vitest summary line reports ≥ 494 passing tests
    - Zero test failures in `test/adapters/expo/` directory
    - `git diff src/core/Analyzer.ts` shows zero changes
    - `git diff test/adapters/expo/__snapshots__/` shows zero changes
    - Plan SUMMARY records observed test count and confirms EXPO-SLOT-01 verified green via existing commit 7b80ae0
    - <done>Full suite green at ≥ 494, Req 1 verified with no code changes.</done>
  </acceptance_criteria>
  <done>EXPO-SLOT-01 verified green at 494+ tests with zero edits to Analyzer.ts or snapshots — satisfies Phase 13 Req 1 entirely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-source → Babel parser | user repo's .ts/.tsx files are untrusted input parsed by @babel/parser |
| Babel AST → core utility functions | AST nodes pass into parseStyleSheetCreate / flattenStyleArray / extractNativeWindClassNames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Wave 0 stubs returning placeholder data | accept | Stubs return `new Map()` / `[]` — no consumer wired yet in Wave 0; Wave 1 implements with t.is* guards |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in Phase 13 — RESEARCH Package Legitimacy Audit confirms `[OK]` for @babel/parser, @babel/traverse, @babel/types, vitest (all already present) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across all new files
- `vitest run test/architecture/island.test.ts` passes (no src/adapters/ imports in src/core/styles/rn/)
- `vitest run test/core/styles/rn` accepts new stub test files
- `vitest run` full suite ≥ 494 passing — confirms EXPO-SLOT-01 Req 1 needs no code changes
- `grep -rE '(adapters/|src/adapters)' src/core/styles/rn/ | grep -v '^#'` returns nothing
</verification>

<success_criteria>
- src/core/styles/rn/ exists with stylesheet-create.ts, style-prop.ts, index.ts — all three export the contracted function signatures
- test/core/styles/rn/ exists with three test files containing ≥ 19 named it.todo entries covering RN-04, RN-05, RN-06, RN-07, RN-08
- Full vitest suite green at ≥ 494 passing tests
- Zero edits to src/core/Analyzer.ts or test/adapters/expo/__snapshots__/*.md in this plan
- Island rule (test/architecture/island.test.ts) passes
- All stubs use `import { traverse } from "../../babel-shim.js"` (NEVER directly from @babel/traverse)
- extractRNInlineStyle delegates to extractInlineStyle (no reimplementation)
</success_criteria>

<output>
Create `.planning/phases/13-rn-style-signal-extraction/13-01-SUMMARY.md` when done, recording: files created, observed vitest count for EXPO-SLOT-01 verification, and confirmation that Wave 1 plans (13-02-PLAN.md) can proceed.
</output>
