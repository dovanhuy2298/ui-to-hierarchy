---
milestone: v1.1
audited: 2026-05-12T03:15:00Z
status: gaps_found
scores:
  requirements: 17/17 (functional) — but 3-source cross-reference flags doc drift
  phases: 1/2 (Phase 7 verified; Phase 8 missing VERIFICATION.md)
  integration: 11/11 wired exports
  flows: 3/3 E2E flows complete
gaps:
  process:
    - id: "PHASE-8-NO-VERIFICATION"
      severity: blocker
      description: "Phase 8 is missing both 08-VERIFICATION.md and 08-VALIDATION.md. Per audit-milestone workflow: 'If a phase is missing VERIFICATION.md, flag it as unverified phase — this is a blocker.'"
      remediation: "Run /gsd-verify-work 8 to produce 08-VERIFICATION.md (and optionally /gsd-validate-phase 8 for Nyquist coverage)."
  requirements: []
  integration: []
  flows: []
doc_drift:
  - file: ".planning/REQUIREMENTS.md"
    issue: "Traceability table marks INIT-01..INIT-14 and POLISH-03 as 'Pending' even though all are functionally complete (code present, tests green, integration verified)."
    remediation: "Update checkboxes to [x] and Status column to 'Complete' before milestone archive — or let /gsd-complete-milestone do it via the requirements archive step."
tech_debt:
  - phase: 08-v1-0-polish
    items:
      - "Defensive `base.warnings ?? []` fallback redundant: buildEnvelope always initializes warnings: []. Cosmetic; no impact."
      - "`__INIT_MARKER_VERSION__` used in src/init/index.ts without typeof guard (unlike __TOOL_VERSION__ in src/mcp/server.ts). Safe today because runInit is only invoked via dist/cli.js where tsup substitutes the define; would throw ReferenceError if someone called runInit from tsx/vitest without a vitest `define`."
  - phase: carry-forward (from STATE.md)
    items:
      - "F-01: live Claude Code transcript export (still reconstructed from stdio-equivalent capture)"
      - "Two orphan exports in src/mcp/errors.ts (`notImplemented`, `invalidInput`) — deferred to v1.2 cleanup"
nyquist:
  compliant_phases: ["07-init-file-writer"]
  partial_phases: []
  missing_phases: ["08-v1-0-polish"]
  overall: partial
---

# Milestone v1.1 Audit — Agent Onboarding & v1.0 Polish

**Audited:** 2026-05-12T03:15:00Z
**Scope:** Phase 7 (`--init` File Writer), Phase 8 (v1.0 Polish)
**Verdict:** ⚠ gaps_found (one process-level blocker; functional code complete)

---

## Executive Summary

Functionally, v1.1 is **done**:

- All 14 INIT requirements satisfied (Phase 7 VERIFICATION.md confirms; 340/340 tests green at verification time)
- POLISH-01, POLISH-02, POLISH-03 all wired end-to-end (Phase 8 SUMMARYs + integration check confirm)
- Cross-phase integration: 11/11 connections wired, 0 orphans, 0 broken flows
- Full test suite at audit time: **356/356 passing**, build clean (`dist/cli.js` 101.20 KB), tsc clean (only pre-existing fixture error in `test/fixtures/phase-05/micro/parse-error/app/page.tsx`)

The blocker is **process**, not function: Phase 8 never produced an `08-VERIFICATION.md` (or `08-VALIDATION.md`). The integration checker effectively did the verification work, but the milestone's verification artifact gate is not satisfied per GSD workflow.

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements (functional) | 17/17 | All INIT + POLISH satisfied in code |
| Requirements (documentation) | 3/17 | Traceability table stale: only POLISH-01 + POLISH-02 + (no INIT) marked complete |
| Phase verification artifacts | 1/2 | Phase 7 ✓; Phase 8 ✗ (no VERIFICATION.md) |
| Cross-phase integration | 11/11 | All wired; init path isolated from core path |
| E2E flows | 3/3 | `--init`, MCP stdio + json/markdown, POLISH-03 line-accuracy |
| Nyquist (VALIDATION.md coverage) | 1/2 | Phase 7 has VALIDATION.md; Phase 8 does not |

## 3-Source Requirements Cross-Reference

| REQ | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|-----|-----------------|---------------------|-----------------|--------------|
| INIT-01..INIT-14 | passed (07) | listed across plans 07-01..07-05 | `[ ]` Pending (stale) | **satisfied** (update checkboxes) |
| POLISH-01 | missing (no 08-VERIFICATION.md) | `requirements-completed: [POLISH-01]` (08-01) | `[x]` Complete | **partial** (verification artifact gap) |
| POLISH-02 | missing | not listed in 08-04 frontmatter (deviated — closure noted in body) | `[x]` Complete | **partial** (verification artifact gap) |
| POLISH-03 | missing | enabling work documented in 08-02; part B in 08-03 | `[ ]` Pending (stale) | **partial** (verification artifact gap; doc drift) |

**Interpretation:** Per the status-determination matrix in audit-milestone.md, "passed × listed × any" → satisfied; "missing × any × any" → partial. So functionally everything is done, but Phase 8's three POLISH requirements drop to `partial` purely because the verification artifact does not exist.

## Cross-Phase Integration (from gsd-integration-checker)

- **Init / core isolation:** `src/init/*` imports nothing from `src/core/`, `src/ir/`, `src/renderers/`, or `src/mcp/`. Phase 8 changes have zero overlap with the init path. Confirmed by import grep.
- **renderMarkdown wiring:** All 4 MCP tools (`get_full_hierarchy`, `focus_on`, `find_by_text`, `find_by_style`) import `renderMarkdown` directly and pass envelopes containing `warnings: []` (initialized by `buildEnvelope`). The POLISH-01 prefix block fires whenever the Analyzer pushes any warning into the envelope.
- **declLines chain:** `parseFile` → `ParseResult.ok.declLines` → `resolveSpecifierToFile` (both branches) and `chaseBarrel` → `ResolveResult.local.line` → `Analyzer.resolveComponentCallsites` writes `line: result.line` → `TreeNode.line` → `lineFor(node)` in `renderMarkdown`. D-02 cache identity preserved (resolver's `parseFile` call hits the existing astCache entry; zero re-parse cost). D-03 `?? 1` fallback present at all three read sites.
- **Integration helper:** `test/integration/_helpers.ts` consumed by both `mcp-e2e.test.ts` and `mcp-markdown.test.ts`. `assertFreshBuild` runs inside `spawnMcpClient`, so callers cannot bypass.

## E2E Flow Verdict

1. **`npx ui-hierarchy-mcp --init`** → `cli.ts:meta.init` → `parseInitArgs` (strict) → `runInit` per target → `renderGuide` + `computeFingerprint` + `assembleMarkerBlock` → `writeAtomic` → stderr summary → exit code. **COMPLETE.**
2. **`npx ui-hierarchy-mcp` (no flag)** → `startServer` → MCP `initialize` + `listTools=4` → tool call with `format:"markdown"` returns envelope-aware markdown with HTML-comment warning prefix when warnings present. **COMPLETE.**
3. **POLISH-03 line accuracy** → resolved local-kind component declared past line 1 reports its true `loc.start.line` in `TreeNode.line`. Regression test (`fooNode.line === 3`) green. **COMPLETE.**

## Tech Debt (non-blocking)

**Phase 8:**
- `base.warnings ?? []` fallback in all 4 tool handlers is redundant (buildEnvelope always initializes `warnings: []`). Cosmetic only.
- `__INIT_MARKER_VERSION__` used in `src/init/index.ts` without `typeof` guard, unlike `__TOOL_VERSION__` in `src/mcp/server.ts`. Safe today (runInit is only invoked via dist/cli.js where tsup substitutes the define), but a tsx/vitest direct call would throw `ReferenceError`.

**Carry-forward (from STATE.md, untouched in v1.1):**
- F-01: live Claude Code transcript export still reconstructed from stdio-equivalent capture
- Two orphan exports in `src/mcp/errors.ts` (`notImplemented`, `invalidInput`) — deferred to v1.2 cleanup

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 07-init-file-writer | exists | true | None — green |
| 08-v1-0-polish | missing | n/a | Optional: `/gsd-validate-phase 8` |

## Recommendation

Two options:

**A. Close the gap (recommended).** Run `/gsd-verify-work 8` to produce `08-VERIFICATION.md`. The work is trivial because the integration checker's report above already enumerates the evidence — the verifier just needs to formalize it. Optionally `/gsd-validate-phase 8` afterward to fill the Nyquist gap. Then re-run `/gsd-complete-milestone v1.1`.

**B. Accept and proceed.** Pass `/gsd-complete-milestone v1.1` with `gaps_found` status. The milestone-archive flow will record the gap in MILESTONES.md under `### Known Gaps`. Functionally nothing is missing; the documentation artifact will be the only outstanding item.

---

_Audit performed by: gsd-audit-milestone (orchestrator) + gsd-integration-checker (sonnet)_
