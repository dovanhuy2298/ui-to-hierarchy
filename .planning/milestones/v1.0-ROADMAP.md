# Milestone v1.0: Next.js App Router Parser

**Status:** ✅ SHIPPED 2026-05-05
**Phases:** 1–6
**Total Plans:** 37 (5 + 5 + 6 + 4 + 5 + 10 + 2 inserted gap-closures)
**Published:** `@hudyv2298/ui-hierarchy-mcp` v0.1.0 on npm (commit 24f15da, 2026-05-05)

## Overview

v1.0 delivers an MCP (Model Context Protocol) server that parses Next.js App Router projects and returns a structured UI component hierarchy (markdown tree + JSON) so AI coding agents can ground image/description-based UI edits in exact file/component locations. Distributed via `npx`, four MCP tools exposed (`get_full_hierarchy`, `focus_on`, `find_by_text`, `find_by_style`), all 24 v1 requirements satisfied end-to-end.

## Phases

### Phase 1: Scaffolding & IR Foundation

**Goal:** Project skeleton compiles and IR + renderers are provably correct against hand-written fixtures, independent of any parser.
**Requirements:** OUT-01, ARCH-03
**Plans:** 5

- [x] 01-01: Project scaffold (package.json, tsconfig, tsup, vitest, biome, island dirs, cli stub)
- [x] 01-02: IR zod schema (9-kind discriminatedUnion + envelope)
- [x] 01-03: Babel traverse ESM interop shim + test
- [x] 01-04: paths helper + resolveRoot (ARCH-03)
- [x] 01-05: Renderers (markdown + JSON) + envelope-builder + fixtures + snapshots

**Verification:** passed (5/5)

### Phase 2: MCP Transport Shell

**Goal:** A real MCP client can launch the server via `npx`, discover all four tools with typed schemas, and receive structured "not implemented" errors — with stdout guaranteed clean.
**Requirements:** MCP-01, MCP-02, MCP-03, MCP-04
**Plans:** 5

- [x] 02-01: Wave 0 scaffolding (global.d.ts, test stubs, test:smoke script, biome noConsole, inspector devDep)
- [x] 02-02: src/mcp/errors.ts + src/mcp/log.ts
- [x] 02-03: src/mcp/tools/*.ts (four tool schemas + stub handlers)
- [x] 02-04: src/mcp/server.ts + src/cli.ts replacement
- [x] 02-05: Tier 1 + Tier 2 test implementation + phase gate

**Verification:** passed (5/5) — SC-1 + SC-5 closed by Phase 6 UAT evidence on 2026-05-05

### Phase 3: Parser Core (AST + Resolution + Extractors)

**Goal:** Given any TSX file, the parser produces a framework-agnostic `ComponentDefinition[]` with render flow, style signals, conditional branches, and resolved import paths — exposed behind the 5-method `FrameworkAdapter` contract.
**Requirements:** PARSE-01, PARSE-02, PARSE-03, PARSE-04, OUT-02, OUT-03, OUT-04, ARCH-01
**Plans:** 6

- [x] 03-01: Types + FrameworkAdapter interface + island enforcement test
- [x] 03-02: Babel parse primitive (errorRecovery + 10-plugin set + per-call AST cache)
- [x] 03-03: Resolver: tsconfig paths + barrel chase + cycle guard
- [x] 03-04: Style extractors: Tailwind layout filter + inline + CSS Modules + styled-components
- [x] 03-05: Render-flow walker + HOC unwrap + class component detection
- [x] 03-06: NextJsAdapter orchestrator + kitchen-sink end-to-end

**Verification:** passed (16/16)

### Phase 4: Next.js App Router Adapter

**Goal:** `NextJsAdapter` implements all 5 methods; routing correctly composes layout chains for route groups, parallel slots, intercepting routes, and dynamic segments.
**Requirements:** NEXT-01, NEXT-02, NEXT-03, NEXT-04
**Plans:** 4

- [x] 04-01: types contracts (RouteMatch + runtime field) + kitchen-sink + detect fixtures
- [x] 04-02: detect.ts + discover.ts (R5 + R6) + tests
- [x] 04-03: segments.ts + route-map.ts (NEXT-01/02/03) + tests
- [x] 04-04: NextJsAdapter wiring + runtime plumbing (NEXT-04) + phase gate

**Verification:** passed (4/4)

### Phase 5: IR Queries & Tool Wire-up

**Goal:** All four MCP tools are fully functional end-to-end; `Analyzer` orchestrates per-call pipeline with no cross-call state.
**Requirements:** TOOL-01, TOOL-02, TOOL-03, TOOL-04, ARCH-02
**Plans:** 5

- [x] 05-01: Fixtures (kitchen-sink + parse-error + mutation-test)
- [x] 05-02: Analyzer class (IR-build, slot-substitution, 4 query methods, style sidecar, Levenshtein) — single-file `src/core/Analyzer.ts`
- [x] 05-03: Tier 1 Analyzer unit tests (R1–R8 + ARCH-02 mutation + grep gates)
- [x] 05-04: Wire 4 MCP tool handlers to Analyzer
- [x] 05-05: Tier 2 MCP integration tests (4 tools via InMemoryTransport)

**Verification:** passed (5/5)

### Phase 6: Hardening & Fixture Gates

**Goal:** v1 is provably correct on realistic Next.js project shapes across Windows + pnpm monorepos + shadcn barrels, and end-to-end verified with a real MCP client.
**Requirements:** ARCH-04
**Plans:** 10 (7 base + 3 gap closures)

- [x] 06-01: Wave 0 fixture — shadcn-barrels (R1)
- [x] 06-02: Wave 0 fixture — nested-routes (R2)
- [x] 06-03: Wave 0 fixture — pnpm-monorepo (R3)
- [x] 06-04: Integration suite `mcp-e2e.test.ts` + `test:integration` script (R4 + R5)
- [x] 06-05: Perf script `measure.ts` + 06-PERF.md (R7)
- [x] 06-06: 06-UAT.md template (R6)
- [x] 06-07: Operator UAT — 8/8 PASS, 1 defer (F-01)
- [x] 06-08: Format-symmetric MCP tools + integration argsFor (D-15)
- [x] 06-09: Wire `adapter.resolveModule` into Analyzer IR build
- [x] 06-10: TreeNode `attributes` field + Analyzer prop emission

**Verification:** passed (4/4) — 20/20 integration in 2.50s, 8/8 UAT, 18 security threats closed

## Milestone Summary

### Decimal Phases

None — all insertions stayed within Phase 6 (gap closures 06-08, 06-09, 06-10) instead of becoming new phases.

### Key Decisions

| Decision | Rationale | Outcome |
|---|---|---|
| Multi-framework adapter architecture, NextJsAdapter only in v1 | Future-proof pluggability without paying the cost now | ✓ Good — `core/`/`ir/` island stayed pristine; FrameworkAdapter has exactly 5 methods |
| Query-only tools (no structural edits) | Agents already have Edit; MCP's job is to *inform* edits | ✓ Good — tight scope, fast ship |
| Parse-on-demand (no cache in v1) | Simpler, correctness guaranteed; cache when perf demands | ✓ Good — fresh `Analyzer` per call (ARCH-02) verified by mutation test |
| Markdown (default) + JSON output | Markdown for LLM comprehension, JSON for programmatic traversal | ✓ Good — both surfaces used in UAT |
| App Router only (Pages Router = v2) | Pages Router legacy; focus = depth | ✓ Good |
| Ship as stdio MCP via npm `npx` | Standard MCP distribution; zero-install UX | ✓ Good — published as `@hudyv2298/ui-hierarchy-mcp` |
| `traverse.default ?? traverse` interop shim | Babel ESM/CJS interop is a known footgun | ✓ Good — covered by unit test that fails loudly |
| Per-tool inline `format` param (not shared) | Preserves wire-protocol self-description | ✓ Good (decided in 06-08) |
| TreeNode `attributes` is literal-string-only in v1 | Keeps wire boundary simple | ✓ Good (decided in 06-10) |
| `findByText` returns matched component/element node when attribute matches (not synthetic text node) | file:line points at JSX site that carries the prop | ✓ Good (decided in 06-10) |

### Issues Resolved

- Phase 6 review (REVIEW.md) shipped 5 warnings (WR-01..WR-05) all fixed: bound Case A children-slot injection, default-export preference, parallel-slot warning, synthetic line:0 filtering, markdown backslash escaping
- Babel ESM/CJS interop landmine — fixed once with shim + test
- Windows path discipline — forward-slash normalization at every TreeNode build site, regex gate `/^[^\\]*$/` per node + envelope-wide
- Stdout cleanliness — biome `noConsole: error` for `src/mcp/**` + `src/cli.ts`; smoke test asserts JSON-RPC integrity
- 18 security threats from threat model all closed (06-SECURITY.md)
- Audit gaps (paperwork-only) closed 2026-05-05: 02 status flip, 03 promote, 05/06 VERIFICATION.md produced, 02/04/06 Nyquist gates closed

### Issues Deferred

- **F-01** (Phase 6 UAT): Claude Code transcript was reconstructed from stdio-equivalent capture instead of live in-app export. Methodology footnote, not a contract gap. Deferred to v1.1 backlog.
- Markdown renderer drops `envelope.warnings` — minor: integration test exercises only `format: "json"`; UAT 8/8 PASS validated UX as usable. Deferred to v1.1.
- Two orphan exports in `src/mcp/errors.ts` (`notImplemented`, `invalidInput`) — Phase 2 stubs superseded by Phase 5 wire-up. Deferred to v1.1 cleanup.
- Integration test exercises only `format: "json"` (16/16 invocations) — markdown surface relies on operator-attested UAT. Deferred to v1.1.

### Technical Debt Incurred

- Resolved component nodes use `line: 1` placeholder (ResolveResult exposes only absolutePath); a `discoverComponents` lookup for true declaration line is deferred — file pointer alone satisfies R1/R3 acceptance.
- v1 attributes carve-out: expression/spread/non-string-literal JSX attrs are dropped at the wire boundary.

### Stats

- **Plans:** 37/37 complete
- **Tasks:** ~85 (across phase task lists)
- **Source LOC:** ~4,890 lines TypeScript (`src/`)
- **Test files:** 35
- **Test results:** 256 unit + 20 integration + 8/8 UAT, all green
- **Security:** 18 threats modeled, all closed
- **Perf:** p95 measured for all 4 tools on medium fixture (06-PERF.md)
- **Git activity:** 50 commits in `main` branch
- **Released:** `@hudyv2298/ui-hierarchy-mcp` v0.1.0 on npm

---

*For current project status, see .planning/ROADMAP.md*
